# AIReviewSystem V1.009.1 发布说明

**发布日期**：2026-09-13
**版本类型**：功能大版本 + 修补
**上一版本**：V1.008.3
**说明**：V1.009.1 = V1.009.0 全部功能 + 两处历史数据缺陷修复 + 清洁版隐私加固（见第六节）

---

## 一、新增模块：【A股每日复盘】

左侧导航新增「每日复盘」（路径 `/daily`）。选定日期后一键采集当日盘面全貌，覆盖以下 10 个板块：

| # | 板块 | 内容 |
|---|---|---|
| 1 | 大盘指数 | 上证 / 深成 / 创业板 / 科创50 / 北证50 / 沪深300 / 中证500 / 中证1000，共 8 个（4 列两行） |
| 2 | 涨跌分布 | 涨 / 跌 / 平家数，含涨跌家数比、分档统计（对齐通达信 880005 口径，涨跌停单列不计入） |
| 3 | 涨停梯队 | 首板 / 2板 / 3板 / … 最高板分层，附连板股名单与所属概念 |
| 4 | 炸板池 | 炸板股清单 + 炸板率（分母含炸板股本身） |
| 5 | 跌停池 | 跌停股清单 |
| 6 | 量能 | 两市成交额与较前一日的量能变化 |
| 7 | 概念板块温度 | 板块涨跌幅榜（成分股等权平均）、温度分档统计 |
| 8 | 人气热度榜 | 同花顺热榜 + 东财人气榜（实时：热度值 / 排名变化 / 上榜标签 / 上榜原因） |
| 9 | 龙虎榜与游资 | 龙虎榜个股、买卖席位、游资动向 |
| 10 | 严重异动提醒 | 当日触发严重异动的标的清单 |

另含：

- **AI 盘面点评**：基于采集到的当日数据（程序生成的数值不可被模型篡改）撰写盘面点评
- **手写每日复盘**：支持手写当日复盘，草稿自动缓存（防抖 400ms），误关不丢
- **指标备注**：所有 30 项关键指标均带「?」悬浮备注，显示**指标定义 + 计算公式/口径 + 数据来源**三段

---

## 二、重点能力：60 个交易日内历史盘面完整回溯

此前历史日期只能显示「未采集 / 无免费历史源」，本版做了系统性补齐，**任意历史交易日均可完整回看**：

| 数据 | 回溯方案 |
|---|---|
| 涨停 / 炸板 / 跌停 | 逐日读取全市场日K重建，独立判定涨跌停（见下方口径说明），标 `source=kline` 以区分口径 |
| 涨跌家数 / 成交额 | 由日K全市场重建 |
| 概念板块温度 | 由成分股日K等权平均重建 |
| **人气热度榜** | 通过东方财富个股视角接口逐日回溯全市场人气名次（窗口约 120 自然日），对约 4200 只候选股批量抓取后落库，之后任意历史日**零网络请求**；与实时榜单 top100 重合度 **98%** |

**口径取舍（已在报告中标注）**：历史重建序列统一使用日K口径（东财涨跌停池仅可回溯约 15 日，若混用会造成窗口台阶、破坏情绪周期的连续性）；实盘当日仍使用东财池口径。两者差异约 15%，属于已知取舍。

**涨跌停判定精度**（历史重建的关键）：
- 基数采用**除权参考价**（非除权日 = 不复权前收；除权日按前复权比例折算），避免除权日误判
- 取整方向按市场区分：沪深 `ROUND_HALF_UP`、北交所**不越界**（涨停向下、跌停向上取整）
- 比例反推使用**绝对容差**（普通日 0.005 元 / 除权日 0.015 元），避免高价股被误判
- 涨跌停比例顺序：创业板/科创板 20% → 主板 ST 5% → 北交所 30% → 主板 10%

---

## 三、新增：主线板块角色分层（龙头 / 中军 / 跟风 / 补涨）

「主线题材与周期阶段」表每行可展开，显示该板块当日四类角色股（名称、代码、涨跌幅、连板数、成交额）；「概念板块」列直接显示龙头股（如 `龙头 闽东电力 3板`），无需展开即可扫到。

判定口径（**四类互斥**，判定顺序 龙头 → 中军 → 跟风 → 补涨，**全部剔除 ST**）：

| 角色 | 口径 | 上限 |
|---|---|---|
| **龙头** | 板块内**涨停股**中「连板数 → 当日涨幅 → 成交额」最高者；无涨停则不输出 | 1 |
| **中军** | 市值不低于板块中位数、当日收涨且**未涨停**者中成交额最大 | 2 |
| **跟风** | 其余「涨停 或 涨幅 ≥ 3%」，按涨幅降序 | 5 |
| **补涨** | 其余「涨幅 ≥ 2% 且近 5 日累计涨幅落后板块中位 5 个百分点以上」，按近 5 日累计升序 | 5 |

实时路径与历史重建路径**共用同一套判定入口**，实测 720 组板块角色 **36/36 完全一致**。

---

## 四、修复与优化

1. **修复历史日期「严重异动提醒」整块失败**
   根因：`daily_extra` 模块调用涨跌停判定函数时未导入所属模块，抛 `NameError` 后被兜底 `except` 静默吞掉，导致 60 个重建日中 59 天该区块为空、界面仅显示「无数据」。
   修复后 60/60 天全部成功，每日 13~50 只标的；并做三项加固：兜底异常日志补异常类型与 traceback；**区分 `ok`（检测是否成功执行）与 `available`（当日是否有标的）两个语义**，前端在检测未执行时显示红色告警标签，杜绝「执行失败」被误读成「当日无异常」；报告与数据可得性清单改看 `ok`。

2. **修复次新股被整体剔除导致涨停漏计**
   日K加载的「最少根数」参数被设为 62 根，导致上市不足 62 个交易日的次新股被整体过滤（历史路径缺失、实时路径存在，同一天两个口径对不上）。改为 7 根后多纳入 38 只次新股，修正 09-11 涨停数 39 → 40。

3. **修复清单顺序抖动**
   实时快照与日K计算的涨跌幅存在 ≤0.005 个百分点的末位差异，排序与阈值判定改用两位小数比较后，同类股票顺序与入选结果稳定。

4. **修复非交易日空壳缓存**
   周末查询不再写入空数据缓存，报告文案改为「该日非交易日」而非「未采集」。

5. **清洁版打包修正**
   清洁版清空数据库时关闭外键约束，确保所有表都被正确清空（此前 `trade_screenshots` 的外键约束会导致部分表清不掉）。

---

## 五、本版修复（V1.009.1）

### 5.1 重建好的历史数据，不再被一次「重新抓取」清空

**现象**：跑完「历史数据重建」后，再点一次「重新抓取数据」，重建出来的数据全没了。

**根因**：快照缓存原先只保护了「实盘数据不被回溯覆盖」，而历史日期点「重新抓取」时，
实时接口按历史日期取不到涨跌家数与概念板块，只会生成一份 **partial 残缺快照** ——
它却把已经重建好的完整数据**覆盖掉了**。

**修复**：
- 快照缓存改为按数据质量分级 **live(3) > rebuild(2) > partial(1)**，
  低等级一律不得覆盖高等级（同类缓存有多个质量档次来源时，用等级表比较，而不是逐个 if 列特例）；
- 历史日期**不再提供「实时重抓」**：对历史日而言「重建」是唯一刷新手段，
  点「重新抓取数据」时会直接读取本地数据，并明确提示改用「历史数据重建」。

### 5.2 情绪趋势不再只有最近 15 个交易日

**现象**：情绪趋势图无论重建多少天，始终只有最近 15 个交易日的数据。

**根因**：趋势接口原先逐日调用东财涨/炸/跌停池，而该接口的回溯窗口只有约 **15 个交易日**，
更早日期一律返回空 —— 重建的 60 天数据根本没被用上。

**修复**：趋势接口改为**本地重建缓存优先**（直接复用每个交易日快照里的涨停/炸板/跌停家数与
连板高度，与当日看板同源同口径），只用东财池回补「窗口内且缓存缺失」的日期；
回补结果若为全 0 判为「取不到」而非「当日为 0」，直接丢弃。
前端趋势弹窗新增**「近 60 日」**档位、数据来源统计与缺失日期提示。

### 5.3 清洁版隐私加固（重要）

**问题**：清洁版此前只是 `DELETE` 掉各表数据，而 SQLite 的 `DELETE` **只把页面标记为空闲、
不清零内容** —— 被删的交易记录、复盘正文、资金流水仍留在数据库文件的空闲页里。
实测旧清洁版包：空闲页占比 **57%~59%**，用十六进制查看可直接搜到已删数据的字符串。

**修复**：清库时 `PRAGMA secure_delete=ON`（删除即清零页面）+ 清完 `VACUUM`（重建文件、回收空闲页）。

**效果**：
- 清洁版 app.db **61.8MB → 0.2MB**，空闲页 **0**，文件内搜不到任何已删数据痕迹
- 清洁版 zip 体积 **101.9MB → 82.6MB**

> ⚠️ 若你此前下载或转发过旧版清洁版（V1.008.3-clean / V1.009.0-clean），
> 建议改用本版（V1.009.1-clean）覆盖 —— 旧包可能含发布者本机的历史数据片段。

---

## 六、升级说明

- 直接解压覆盖到原目录即可，`app.db` 与 `uploads/` 请先备份再保留（**不要用清洁版的 app.db 覆盖自己的数据库**）
- 本版新增 5 张数据表（`daily_market_cache` / `board_member_cache` / `market_cache_meta` / `stock_kline_cache` / `stock_hot_rank_cache`），首次启动**自动创建**，无需手工迁移
- 首次进入「每日复盘」并选定历史日期时，需要重建该日数据（全市场日K抓取，约 2 分钟/日）；也可一次性重建近 60 个交易日
- 每日复盘功能需要联网；AI 盘面点评需在【API 设置】中配置模型 Key

## 七、已知取舍

- 历史日的炸板/涨停采用日K口径（最高价触板未封），与实盘当日的东财池口径存在约 15% 差异，报告中有 `source` 字段标注
- 历史日的人气热度榜**不提供**「同花顺热度值 / 上榜原因 / 上榜标签」（这些字段无任何历史数据源），仅提供东财人气排名；前端在历史模式下自动隐藏对应列
- 历史分钟级数据不可得，因此无法复现「盘中封板后开板」的完整过程
