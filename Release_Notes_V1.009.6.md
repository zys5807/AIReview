# AIReviewSystem V1.009.6 发布说明

**发布日期**：2026-09-15
**版本类型**：核心口径重写 + 功能新增
**上一版本**：V1.009.5
**说明**：V1.009.6 = V1.009.5 全部内容 + 持续性口径重写（去规模歧视）+ 资金容量并入板块涨幅 + 新增「今日启动」信号 + 概念涨幅榜「首日 / 延续 / 无涨停」标注 + 两处静默分叉修复

> ⚠️ **本版必须重做「历史数据重建」**：板块历史序列原本不落「上涨家数」字段，持续性新口径的条件 c 在历史日恒不成立、在榜天数会静默偏少（不报错）。升级后请执行一次「历史数据重建」（约 1~3 分钟）。

---

## 一、持续性口径重写：从「绝对名次」到「规模无关的三条件」

**问题原话**：*「持续性的算法可能还需要改一下，涨停家数进入市场前 15，没有考虑到板块个股数量不一样的规模问题」*

### 旧口径的规模歧视

旧持续性问的是：*近 5 日里，有几天「板块有涨停 且 涨停家数进全市场概念前 15 名」*。

问题出在「前 15 名」这个**绝对名次**上 —— 它对不同规模的板块意味着完全不同的难度：

| 板块 | 成分股数 | 要进前 15 名需涨停家数 | 折算占比 |
|---|---|---|---|
| 20 只的细分题材（如培育钻石） | 20 | 7 ~ 9 家 | **35% ~ 45%** |
| 985 只的央国企改革 | 985 | 约 9 家 | **0.9%** |

同一个「前 15 名」，在小板块要几乎所有票封板，在大板块几乎什么都不用做，**含义相差约 50 倍**。

实测后果：候选池内 `persist > 0` 的板块，成分股数最小是 98 只 —— **成分股 ≤42 只的小板块，在榜命中率是 0.0%**，它们在持续性维度上被系统性清零。

### 新口径：三条件复合，与规模完全无关

某日「在榜」需**同时**满足：

| 条件 | 内容 | 作用 |
|---|---|---|
| a | 板块涨幅 > 上证指数涨幅 | 保证是「强于大盘」，而不是被普涨带起来的 |
| b | 板块内有涨停（≥1 家） | 保证有资金在封板（攻击证据） |
| c | 上涨家数 > 成分股数 × 60% | 保证是板块内**多数个股**在涨，而不是一两只票拉指数 |

分值 = 近 5 日「在榜日」按 `[1.0, 0.8, 0.6, 0.4, 0.2]` 近端加权后**除以权重和**。

几个细节：
- 条件 c 的分母是**当日有效成分股数**（停牌股会改变当日基数），不是静态板块规模；
- 条件 c 要求**严格大于** 60% —— 恰好 60% 不算在榜；
- 新口径**与成分股数量完全无关**：20 只和 985 只的板块，只要满足同样的比例就同样在榜。

---

## 二、去掉「覆盖度乘子」（实现中发现的关键修正）

**这是本版最容易漏掉的一处**。按上面的公式实现后，持续性维度**静默失效**了 —— 不报错，但等于没改。

原因是「覆盖度乘子」：

```
旧实现：持续性 = 近端加权率 × (命中天数 ÷ 窗口)
```

这个乘子是为**旧定义**校准的 —— 旧口径下真主线能连续 5 天在榜、拿到 1.0，乘 1.0 覆盖度不损失什么。但**新条件严得多**：65 个交易日里没有任何板块能做到连续 3 天满足三条件，加权率最大只有约 **0.47**。再乘上 0.2~0.4 的覆盖度，全都被压到 **0.07** 一带：

| 变体 | 候选池内持续性标准差 | 是否存活 |
|---|---|---|
| 原案（AND + 覆盖度） | 0.0489 | ❌ **死** |
| **去掉覆盖度（本版采用）** | **0.1626** | ✅ 活 |
| 软合成（0.4/0.3/0.3） | 0.1049 | ✅ 活 |

「死」的含义：标准差低于 `0.05` 时会触发自适应权重机制，该维度被判定为**无区分力**，权重被按比例转移给其余三维 —— **持续性权重直接归零**，培育钻石仍排第 53 名。

而且这个乘子在数学上本就是**重复惩罚**：近端加权 `[1.0, 0.8, 0.6, 0.4, 0.2]` 已经把「昨天在榜」的权重压到「5 天前在榜」的 5 倍，再乘一次命中比例等于把同一件事算了两遍。

去掉乘子后：
- 候选池内标准差 **0.1626**，65/65 天全部存活；
- 区分度没有丢：「一日游」= **0.333**，「连续 5 天在榜」= **1.0**。

---

## 三、资金容量并入板块涨幅

**问题原话**：*「amt 还是涨停个股没问题，我认为可以再把板块涨幅这个数据考虑进去」*

容量维度原本只看「涨停股成交额合计」。这会漏掉一类重要的资金 —— **大涨但没封板的**。

实测 09-14「培育钻石」：系统只算到 **3.56 亿**（涨停股成交额），而当天它的涨停/大涨股成交额实际接近 **百亿**：

| 个股 | 当日成交额 |
|---|---|
| 黄河旋风 | 48.93 亿 |
| 四方达 | 28.51 亿 |
| 力量钻石 | 22.56 亿 |
| 英诺激光 | 14.95 亿 |

**差 30 倍**，容量分位只有 **0.119** —— 一个当天资金最活跃的方向，在「上不上得了仓位」这一维上被判为垫底。

### 新公式

```
资金容量 = 0.4 × 涨停股成交额分位 + 0.6 × 板块涨幅分位
```

板块涨幅由**全部成分股等权平均**得出（与东财板块指数同口径），正好把漏掉的大涨股补回来。

### 配比是标定出来的，不是拍的

| 涨停股成交额权重 w | 培育钻石排名 | CRO 排名 | 两者都进前 12 |
|---|---|---|---|
| 1.0（旧口径） | #18 | #17 | ✗ |
| 0.8 | #12 | #15 | ✗ |
| 0.6 | #9 | #14 | ✗ |
| 0.5 | #7 | #13 | ✗ |
| **0.4（本版采用）** | **#7** | **#11** | ✅ |
| 0.2 | #6 | #11 | ✅ |
| 0.0（纯涨幅） | #6 | #12 | ✅ |

**为什么停在 0.4 而不再往下调**：CRO 的临界点在 w ≈ 0.45，属于薄冰 —— 继续为「让某个板块进榜」调参，就是在对**单日数据过拟合**。0.4 是「两者都进」的最小改动量，保留了对成交额信息的独立权重。

排名变化（09-14，候选池 150 个）：

| 板块 | 旧口径 | 本版 |
|---|---|---|
| 培育钻石 | 第 58 名 | **第 6 名** |
| CRO | 第 40 名 | **第 12 名** |

（持续性 0.0000 → 0.4667 / 0.4000，容量 0.119 → 0.6407、0.446 → 0.7780）

---

## 四、新增「今日启动」独立信号

**问题原话**：*「今日资金流向哪只是当日的一个静态表现，信息量不足，无法筛掉一日游题材」*

### 为什么需要它：四维打分的结构性盲区

四维里有 **60% 权重靠时间积累**（持续性 0.30 + 空间高度 0.30）。一个**今天才启动**的题材，这两维结构上就是 0 —— 不管它今天多强，总分一定低。

实测 09-14 的极端案例：

| 板块 | 当日涨幅排名 | 主线表排名 |
|---|---|---|
| CRO | 第 **1** | 第 40 |
| 培育钻石 | 第 **2** | 第 58 |

**涨幅榜前 20 名里，只有 2 个进了主线表**。这不是调参能解决的，而是「历史组占 60%」的必然结果 —— 需要一条**不依赖历史**的并行信号来兜。

### 信号定义

板块同时满足：
1. 涨幅进入全市场概念**前 20 名**；
2. 涨停占板块比 **≥ 3%**。

→ 单列展示，**不参与四维排序、不进主线表**，避免一日游题材污染主线判断。

用「涨停占板块比」而不是涨停家数，理由与持续性口径一致 —— 家数门槛对大小板块的含义完全不同。

每一条都标注**是否已进主线表**：
- 已在主线表 → 说明它**既在启动、也通过了四维打分**；
- 不在 → 说明**只满足启动条件**，属于「刚开始动」，是更早期的信号。

09-14 实测命中 **8 个**：CRO（+4.54% / 涨停占比 4.1%）、培育钻石（+4.48% / 5.0%）、VPN（+3.58% / 5.3%）、MLCC（+3.31% / 5.1%）、汽车整车（+2.81% / 13.0%）、复合集流体（+2.26% / 3.3%）、数据安全（+2.22% / 4.9%）、PCB（+2.19% / 4.0%）。

---

## 五、概念涨幅榜新增「首日 / 延续 / 无涨停」标注

**问题原话**：*「概念涨幅榜那里，可以标注一下是不是首日启动」*

判定方式：回看**此前**最近 10 个交易日，该板块是否有过「在榜日」。

| 标注 | 条件 | 操作含义 |
|---|---|---|
| **首日** | 此前无在榜日 **且** 今日有涨停 | 右侧第一买点 |
| 延续 | 此前有在榜日 | 追高 |
| 无涨停 | 今日 0 家涨停 | 启动/延续都不判定 |

### ⚠️ 回看窗口必须剔掉当日

这是实现中的一个语义陷阱：如果把当日算进回看窗口，「今天首次在榜」会得到**命中 1 次**，于是被判为「不是首日」—— 恰好把**最该提示的真·启动首日**漏掉，只剩下「从未在榜过」的板块才会显示首日，语义是**反的**。

修正后，VPN / 减肥药 / 基因测序 这类「今日才首次在榜」的板块能正确显示为首日。

### 为什么需要第三态「无涨停」

在榜条件 b 要求有涨停，于是 **0 涨停的板块永远没有在榜日**，会被**永久标成「首日」**。

实测 09-14 会误标：CAR-T细胞疗法（+2.68%）、电子纸概念（+2.45%）、青蒿素（+2.31%）—— 都是「涨幅高但无人封板」的板块。按这个标签去交易是**错误信号**，因此单独成一态、明确显示「无涨停」而不是红色「首日」。

---

## 六、两处静默分叉修复

### ① 实时路径的涨停家数恒为 0

东财实时板块行（`clist` 接口）**没有涨停家数字段** —— 涨停家数是由「涨停股 → 所属概念」另算出来的。

当日日截面与今日启动信号此前直接取 `row["zt"]`，于是：

- **实时日**：条件 b「有涨停」恒假 → 持续性恒 0；
- **历史重建日**：行里带 `zt`，一切正常。

形成了一条**只在实时路径出现、且不报错的静默分叉** —— 用户看到的是「今天所有板块持续性都是 0」，而看不出这是 bug。

修复：统一收口为 `_zt_cnt` 映射（`{板块代码: 涨停家数}`），所有需要当日涨停家数的地方都过这个映射。

### ② 条件 c 的分母两套尺度

- 历史行 `size` = 当日有效成分股数（`count`）；
- 实时行 `size_map` = 上涨 + 下跌 —— **漏掉了平盘股**，会偏小。

同一个 60% 阈值作用在两套尺度上，等于口径不一致。修复：实时行优先取 `count`。修完 CRO 从第 13 名进到第 12 名。

---

## 七、升级后必须做的操作

**本版改了口径，且历史序列需要补字段，升级后请执行一次「历史数据重建」**（约 1~3 分钟）：

- 板块历史序列原本不落「上涨家数」字段：504 板块 × 65 天 = **32760 行，含该字段的行为 0**；
- 不重建的后果：条件 c 在历史日恒不成立 → 在榜天数**静默偏少**，持续性维度整体偏低，但**不会报错**；
- 重建后实测：**32760 / 32760 行**全部带上「上涨家数」。

---

## 八、验证

| 项 | 结果 |
|---|---|
| 单元测试 | **112 / 112 通过** |
| 三条件各自必须成立 | ✅ 缺一不算在榜 |
| 条件 c 边界 | ✅ 恰 60% 不算、61% 算 |
| 规模无关性 | ✅ 20 只与 985 只板块持续性均 1.0000 |
| 缺上证指数降级 | ✅ 降级为「涨幅 > 0」，不静默归零 |
| 覆盖度乘子移除 | ✅ 标准差 0.0489 → 0.1626 |
| 65 天稳健性 | ✅ 存活 65 / 65 天（原口径 61/65） |
| 容量配比 | ✅ 培育钻石 #18→#7、CRO #17→#11 |
| 今日启动命中 | ✅ 09-14 共 8 个 |
| 首日标注 | ✅ 剔当日修正后 VPN / 减肥药 / 基因测序 正确显示首日 |
| 无涨停误标 | ✅ 被挡住，显示「无涨停」而非「首日」 |
| 静默分叉修复 | ✅ 实时路径 `zt` 与历史同口径 |

---

## 九、已知局限（未修，待后续）

四维打分对「**启动首日 / 重启首日**」题材的结构性低分**仍然存在** —— 本版新增的「今日启动」信号是**绕过**而非**修复**了这个问题。两个更根本的缺陷已定位但未动：

1. **候选池的规模偏好**：`persist > 0` 的成分股数下限仍是 98 只，成份股少的小板块在资格线阶段就容易被筛掉；
2. **容量口径**：并入板块涨幅只是部分补偿，`amt` 仍只统计涨停股。若某天全市场封板意愿低，容量维度会整体失真。

这两项需要单独一轮设计，不在本版范围内。
