# 腾讯文档《情绪周期确认方法》核对 —— **正确文档版**

> 日期：2026-09-18
> 文档：`https://docs.qq.com/aio/DSk9MSEJkSFhYY0dG`
> 分享 ID `DSk9MSEJkSFhYY0dG` → **file_id `JOLHBdHXXcGF`**（smartcanvas，8 页，约 19,500 字符）
> **本文作废并替代 `情绪周期确认方法_文档核对_20260918.md`**（那份误读了另一篇 `DSmRWUUp4clRSeWdm` / `JdVQJxrTRygf`）
> 状态：**只读核验，未改任何生产代码、未动版本号、未改数据库**

---

## 0. 先纠错：我上次读错了文档

| 项 | 上次读的（❌ 作废） | 本次读的（✅ 正确） |
|---|---|---|
| 分享 ID | `DSmRWUUp4clRSeWdm` | `DSk9MSEJkSFhYY0dG` |
| file_id | `JdVQJxrTRygf` | `JOLHBdHXXcGF` |
| 标题 | （无独立标题，正文直接两问） | **《情绪周期确认方法》** |
| 内容 | ① 一字缩量+涨停增加=高潮？② 老龙断板+新龙=？+ 五项判定表 | **你给元宝的原始需求 + 元宝的 HMM 式量化方案** |
| 体量 | 3 页 5,640 字符 | 8 页 约 19,500 字符 |

**作废清单**（均需按本文重写）：

- `情绪周期确认方法_文档核对_20260918.md` —— 全文作废
- `.workbuddy/memory/2026-09-18.md` §7 —— 需纠正
- `.workbuddy/skills/aireview-emotion-cycle/SKILL.md` 「难题十三」—— 基于错误文档，需改写

**但那次也不是全白做**：错误文档里那套「五项判定表」（老龙收盘 / 新龙属性 / 活口 / 跌停 / 溢价）**本身是可算的**，PCB 9/15 自动判出「强分歧」与你手工结论一致 —— 这条独立结论仍然有效，只是**它不属于《情绪周期确认方法》这篇**。可另立一篇存放。

---

## 1. 这篇文档的真正价值：它把你 6-24 的原始需求 + 一套**概率式状态机**放在了一起

文档结构（8 页）：

| 页 | 内容 | 关键性 |
|---|---|---|
| 1 | **你的原始需求**（六状态定义 + 完整转移规则 + 「本质是赚钱效应的扩散和收缩」）+ 元宝七项指标表 + **先验转移概率矩阵** | ⭐⭐⭐ |
| 2 | 冰点/启动/发酵三态**似然打分条件**；随后你追问「可转移状态及先验概率是什么意思」 | ⭐⭐⭐ |
| 3 | 元宝解释：可转移状态 = **规则约束**，先验概率 = **约束力度** | ⭐⭐ |
| 4 | **综合得分示例**（0.7×0.8=0.56 vs 0.3×0.2=0.06 → 判冰点）+ 你追问「历史数据有情绪周期标签吗」 | ⭐⭐⭐ |
| 5 | 「先有标签再统频」路线：`â_ij = N(i→j) / Σ_k N(i→k)` + 平滑 `a_final=(1-α)·a_emp+α·a_manual`（α 0.1~0.3） | ⭐⭐⭐ |
| 6-7 | 「没人工标签：HMM 无监督学」（Baum–Welch 估 π / A / B，Viterbi 解序列，末了手动把不允许的 edge 置 0 重归一） | ⭐⭐ |
| 6-7 | **概念板块口径四个坑** + 冷启动四步建议 | ⭐⭐⭐ |

---

## 2. 你的原始需求原文（文档第 1 页，逐字）

> 一个概念板块的情绪周期分为冰点，启动，发酵，高潮，分歧，退潮……我希望能实现闭环，状态间的切换要连续，**不要存在启动到发酵之间有状态遗漏**。
>
> **冰点只能到启动**，启动和退潮都是**一日状态**，启动后可以接发酵、可以接高潮、可以退潮；发酵后可以接高潮、可以接分歧、可以接退潮；高潮后可以接分歧、可以接退潮；分歧后可以接高潮、可以接退潮；退潮后可以接启动、可以接冰点。
>
> 这个周期的本质其实是**赚钱效应的扩散和收缩**：
> - 冰点 = 毫无赚钱效应
> - 启动 = 试错资金第一次有了赚钱效应
> - 发酵 = 场外资金看到试错资金有赚钱效应后跟进，也有赚钱效应
> - 高潮 = 场外资金疯狂跟进，场内资金惜售
> - 分歧 = 场内获利了结、场外认为赚钱效应还在 → **场内场外换手**
> - 退潮 = 场外不再认为有赚钱效应，谨慎入场；场内获利了结或短线止损 → **当日整体亏钱**

### 2.1 从这段原文能推出的「合法边集」（本次新增的可证伪基准）

原文**没有**给边表，但表述是完备的，逐句转写如下：

| 当前态 | 原文允许的出边 | 原文**未提**的（= 禁止） |
|---|---|---|
| 冰点 | 启动 | 冰点自环 ❌、其余全部 ❌ |
| 启动 | 发酵、高潮、退潮 | 启动自环 ❌（「一日状态」） |
| 发酵 | 高潮、分歧、退潮 | 发酵自环 ❌、→启动 ❌、→冰点 ❌ |
| 高潮 | 分歧、退潮 | 高潮自环 ❌、**→发酵 ❌**、→启动 ❌、→冰点 ❌ |
| 分歧 | 高潮、退潮 | 分歧自环 ❌、**→发酵 ❌**、→启动 ❌、→冰点 ❌ |
| 退潮 | 启动、冰点 | 退潮自环 ❌（「一日状态」） |

**⭐ 重大发现：这份原文的边表与你 09-18 的裁定「高潮可以连续」「分歧可以延续」相互矛盾。**

- 原文（6-24）：高潮**只能**→分歧/退潮，即**禁止高潮自环**
- 你 09-18 裁定：**「高潮可以连续」** → 允许高潮自环
- 原文写「发酵后可以接高潮/分歧/退潮」，也没写发酵自环；但元宝的矩阵给了 0.4

**这是时间上的口径演进，不是谁错**。要点是：**你 6-24 写下的原始需求，比 09-18 的裁定更严格**。如果以原需求为准，那 09-18 那条「高潮可以连续」是**放宽**。

> ⚠️ **需要你定**：合法边表以哪个为准？
> - 用 **6-24 原需求版**（严格）：则生产当前「高潮自环 41 条」「分歧自环 175 条」**全属违规**，违规率会从 16.4% 跳到 **21.3%**
> - 用 **09-18 裁定版**（放宽，也是现在技能里定稿的那份）：违规率 16.4%

---

## 3. ⭐⭐ 元宝的「先验转移概率矩阵」vs 生产实测 —— 这是本文最有价值的量化

元宝给的矩阵（文档第 1 页，直接抄录）：

| 当前状态 | 可转移状态及先验概率 |
|---|---|
| 冰点 | 冰点 0.7，启动 0.3 |
| 启动 | 发酵 0.4，高潮 0.3，退潮 0.3 |
| 发酵 | 发酵 0.4，高潮 0.3，分歧 0.2，退潮 0.1 |
| 高潮 | 高潮 0.2，分歧 0.5，退潮 0.3 |
| 分歧 | 分歧 0.3，高潮 0.4，退潮 0.3 |
| 退潮 | 启动 0.5，冰点 0.5 |

### 3.1 实测对照（生产全量重算：23,668 板块×日 / 334 板块 / 活跃态内转移 4,179 条）

| 昨日 | 今日 | 实测条数 | 实测占比 | 文档先验 | 文档允许？ |
|---|---|---:|---:|---:|---|
| **冰点** | 冰点 | 468 | **70.1%** | **0.70** | ✅ |
| 冰点 | 启动 | 107 | 16.0% | 0.30 | ✅ |
| 冰点 | 发酵 | 93 | 13.9% | — | ❌ |
| **启动** | 退潮 | 229 | 38.6% | 0.30 | ✅ |
| 启动 | 分歧 | 201 | 33.9% | — | ❌ |
| 启动 | 发酵 | 151 | 25.5% | 0.40 | ✅ |
| 启动 | 高潮 | 12 | 2.0% | 0.30 | ✅ |
| **发酵** | 退潮 | 648 | 40.7% | 0.10 | ✅ |
| 发酵 | 发酵 | 455 | 28.6% | 0.40 | ✅ |
| 发酵 | 分歧 | 398 | 25.0% | 0.20 | ✅ |
| 发酵 | 高潮 | 92 | 5.8% | 0.30 | ✅ |
| **高潮** | 退潮 | 69 | 35.0% | 0.30 | ✅ |
| 高潮 | 分歧 | 54 | 27.4% | **0.50** | ✅ |
| 高潮 | 高潮 | 41 | 20.8% | **0.20** | ✅ |
| 高潮 | 发酵 | 33 | 16.8% | — | ❌ |
| **分歧** | 退潮 | 332 | 40.1% | 0.30 | ✅ |
| 分歧 | 发酵 | 268 | 32.4% | — | ❌ |
| 分歧 | 分歧 | 175 | 21.1% | 0.30 | ✅ |
| 分歧 | 高潮 | 53 | 6.4% | **0.40** | ✅ |
| **退潮** | 冰点 | 208 | 69.3% | **0.50** | ✅ |
| 退潮 | 发酵 | 92 | 30.7% | — | ❌ |
| 退潮 | 启动 | 0 | 0.0% | 0.50 | ✅ |

**总违规 687 / 4,179 = 16.4%**

**⭐ 与上一轮用「09-18 定稿合法边表」独立算出的 16.2% 几乎完全一致** —— 两条独立路径互相印证，这个数字可信。

### 3.2 ⭐⭐ 自环率：四处惊人吻合，一处结构性归零

| 状态 | 实测自环率 | 文档先验 | 差异 |
|---|---:|---:|---|
| **冰点** | **70.1%** | **0.70** | ✅ 几乎完全吻合 |
| **高潮** | **20.8%** | **0.20** | ✅ 几乎完全吻合 |
| 发酵 | 28.6% | 0.40 | 同量级（偏低） |
| 分歧 | 21.1% | 0.30 | 同量级（偏低） |
| **启动** | **0.0%** | **0（一日状态）** | ✅ 结构性 100% 吻合 |
| **退潮** | **0.0%** | **0（一日状态）** | ✅ 结构性 100% 吻合 |

**解读**：

1. **冰点 / 高潮的自环率与元宝的凭经验给值几乎逐位吻合（70.1% vs 70%、20.8% vs 20%）** —— 这说明生产状态机在这两个态上输出稳定，并且**与你对市场的直觉一致**。这是个很强的正面信号。
2. **启动 / 退潮自环实测严格为 0** —— 你 6-24 写的「一日状态」在实现上是**结构性成立**的（分支 B 无法输出启动自环；退潮在活跃态内被 A1/A1b 优先捕获后次日必然流向别处）。这条**完全对上了**。
3. **分歧自环实测 21.1% 但元宝给 0.30**，且技能里已知「分歧有 126 段多日（19.3%，最长 6 天）」→ 说明「分歧可延续」在**数据上早就成立**，你 09-18 的裁定只是追认了既成事实。
4. **发酵自环 28.6% vs 0.40** —— 生产比文档**更保守**。原因：发酵的 15,930 个出口里「退潮」占 40.7% 最高，生产在活跃态内 A1 判退潮优先级最高，把大量本该「发酵延续」的日子推给了退潮。

### 3.3 ⭐ 4 条违规边，与上一轮独立结论**逐条对上**

| 违规边 | 本次条数 | 上一轮（09-18 定稿边表） | 性质 |
|---|---:|---:|---|
| 分歧 → 发酵 | **268** | 268 | 你已裁定「分歧可延续」→ 这 268 天**应改判** |
| 启动 → 分歧 | **201** | 201（你已裁定合法） | 你 09-18 已裁定**合法**→ 应补进边表 |
| 冰点 → 发酵 | 93 | 93 | 分支 B 直判发酵（699 天的子集） |
| 退潮 → 发酵 | 92 | 92 | 同上 |
| 高潮 → 发酵 | 33 | 33 | 唯一你明确否掉的边 |

**如果采纳你 09-18 的裁定（分歧→发酵合法、启动→分歧合法）+ 采纳「延续分歧」**：
- 违规从 687 → **126 条**
- 违规率 16.4% → **3.0%**

---

## 4. ⭐⭐⭐ 文档七项指标 vs 生产字段 —— 覆盖率实测（本次新做的硬核部分）

文档第 1 页给了七项指标表（另在第 6 页改进项里提到 `D_t` 跌停家数）：

| 符号 | 含义 | 生产字段 | 全期样本 | 非空条数 | **非空率** |
|---|---|---|---:|---:|---:|
| `L_t` | 当日涨停家数 | `zt` / `count` | 1080 | 1080 | **100.0%** |
| `H_t` | 最高连板高度 | `lbc_max` | 1080 | 1080 | **100.0%** |
| `M_t` | 昨日涨停股今日平均盈亏 | `prem` | 1080 | 482 | ⚠️ **44.6%** |
| `P_t` | 涨停晋级率 | `promo` | 1080 | 482 | ⚠️ **44.6%** |
| `Z_t` | 炸板率 | `zb_r` | 1080 | 941 | 87.1% |
| `R_t` | 上涨家数占比 | `up_r` | 1080 | 1080 | **100.0%** |
| `V_t` | 量能比 | ❌ **无字段** | — | — | — |
| `D_t` | 板块跌停家数 | ❌ **无字段** | — | — | — |

**结论：七项里 5 项已落库且可用，2 项需新增，且最关键的 `M_t`/`P_t` 有 55.4% 缺失。**

### 4.1 ⚠️ `M_t` / `P_t` 缺失 55.4% 的根因（实测定位）

`daily_market.py:3240-3247`：

```python
# 晋级率 = 昨日涨停股今日再涨停 ÷ 昨日涨停数
promo = (len(set(zt_codes) & prev_zt) / len(prev_zt)) if prev_zt else None
# 接力溢价 = 昨日涨停股今日平均涨幅
prem = None
if prev_zt:
    _ps = [(code_pct.get(c) or {}).get(d) for c in prev_zt]
    _ps = [x for x in _ps if x is not None]
    if _ps:
        prem = sum(_ps) / len(_ps)
```

**根因**：两者都依赖 `prev_zt`（**本板块昨日涨停股集合**）。当板块**昨日无涨停**时 `prev_zt` 为空 → 直接返回 `None`。

**这是文档方案在本系统上最大的工程障碍**：
- 文档把 `M_t`（昨日涨停股今日均收益）定为「**判断赚钱效应最直接的指标**」
- 但冰点/潜伏区恰恰**大量是「昨日无涨停」的板块** → `M_t` 天然为 `None`
- 文档用它判「冰点：`M_t < 0` 越显著越高」→ 恰恰在最需要它的场景**没数据**

> 元宝自己也提到了这个坑（第 6 页）：「**小概念样本薄**：很多概念日涨停为0、连板为0，直接算晋级率会空值。建议用『全市场短线情绪 + 本概念相对强度』双轨」。**你这份文档在写的时候就预见到了这个问题，但方案没落到底。**

### 4.2 `V_t` 可算性验算（按 K 线 `bar[3]` 逐股累加）

先避开一个已知陷阱：

> ⛔ `row["amt"]` **不是板块成交额**，是**当日涨停股成交额合计**（只在 `_lim==1` 累加）→ 冰点日 100% 为 0，**绝不能用来算 `V_t`**。

按 K 线 `bar[3]`（成交额）逐股累加实测（成分股是 **dict 列表**，取 `m["code"]`）：

| 板块 | 日期 | 当日成交额 | 命中股数 | 前5日均额 | **V_t** | 成分数 |
|---|---|---:|---:|---:|---:|---:|
| 交运设备 | 2026-09-15 | 104.9亿 | 48 | 129.2亿 | 0.812 | 52 |
| 交运设备 | 2026-09-16 | 78.3亿 | 44 | 125.3亿 | 0.625 | 52 |
| 汽车整车 | 2026-09-15 | 134.2亿 | 23 | 96.9亿 | **1.384** | 26 |
| 汽车整车 | 2026-09-16 | 88.9亿 | 20 | 107.9亿 | **0.824** | 26 |
| 机器人概念 | 2026-09-15 | 2302.1亿 | 770 | 2610.6亿 | 0.882 | 772 |
| 机器人概念 | 2026-09-16 | 2108.7亿 | 723 | 2507.4亿 | 0.841 | 772 |
| CPO概念 | 2026-09-15 | 1787.4亿 | 92 | 2241.0亿 | 0.798 | 92 |
| CPO概念 | 2026-09-16 | 2074.9亿 | 81 | 2116.6亿 | 0.980 | 92 |

**结论：`V_t` 完全可算，日命中率 ≥ 85%（缺口是停牌/新股无 bar）。** 两个口径都要注意：
- **成交量/额用不复权** —— 除权不改变实际成交金额，`bar[3]` 正是额
- **新股上市首日无前 5 日** → `V_t` 为 `None`，文档未给缺省处理

---

## 5. ⭐⭐⭐ 文档「状态似然打分条件」实测 —— 严格版几乎不可用，宽松版判别力强

文档第 2 页给的打分条件（原文）：

| 状态 | 文档条件 |
|---|---|
| 冰点 | `M_t < 0` 越显著越高；`L_t` 近 60 日分位 `<0.2`；`H_t ≤ 2`；`Z_t > 0.5` |
| 启动 | `M_t > 0` 且绝对值不大；`L_t` 较昨日增加；`H_t ∈ {2,3}`；`P_t ≥ 0.5`；`V_t` 温和放大 |
| 发酵 | `M_t` 保持为正；`L_t` 分位数在 ▮（**文档此处原文被截断，未给上界**） |

### 5.1 单条条件在各实际阶段上的命中率（分母 = 该阶段可算样本；样本 1,639 条 / 72 天）

| 条件 | 启动 | 发酵 | 高潮 | 分歧 | 退潮 | 潜伏 |
|---|---:|---:|---:|---:|---:|---:|
| 冰点·`M_t<0` | 5.5% | 4.3% | 0.8% | 5.8% | **31.4%** | 17.5% |
| 冰点·`H_t<=2` | 100.0% | 61.8% | 18.8% | 59.6% | 36.1% | 88.5% |
| 冰点·`Z_t>0.5` | 1.9% | 2.5% | 1.5% | 7.9% | 0.0% | **10.7%** |
| 启动·`M_t>0` | 93.6% | 95.4% | 99.2% | 94.2% | 68.6% | 82.1% |
| 启动·`0<M_t<3%` | 17.4% | 10.7% | 14.3% | **34.9%** | **31.4%** | 17.5% |
| 启动·`H_t∈{2,3}` | 38.0% | 55.6% | 30.8% | **65.2%** | 52.8% | 14.2% |
| 启动·`P_t>=0.5` | 51.4% | 59.6% | 48.9% | **19.8%** | 34.3% | 45.9% |
| 发酵·`R_t>=0.5` | 100.0% | 95.7% | 86.5% | 68.5% | **0.0%** | 73.0% |

### 5.2 组合后的判别力（lift = 该状态命中率 / 该状态在全体占比）

| 组合 | 启动 | 发酵 | 高潮 | 分歧 | 退潮 | 潜伏 |
|---|---:|---:|---:|---:|---:|---:|
| 冰点全 4 条 | 0.00× | 0.00× | 0.00× | 0.21× | 0.00× | 0.00× |
| 冰点前 2 条（`M<0 & H≤2`） | 0.21× | 0.12× | 0.00× | 0.83× | **5.06×** | 0.15× |
| 启动 4 条 | 0.00× | 0.02× | 0.09× | 0.21× | 1.26× | 0.02× |
| **启动宽松版（`M>0 & H∈{2,3}`）** | **2.53×** | 1.82× | **3.80×** | **11.79×** | **18.97×** | 0.32× |
| **发酵宽松版（`M>0 & R≥0.5`）** | 3.58× | **2.45×** | **10.56×** | **11.59×** | 0.00× | 0.42× |

### 5.3 三条硬结论

1. **文档的严格条件是「联立 AND」，本系统数据上几乎永不成立**：
   - 冰点全 4 条 → 全期只命中 **2 条（0.1%）**
   - 启动 4 条 → 命中 **11 条（0.7%）**
   - **原因就是 §4.1 的 `M_t`/`P_t` 缺失 55.4%** —— 只要样本里 `M_t` 为 `None`，整条 AND 直接判 `None`，把样本量砍掉一半以上
   - **如果照搬这套规则，91%+ 的板块日会落到「无法判定」**

2. **宽松版判别力可观，且指向的结论与生产当前做法一致**：
   - `启动(M>0 & H∈{2,3})` 在**启动组 lift 2.53×**，与生产用「高度仅 2 板 + 涨停强度分位」判启动**方向相同**
   - `发酵(M>0 & R≥0.5)` 在**发酵组 lift 2.45×**，与生产 `_EMO_FERM_UPR=0.50`（扩散度 ≥50%）**数值上几乎重合** —— **这是巧合还是印证？我倾向印证**：你 6-24 的原始描述里「发酵 = 场外资金跟进」对应「扩散度高位」，生产把阈值定在 0.50 与文档的 `R_t≥0.5` **逐位相同**。

3. **⚠️ `H_t∈{2,3}` 不是「启动」特征，而是「高位撕裂」的伴随特征**：
   - 它在**分歧组 lift 11.79×、退潮组 18.97×**，远高于启动组的 2.53×
   - **这条不能用** —— 若拿它判启动，会把大量高位撕裂日误判成启动
   - 这与上一轮的独立发现一致：「一字板 `ow_n` 只作展示 + 高潮佐证，不改判定」（高潮 38.8% vs 分歧 35.0% 太接近）

4. **`M_t<0` 只在「退潮」上有特异判别力**：退潮组 31.4% vs 其余组 0.8~17.5%。
   这与生产 `_EMO_FADE_JUN_RS`/A1b 的思路一致 —— **`M_t` 是退潮证据，不是冰点证据**。文档把 `M_t<0` 定为**冰点**首要条件的做法，**在本系统数据上站不住**（冰点组可算样本 0 条，冰点全期只有 16 条有值）。

---

## 6. ⭐⭐⭐ 元宝方案最有价值的地方 = 「综合得分 = 先验转移概率 × 当日似然」

文档第 3~4 页的核心机制：

```
从冰点转移为冰点的综合得分 = 0.7 × 0.8 = 0.56
从冰点转移为启动的综合得分 = 0.3 × 0.2 = 0.06
→ 最终判定今天依然是冰点
```

元宝的原话（第 3 页）：

> 「**可转移状态」是规则约束，「先验概率」是约束的力度**。两者合在一起，保证了状态机不会跳到不允许的状态，同时又能平滑地完成周期循环。

**这是本文对你现有实现最重要的启发，理由如下：**

### 6.1 它解决的正是你已知的「状态机结构性缺陷」

技能里已记录三个未修缺陷，全部是**「硬规则无平滑」的直接后果**：

| 已知缺陷 | 现状 | 文档方案能否缓解 |
|---|---|---|
| **A/B 准入不对称**（B 分支完全不认高度） | 被踢出活跃态后最高板推 8 板也回不来 | ⚠️ 部分：B 分支的输出若也参与「先验 × 似然」，进入活跃态的概率会随当日指标升高而非被硬门槛挡住 |
| **「分歧→高潮」结构性堵死** | ⑥a 仅占分歧出口 6.4%（A2 硬要求最高板 ≥4，而分歧=龙头断板） | ✅ **能**：文档给分歧→高潮先验 **0.40**（最高！）→ 用先验给这条边「加分」，不会因为单一硬条件被卡死 |
| **`jun>0` 切碎高潮段** | 1.48→1.26 天、≥3 天段 33→2 | ✅ **能**：文档给高潮自环先验 0.20，不是 0；不需要靠 `jun>0` 硬条件来抑制高潮延续 |

### 6.2 但文档方案在你这里**有一个致命前提不成立**

文档第 4~5 页你自己就追问了：**「历史数据有情绪周期的标签吗？」**

元宝的回答（第 5 页）：

> 历史行情里**通常没有现成的「冰点/启动/发酵/高潮/分歧/退潮」标签**……情绪六状态是**你自己定义的主观框架**。
>
> 优点是可解释、完全贴合你定的拓扑；**缺点是打标规则本身带主观性，统计出的「先验」其实是对你规则的自我印证。**

**这句话是整篇文档里最诚实、也最重要的自我批评。** 落到你这里：

- 你的「先验矩阵」**不能**从历史统计学出来 —— 因为历史标签是**生产状态机（硬规则）自己打的**
- 用自己打的标签统出来的转移频率，**只能是自我印证**，不会比规则本身更准
- 唯一的出路是元宝第 6 页的 **HMM 无监督**路线（不依赖人工标签），但代价是：**解读性下降**（状态编号随机，得靠指标画像映射语义）

### 6.3 元宝给的「冷启动四步」我逐条评估

| 步骤 | 可行性 | 我的评估 |
|---|---|---|
| ① 先用规则软打标跑 3~5 年 | ⚠️ **只有 72 天** | 生产库只养了 72 个交易日（2026-06-09 ~ 09-17）。**这是最硬的约束** |
| ② 同时跑 HMM，对比规则标签吻合度（混淆矩阵 / 人工抽 100 天） | ✅ **可做** | 需要 `hmmlearn`；72 天对 6 状态 HMM 偏少（每状态仅 12 天均值），但可跑通 |
| ③ 吻合低的地方人工修订规则阈值 | ✅ **正是本轮在做的** | 与你「先定验收标准再动手」的习惯一致 |
| ④ 上线后在线更新 `a_ij^(t) = (1-β)·a_ij^(t-1) + β·1{i→j 今日发生}`，β 0.01~0.05 | ⚠️ | **β=0.01 时单日新样本权重 1%**，在 72 天数据量下要 250+ 天才收敛到有意义的更新。**当前不实用** |

### 6.4 ⭐ 元宝「概念板块口径四个坑」——这四条**条条命中你的项目**

| 元宝提的坑 | 你的实际状态 | 判定 |
|---|---|---|
| **成分股口径固定**：「用某日概念名单回测会引入未来函数，应按『股票进入概念的时间点』切」 | `board_member_cache` 存的是**当前时点**的成分名单，回补历史时**用的就是今天的名单** | ⛔ **你有这个未来函数**，且未被识别过 |
| **小概念样本薄**：建议「全市场短线情绪 + 本概念相对强度」双轨 | 你有 `_emo_z` 贝叶斯收缩（`K=20, p₀=1.6328%`）解决规模歧视，`qz` 用的是**当日在册板块分位** | ✅ **已解决**，且方案比元宝的更精细 |
| **启动/退潮作为一日状态**：统频时若连续两天极少就自环强制 0；若 HMM 自发学到自环，用 Viterbi 做「单日截断」 | **实测自环严格 0**，结构性成立 | ✅ **已达成** |
| **滚动而非全历史**：建议滚动 250~500 交易日重估转移矩阵，每月更新 | `zb_base` 用 20 日窗口；阶段判定本身是**逐日推进**（无全历史拟合） | ✅ **已达成** |

**⭐ 第一条「成分股未来函数」是本文最值得你警惕的新发现**，它不在我上一轮任何结论里。

**具体影响**：`rebuild_board_daily(members_map, klines, DATES)` 里的 `members_map` 是**单一快照**，对所有历史日期都用同一份名单。所以：
- 某只股票**9 月才被纳入**某概念 → 6 月的回算里**已经算进去了**
- 反向：**6 月被剔除**的股票，6 月的历史里**也不见了**
- 结果：历史阶段的成分股数（`count`）、涨停家数（`zt`）、扩散度（`up_r`）**都有偏差**
- **偏差方向不确定**（取决于概念是扩容还是缩容），所以不能简单说「历史偏乐观」或「偏悲观」

**这条要先验证再说**（不必现在改）——验证方法：查 `board_member_cache` 有没有 `updated_at` 类字段，看成分名单是否常年不变；再拿一个 6 月 vs 9 月成分股数变化大的板块看历史 `count` 是否被回溯污染。

---

## 7. 逐条启发汇总（按「该不该动」分三档）

### ⭐⭐⭐ 建议做

| # | 启发 | 依据 | 工作量 |
|---|---|---|---|
| **A** | **把「转移先验」引入 `_board_phase`** —— 用 `argmax_j [ P_prior(i→j) × L(X_t \| j) ]` 替代纯硬规则链 | 文档核心机制；直接缓解「分歧→高潮堵死」「jun>0 切碎高潮」两个已确认缺陷 | 中（需先定 `L(·)` 形式） |
| **B** | **补 `V_t`（量能比）** —— 你 6-24 写的「发酵 = 场外资金跟进」，`V_t` 是「场外资金」唯一的直接代理 | §4.2 已验算可行；生产缺此维度，导致「温和放大」完全无刻画 | 小（K 线 `bar[3]` 逐股累加，加进 `phase_dims`） |
| **C** | **把 `M_t<0` 从「冰点条件」改定为「退潮证据」** | §5.1 实测：退潮组 31.4% vs 其余 ≤17.5%；生产 A1b 已在用类似思路 | 小（文档口径纠偏，改报告文案） |
| **D** | **核验「成分股未来函数」** | 元宝四坑之一，我上轮完全没识别 | 小（只查不改） |

### ⚠️ 可做但要想清楚

| # | 启发 | 顾虑 |
|---|---|---|
| **E** | 补 `M_t`/`P_t` 的配套字段（`prev_limit_up` 全市场版），把非空率从 44.6% 提上去 | `prem` 缺失是**语义性**的（板块昨日无涨停就是没有），硬补需要跨板块借全市场口径 —— 会改变语义 |
| **F** | 用 HMM 无监督跑一遍，与生产标签做混淆矩阵 | 72 天 / 6 状态 = 每状态 12 天，**样本太薄**；结论外推风险高。**建议至少等库养到 250 天** |
| **G** | 补 `D_t`（板块跌停家数） | 文档只在改进项里提了一句，没给用法；且跌停股在概念板块内极稀少 |

### ⛔ 不建议做

| # | 启发 | 原因 |
|---|---|---|
| **H** | 照搬文档的严格 AND 打分条件 | §5.3：冰点全 4 条命中 0.1%、启动 4 条 0.7% —— **91%+ 板块日无法判定** |
| **I** | 把 `H_t∈{2,3}` 当启动条件 | §5.3-3：分歧组 lift 11.79×、退潮组 18.97×，**方向反** |
| **J** | 上线后 β 指数平滑在线更新先验 | 72 天数据量下 β=0.01 要 250+ 天才有意义 |
| **K** | 用「先有标签再统频」学先验 | 元宝自己承认「**是对你规则的自我印证**」，不会比规则更准 |

---

## 8. ⚠️ 需要你定（新增 1 件，累计 6 件）

| # | 事项 | 我的建议 |
|---|---|---|
| ① | 「延续分歧」是否采纳（采纳则 268 天改判，违规 16.4%→**3.0%**） | 建议采纳 —— 文档路径下自环先验 0.30、实测 21.1%，**双向支持** |
| ② | ④ 指标降级/换口径 | **建议先做**（已解剖，一条直线尺与你的有向图模型不匹配） |
| ③ | 文案 bug（A3「龙头首阴（最高板 1→0）」+ 兜底「无涨停联动」） | 零风险，随时可做 |
| ④ | B2 直判发酵的 699 天改判成「启动」还是「潜伏」 | 需你定 |
| ⑤ | 「今日启动」是否与阶段打通（28.76% 自相矛盾） | 需你定 |
| **⑥** | **合法边表以「6-24 原需求版（严格）」还是「09-18 裁定版（放宽）」为准？** | ⭐ **本次新增**。差异：严格版下高潮自环 41 条 + 分歧自环 175 条也算违规，违规率 16.4%→21.3% |

---

## 9. 一句话总结

> **这篇《情绪周期确认方法》的真正价值不是那七项指标（你系统里已落库 5/7），也不是那套严格打分条件（本系统数据上 91% 无法判定）——而是「综合得分 = 先验转移概率 × 当日似然」这个框架本身。它用「先验」给允许的边加分、用「似然」让当日数据说话，恰好能缓解你现在三个已确认的状态机结构性缺陷（分歧→高潮堵死、jun>0 切碎高潮、A/B 准入门槛不对称）。**
>
> **但元宝在文档里自己承认了最大的软肋：「打标规则带主观性，统计出的先验其实是对你规则的自我印证」——这意味着你不能靠历史统计来学先验，只能靠你手工给定（就像你 09-18 裁定的那份合法边表）。**
>
> **另外它甩出了一个我此前漏掉的真问题：你的 `board_member_cache` 是单一时点快照，回算历史时存在「成分股未来函数」。这条要单独验。**

---

**附：本次核验用到的脚本（均只读，未入库）**

| 脚本 | 作用 |
|---|---|
| `backend/_doc3_map.py` | 七项指标覆盖率 + `V_t` 可算性 + 先验矩阵对照 |
| `backend/_doc3_lik.py` | 似然打分条件命中率 + lift 判别力 |
| `backend/_doc3_probe.py` | `snapshot_json` 结构探测 |

**生产代码未改一行，数据库未写一个字节，版本号未动。**
