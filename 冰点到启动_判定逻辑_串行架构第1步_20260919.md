# 冰点 → 启动 判定逻辑（串行架构 · 第 1 步）

> 2026-09-19 · **只做「昨日 = 冰点」这一棵子树** · 生产代码未改一行、库未写一字节
> 全部数字由只读脚本 `backend/_doc6_ice2start.py` 本轮重跑自证（334 板块 × 72 个交易日）

---

## 0 一页速览

| 你要的答案 | 结论 |
|---|---|
| **串行架构对不对** | ✅ 对，而且**现状比你说的还糟一层** —— 分支 A 连 `prev_stage` 的值都不看，只用了一个布尔位 |
| **「冰点→启动」二选一能做到吗** | ✅ 能。**最小必要条件只有 2 条**：`涨停家数 ≥ 2` 且 `上涨占比 ≥ 0.50` |
| **元宝那套方案有效吗** | ⚠️ **骨架有效，6 项打分基本冗余**。且有三处必须修：`H≥2` 方向反、`V` 是标签污染、`H≥1`/`D≤1` 是冗余条件 |
| **那 607 条「冰点→发酵」怎么办** | ✅ 有答案了：元宝独立判据把其中 **90.9%（552 条）判为启动** ⇒ **归「启动」** |
| **⚠️ 最需要你接受的一条** | **「启动」目前不是买入信号** —— 3 日超额 +0.27% vs 基线 +0.07%，差异小于样本噪声；且样本只有 72 天、市况偏弱，**不足以证实也不足以证否** |
| **推翻的直觉** | 「板块沉睡很久后首次涨停」情绪扩散**最弱**（0.95），反而「刚断板一天就重新涨停」扩散最强（2.57） |

---

## 1 架构确认：你的主张 vs 现状的真实差距

### 1.1 你要的形态（串行）

```
昨日状态（已知，是今天的输入而不是并列变量）
      │
      ├─ 冰点 ──→ 用「冰点子树」判 ──→ 冰点 / 启动
      ├─ 启动 ──→ 用「启动子树」判 ──→ ……      ← 今日只做第一条
      ├─ 发酵 ──→ 用「发酵子树」判 ──→ ……
      ├─ 高潮 ──→ …
      ├─ 分歧 ──→ …
      └─ 退潮 ──→ …
```

数学写法：**今日状态 = `f_prev(今日指标)`**，其中下标 `prev` 表示「函数本身由昨日状态选定」。
这是**一阶马尔可夫链 + 状态相关的输出函数**，比「把 prev 当并列特征喂进一个函数」干净得多，因为：

- 每个子树只需要管**自己那一个状态的出路**，互斥性天然成立；
- 阈值的含义随昨日状态而变（同样 `涨停家数=2`，在「昨日冰点」下是**从无到有**，在「昨日高潮」下是**崩塌**）；
- 完备性靠子树内部的 `else` 兜底，不靠全局。

### 1.2 现状的真实形态（比你诊断的更糟）

现行源码 `daily_market.py` L1253 只有一行分叉：

```python
_EMO_ACTIVE = ("启动", "发酵", "高潮", "分歧")   # 冰点 / 退潮 / 潜伏 不在内
```

```python
if prev_stage in _EMO_ACTIVE:   # ← 分支 A
    ...
else:                           # ← 分支 B
    ...
```

**上一轮的探针 1 已经证明**：把 `prev_stage` 强行分别设为 启动 / 发酵 / 高潮 / 分歧 跑四遍，分支 A 的输出分布**逐位完全相同**（发酵 42.0% / 退潮 47.8% / 分歧 8.8% / 高潮 1.4%）。

⇒ **分支 A 根本没读 `prev_stage` 的值**，它只读了一个比特：`prev 在不在那 4 个态里`。

所以准确的说法是：

| 你的诊断 | 实际 |
|---|---|
| 「昨日状态和当日指标是并行变量」 | 分支 B 是（`active_recent` 也算一种粗糙的昨日信息），**分支 A 完全丢掉了昨日状态** |
| 后果 | 一半的样本里，「昨日是谁」这个信息**一个字都没用上**；另一半只用了 1 bit |

这正是「昨日高潮」和「昨日分歧」会走到同一条路上的原因 —— 也是你说「不得已要推倒重来」的合理来源。

### 1.3 串行架构的落地形态

保留 A/B 的做法是「**先问在不在周期内，再问怎么演化**」；你的做法是「**先问昨日是谁，再问今天怎么变**」。后者才是一棵**判定子树森林**：

| 子树 | 输入 | 出路（按你的裁定） |
|---|---|---|
| `T_冰点` | `prev=冰点` | 冰点（可自环） / 启动 |
| `T_启动` | `prev=启动` | 发酵 / 分歧 / 退潮（**启动是一日态，不能自环**） |
| `T_发酵` | `prev=发酵` | 发酵（可自环） / 高潮 / 分歧 / 退潮 |
| `T_高潮` | `prev=高潮` | 高潮（可自环） / 分歧 / 退潮 |
| `T_分歧` | `prev=分歧` | 分歧（可自环） / 高潮 / 退潮 |
| `T_退潮` | `prev=退潮` | 冰点 / 启动 / 发酵（**退潮是一日态，不能自环**） |

**本轮只做 `T_冰点`。** 剩下 5 棵下一次逐个过。

---

## 2 样本与口径

### 2.1 六态口径（沿用你 09-18 的裁定①：潜伏并入冰点）

```
MAP6 = {"潜伏": "冰点"}
六态 = 冰点 / 启动 / 发酵 / 高潮 / 分歧 / 退潮
```

### 2.2 样本构成

| 项 | 值 |
|---|---|
| 参与判定的板块 | 334 个（有效成分股 ≥ 20 且非伪板块） |
| 交易日 | 72 个（2026-06-09 ~ 2026-09-17） |
| 「昨日 = 冰点」样本 | **18,846 条** → 限定相邻交易日后 **18,842 条** |
| 其中「昨日涨停 = 0」（严格冰点） | **8,774 条（46.6%）** |

**今日实际落点（现行 A0 判定）**：

| 落点 | 条数 | 占比 |
|---|---:|---:|
| 冰点 | 17,660 | **93.73%** |
| 发酵 | 607 | **3.22%** |
| 启动 | 575 | 3.05% |

### 2.3 ⭐ 那 607 条「冰点 → 发酵」在新架构下的归属

**按你的架构，昨日冰点今日只能落「冰点」或「启动」—— 这 607 条必须有人接收。**

我拿元宝那套规则（**独立判据，不依赖现行 stage**）做了交叉表：

| A0 落点 | 元宝判「启动」 | 元宝判「非启动」 | 合计 |
|---|---:|---:|---:|
| 冰点 | 1,590 | 16,070 | 17,660 |
| **启动** | **485（84.3%）** | 90 | 575 |
| **发酵** | **552（90.9%）** | 55 | 607 |

⇒ **结论：那 607 条里 90.9% 应该归「启动」。** 这个判断不依赖现行实现（元宝规则是另一套独立逻辑），可信度较高。

但要注意：元宝规则同时把 A0 判「冰点」的 1,590 条也判成了启动 —— **规则整体会「松」出 2,142 条新启动**。这是好事还是坏事，取决于这 1,590 条是不是真启动（见第 4、5 章）。

---

## 3 把元宝方案落到本系统字段上

### 3.1 七个量的字段映射（含 3 个坑）

元宝方案用了 7 个量。落到本系统：

| 元宝 | 含义 | 本系统字段 | 状态 |
|---|---|---|---|
| `N` | 板块自然涨停家数 | `row["zt"]` | ✅ 直接可用 |
| `R` | 板块指数涨跌幅 | `row["pct"]`（等权平均，对齐东财） | ✅ |
| `D` | 板块跌停家数 | — | ⛔ **未落库**，本轮用 `ctx["lim"]==-1` 现算 |
| `B` | 炸板率 | `row["zb_r"]`（无涨停时元宝规定 =100%） | ✅（注意 `None` 与 `0` 的差别） |
| `V` | 板块成交额增幅 | — | ⛔ **`row["amt"]` 不是板块成交额**，本轮用 K 线 `bar[3]` 逐成分股累加 |
| `H` | 最高连板高度 | `row["lbc_max"]` | ✅ |
| `Y` | 昨板溢价 | `row["prem"]` | ⚠️ 冰点前一日常无涨停 → **大量缺失** |
| （补充） | 红盘率 | `row["up_r"]` | ✅ 元宝只当辅助，实测它很强 |

**三个必须记的坑**：

1. **`row["amt"]` ≠ 板块成交额**。它是「当日**涨停股**成交额合计」（源码里 `amt` 只在 `_lim == 1` 分支累加）。用它当 `V` 会得到与涨停数共线的假信号。**真算必须走 K 线 `bar[3]` 逐成分股累加**（本轮已实现，34,652 条板块·日）。
2. **`D`（跌停家数）根本没落库**。要上生产必须新增字段，且要走 `_emo_dims` 的**唯一透传点**（否则会重演 V1.009.8 `jun_pct`/`ow_n` 那种「库里查不到」的静默丢字段）。
3. **`Y` 在冰点场景下天然残缺**：启动组 575 条里只有 **303 条（52.7%）** 有 `prem`；冰点组 17,660 条里只有 9,322 条（52.8%）。元宝自己也意识到了（「若昨日无涨停股，Y 不参与评分」），但**扣掉一半样本**的代价没有明说。

### 3.2 逐档判别力实测

**基线**：全体样本里「次日 = 启动」的比例 = **3.052%**（这就是「无判别力」的基准）

| 指标 | 档位 | n | 启动率 | **lift** |
|---|---|---:|---:|---:|
| **N 涨停家数** | ≤1 | 12,687 | **0.000%** | 0.00× |
| | =2 | 2,173 | 7.823% | 2.56× |
| | ≥3 | 3,982 | 10.171% | 3.33× |
| **R 板块涨幅** | <0.5% | 11,250 | 0.053% | 0.02× |
| | [0.5%, 2%) | 4,992 | 4.287% | 1.40× |
| | ≥2% | 2,619 | 13.593% | **4.45×** |
| **D 跌停家数** | =0 | 14,171 | 3.606% | 1.18× |
| | =1 | 2,516 | 2.027% | 0.66× |
| | ≥2 | 2,155 | 0.603% | 0.20× |
| **B 炸板率** | ≤25% | 6,863 | 6.950% | 2.28× |
| | (25%, 50%] | 3,365 | 3.982% | 1.30× |
| | >50% | 10,490 | 0.133% | 0.04× |
| **V 成交额增幅** | <10% | 13,879 | 1.808% | 0.59× |
| | [10%, 30%) | 2,952 | 4.573% | 1.50× |
| | ≥30% | 850 | **15.882%** | **5.20×** |
| **H 最高连板** | =0 | 8,286 | **0.000%** | 0.00× |
| | =1 | 7,184 | **5.721%** | **1.87×** |
| | ≥2 | 3,372 | 4.864% | 1.59× ⚠️ |
| **up_r 上涨占比** | <0.5 | 10,098 | **0.000%** | 0.00× |
| | [0.5, 0.7) | 3,605 | 3.440% | 1.13× |
| | ≥0.7 | 5,203 | 8.764% | 2.87× |

**三条零通过率**（12,687 + 8,286 + 10,098 条里，启动数各为 **0**）：`N≤1`、`H=0`、`up_r<0.5`。

### 3.3 元宝方案的整体成绩

**必要条件**（`N≥2` 且 `R≥0.5%` 且 `H≥1` 且 `D≤1`）：

| | 值 |
|---|---|
| 通过 | 3,392 / 18,842 = **18.00%** |
| 其中判启动 | 556 |
| 精确率 | **16.39%**（基线 3.05% → lift 5.4×） |
| 召回（占全体启动） | **96.7%** |

**评分阈值扫描**：

| 阈值 T | 判启动 n | 真启动 n | 精确率 | 召回 |
|---:|---:|---:|---:|---:|
| 4（=只要必要条件） | 3,392 | 556 | 16.39% | 96.7% |
| 5 | 3,390 | 556 | 16.40% | 96.7% |
| 6 | 3,363 | 555 | 16.50% | 96.5% |
| 7 | 3,156 | 547 | 17.33% | 95.1% |
| **8（元宝原版）** | **2,627** | **485** | **18.46%** | **84.3%** |
| 9 | 1,708 | 348 | 20.37% | 60.5% |
| 10 | 756 | 209 | 27.65% | 36.3% |
| 11 | 238 | 96 | 40.34% | 16.7% |
| 12 | 43 | 17 | 39.53% | 3.0% |

### ⭐ 3.4 结论一：6 项打分基本冗余

**T 从 4 提到 8，精确率只从 16.39% → 18.46%（+2.07 个点），召回却从 96.7% → 84.3%（掉 12.4 个点）。**

⇒ 信息几乎全在**必要条件**里；那 12 分的打分表，**边际贡献极小**。

再用**删除法**验证（去掉某一项、其余仍要 ≥6 分，看外生指标「未来 3 日最大涨停」掉多少）：

| 去掉哪一项 | 判启动 n | 3 日最大涨停 |
|---|---:|---:|
| （都不去） | 3,363 | 4.34 |
| 去掉 N 涨停家数 | 3,002 | 4.27 |
| 去掉 R 板块涨幅 | 3,065 | 4.40 |
| **去掉 D 跌停家数** | 2,747 | **4.58（反而更好）** |
| **去掉 B 炸板率** | 2,912 | **4.53（反而更好）** |
| 去掉 V 成交额 | 3,337 | 4.35 |
| 去掉 H 最高连板 | 3,075 | 4.30 |

⇒ **`D` 和 `B` 是纯噪声甚至有害**；`N` 是最不可替代的。

### 3.5 ⛔ 三处必须修的地方

#### (a) `H≥1` 是冗余条件（被 `N≥2` 蕴含）

实测：`N≥2` 的 6,155 条样本里，`H=0` 的有 **0 条**。
⇒ 只要有 ≥2 家涨停，最高板必然 ≥1。**`H≥1` 写进必要条件等于没写**（逐条边际过滤时「+H≥1」和「+D≤1」的通过数完全一样：3,392 → 3,392）。

#### (b) `D≤1` 是冗余条件，且方向可疑

实测：`D≥2` 时仍有 13 条启动（0.603%）—— **不是零通过，不构成必要条件**。
且在「`N≥2` 且 `R≥0.5%` 且 `H≥1`」的子集里，`D` 恒 ≤1 ⇒ **加它不改变任何判定**。

更麻烦的是：`D` 与「未来情绪扩散」**正相关而非负相关**（见 4.2）：

| `D` 档 | n | 未来 3 日最大涨停 | 次日涨停均 | A0 启动率 |
|---|---:|---:|---:|---:|
| 0 | 14,171 | 2.32 | 1.33 | 3.606% |
| 1 | 2,516 | 3.81 | 2.33 | 2.027% |
| 2 | 876 | 5.15 | 3.13 | 1.027% |
| **≥3** | 1,279 | **8.35** | **4.83** | **0.313%** |

⇒ **跌停越多，后续涨停越猛（3.6×），但现行规则越不愿意判它启动。** 元宝给 `D=0` 打满分（2 分）**方向是反的**。

#### (c) ⛔ `H≥2` 打 2 分 —— 方向反了

`H=1` 的启动率 **5.721%** > `H≥2` 的 **4.864%**。

**为什么**：你的定义说得清楚 ——「启动的本质是**关注度从 0 到 1**，先知资金开始试错」。
- `H=1`（只有首板）＝ **正在试错**，是启动；
- `H≥2`（已出现连板龙头）＝ **梯队已经成形**，那是**发酵**的特征。

元宝把「H≥2」打满分，等于用**发酵的特征**去卡**启动**。这个错误与现行实现里「用中军 `jun>0` 卡启动」是同一类（上一轮已实测：中军在启动组中位 +2.77，在发酵组 +2.05，在高潮组仅 +1.25 —— 用高潮特征卡启动，方向搞反）。

#### (d) ⛔⛔ `V`（成交额增幅）是标签污染，不是预测力

| | 对 A0「启动」标签 | 对**外生**指标 |
|---|---|---|
| `V ≥ 30%` vs `< 10%` | lift **5.20×**（最强指标） | 3 日最大涨停 **2.17 vs 2.48**（**lift 0.90×，比不筛还差**） |

**为什么**：现行启动判定的核心条件是 `qz ≥ 0.85`（涨停强度分位），而 `qz` 与板块成交额**高度共线**。所以 `V` 只是在「拟合现行标签」，不是「预测真实市场」。

⇒ **凡是与「涨幅 / 扩散度 / 强度」共线的指标，用 A0 标签做验证都会系统性虚高。这是本次最大的方法论陷阱（见第 4 章）。**

---

## 4 ⚠️ 方法论自查：外生标签本身也不干净

我原来打算用一个「外生标准」来验证启动判据 —— 看被判启动的样本**之后是否真的更强**。为此我做了两版，**结果两版结论完全相反**，必须把这件事讲清楚。

### 4.1 第一版：绝对量（未来 3 日最大涨停）

| `N` 档 | n | 未来 3 日最大涨停 | 次日涨停均 |
|---|---:|---:|---:|
| 0 | 8,286 | 1.55 | 0.73 |
| 1 | 4,401 | 2.35 | 1.30 |
| 2 | 2,173 | 3.36 | 1.85 |
| 3 | 1,283 | 4.42 | 2.62 |
| 4 | 791 | 5.47 | 3.25 |
| 5-9 | 1,336 | 7.34 | 4.91 |
| **≥10** | 572 | **13.41** | **9.65** |

看起来漂亮得不得了（≥10 vs 0 = **8.6×**）。**但这是假的。**

**为什么假**：涨停家数有强自相关。今天涨停 10 家的板块，未来 3 天大概率还是涨停 5-10 家。这个「预测力」纯属大基数效应 —— **它只说明「涨停多的板块下次涨停也多」，没有信息量。**

### 4.2 第二版：增量（未来 3 日最大涨停 − 今日涨停，`dN3`）

**基线 `dN3` = 1.33**（数学上必然 ≥0，因为取的是 max）

| `N` 档 | `dN3` | | `D` 档 | `dN3` |
|---|---:|---|---|---:|
| 0 | +1.55 | | 0 | +0.85 |
| 1 | +1.35 | | 1 | +1.67 |
| 2 | +1.36 | | 2 | **+2.55** |
| 3 | +1.42 | | **≥3** | **+5.05** |
| 4 | +1.47 | | | |
| 5-9 | +0.95 | | | |
| **≥10** | **−1.76** | | | |

⇒ **`N≥10` 从「最强」变成「最弱」**，`D≥3` 变成最强。

**但这也不干净**：`dN3 = max(未来 N) − 今日 N`，今日 `N` 越高，`dN3` 越容易为负 —— 这是**均值回归偏差**（regression to the mean），同样是统计假象。

### 4.3 两版对照：只有「两版一致」的才可信

| 指标档 | 绝对量版 | 增量版 | 判定 |
|---|---|---|---|
| `N=0` | 1.55 ⛔ | +1.55（≈基线） | **无结论** |
| `N≥10` | 13.41 ⭐ | −1.76 ⛔ | ⛔ **两版相反 ⇒ N 无预测力** |
| `U<0.40` | 3.21 | +2.18 ⭐ | 无稳定结论 |
| `U≥0.80` | 2.76 | −0.21 ⛔ | 无稳定结论 |
| `R<-2` | 3.68 | +2.95 ⭐ | 无稳定结论 |
| `R≥2` | 3.20 | −0.71 ⛔ | 无稳定结论 |
| **`V≥30`** | **2.17 ⛔** | **−0.21 ⛔** | ✅ **两版一致：放量后走弱** |
| **`D≥3`** | **8.35 ⭐** | **+5.05 ⭐** | ✅ **两版一致：跌停潮后情绪扩散强** |
| `B(50%,75%]` | 5.95 ⭐ | +3.66 ⭐ | ✅ 一致（但与 A0 标签反向） |

**可信的只有两条**：
1. **`V≥30`（成交额暴增）之后，情绪与价格双双走弱** —— 「放量不涨」。
2. **`D≥3`（跌停潮）之后，情绪扩散显著增强（+5.05）**，但**超额收益为负（−0.40）** ⇒ **典型「恐慌后的反弹陷阱」**。

**其余指标（N / U / R）的「判别力」两版都站不住。**

---

## 5 最干净的一版：严格冰点（昨日涨停 = 0）

要同时切断**自相关**与**均值回归**，只有一个办法：**把起点统一为 0**。
于是限定「昨日涨停 = 0」的严格冰点子集（8,774 条，占「昨日冰点」的 46.6%），按今日涨停分档。

**这一版最干净的理由**：
- 大基数自相关被切断 —— 今天是从 0 生出来的；
- 均值回归偏差被削弱 —— 起点统一为 0；
- **正好对应你的定义：「启动 = 关注度从 0 到 1」。**

### 5.1 今日涨停家数 → 决定「情绪扩散」，不决定「价格」

| 今日涨停 | n | 占比 | 3 日涨幅 | **超额（减大盘）** | 3 日最大涨停 | 次日涨停 | A0 启动率 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 0 | 5,363 | 61.1% | +0.13% | **+0.20%** | 1.24 | 0.55 | 0.000% |
| 1 | 2,042 | 23.3% | −0.06% | +0.12% | 1.74 | 0.92 | 0.000% |
| **2** | 717 | 8.2% | −0.56% | −0.09% | 2.18 | 1.17 | **15.621%** |
| **3** | 294 | 3.4% | −0.50% | +0.01% | 2.67 | 1.57 | **22.449%** |
| **4** | 136 | 1.6% | −0.12% | **+0.35%** | 3.85 | 2.36 | **19.853%** |
| 5-9 | 194 | 2.2% | −0.51% | +0.12% | 3.72 | 2.34 | **32.474%** |
| ≥10 | 28 | 0.3% | **−1.67%** | **−1.08%** | 4.41 | 3.44 | 14.286% |

**⭐ 这一列「3 日最大涨停」从 1.24 单调升到 4.41，是本次唯一稳的规律。**

⇒ **「启动」判据预测的是「情绪会不会扩散」，不是「板块会不会涨」。**

价格那一列（超额）完全无规律：`N=0` 的 +0.20 反而是最好的，`N≥10` 的 −1.08 最差。

### 5.2 ⚠️ 冰点深度：越深，扩散越弱（推翻直觉）

限定今日涨停 ≥1，再看「今日之前连续几天 0 涨停」（`n0run`）：

| `n0run` | n | 3 日涨幅 | 超额 | 3 日最大涨停 | 次日涨停 |
|---|---:|---:|---:|---:|---:|
| 1 天 | 1,563 | −0.41% | −0.11% | **2.57** | **1.44** |
| 2 天 | 736 | −0.10% | +0.25% | 2.21 | 1.34 |
| 3-5 天 | 792 | −0.12% | +0.13% | 1.62 | 0.82 |
| 6-10 天 | 253 | −0.28% | +0.16% | 1.09 | 0.53 |
| ≥11 天 | 67 | **+0.74%** | **+1.25%** | 0.95 | 0.46 |

**我原本以为「沉睡越久、首次涨停」是最强的启动信号 —— 数据说：不是。**

- **情绪扩散**：`n0run=1`（刚断板一天就重新涨停）最强（2.57），`n0run≥11` 最弱（0.95）⇒ **单调递减**。
  解释：`n0run=1` 意味着该板块**刚刚还在活跃**（前天有涨停），这是**持续性**而不是**启动**。
- **收益**：`n0run≥11` 那档反而最好（+0.74% / 超额 +1.25%）—— 但 **n=67，样本太小，不能外推**。

⚠️ **所以「从 0 到 1」的语义与「情绪会扩散」的统计事实之间，存在张力**：真正从零开始的启动，后续扩散反而弱。这一点需要你判断（见第 8 章）。

---

## 6 建议的判定式

### 6.1 必要条件（最小集，实测「违反时启动数 = 0」）

| 条件 | 违反 n | 违反时启动数 | 是否真必要 |
|---|---:|---:|:--:|
| `N ≥ 2`（涨停家数） | 12,687 | **0** | ✅ |
| `up_r ≥ 0.50`（上涨占比） | 10,098 | **0** | ✅ |
| `H ≥ 1`（最高连板） | 8,286 | 0 | ⚠️ 但被 `N≥2` 蕴含 → **冗余** |
| `R ≥ 0.5%`（板块涨幅） | 11,250 | 6 | ❌ 近乎必要 |
| `B ≤ 50%`（炸板率） | 9,070 | 7 | ❌ |
| `D ≤ 1`（跌停家数） | 2,155 | 13 | ❌ **最不必要** |

**⇒ 最小必要条件集只有两条：`N ≥ 2` 且 `up_r ≥ 0.50`。**

### 6.2 我的建议版（待你拍板）

```
【T_冰点 子树】

输入：今日指标 N(涨停家数) / up_r(上涨占比) / R(板块涨幅) / B(炸板率) / H(最高连板) / V(成交额增幅)

① 硬门槛（任一不满足 → 今日 = 冰点）
       N      ≥ 2        涨停家数至少 2 家
       up_r   ≥ 0.50     上涨家数占比至少一半
       R      ≥ 0.5%     板块翻红且不是勉强翻红

② 打分（去掉 D 与 V，H 的权重反转）
       +2  N ≥ 3         （N=2 记 +1）
       +2  R ≥ 2%
       +1  up_r ≥ 0.70
       +1  B ≤ 25%
       +1  H = 1         （注意：H≥2 不加分，甚至可减分）
       +1  超额条件：V ≥ 10% 且板块涨幅 ≥ 1%（同时放量且真涨，才说明是资金进场）

③ 判定
       总分 ≥ 3  →  启动
       否则      →  冰点
```

**与元宝版的差异（逐条理由）**：

| 项 | 元宝 | 我的建议 | 理由 |
|---|---|---|---|
| `N≥2` | 必要 | 必要 | ✅ 保留（12,687 条零通过） |
| `up_r` | 只当辅助 0-2 分 | **升为硬门槛** | 实测 10,098 条零通过，是最强的两个门槛之一 |
| `R≥0.5%` | 必要 | 必要 | ✅ 保留（11,250 条里只有 6 条启动） |
| `H≥1` | 必要 | **删** | 被 `N≥2` 蕴含，写了等于没写 |
| `D≤1` | 必要 | **删** | 加它不改变任何判定；且方向可疑 |
| `H≥2` 得 2 分 | +2 | **0 分（H=1 才得分）** | H=1 启动率 5.72% > H≥2 的 4.86%，元宝方向反 |
| `V≥30%` 得 2 分 | +2 | **删掉单列** | 对 A0 标签 lift 5.20×，对外生 lift 0.90× ⇒ 标签污染 |
| `D` 得 0-2 分 | 满分 2 | **删** | 外生验证里去掉它反而更好 |
| `B` 打分 | 满分 2 | 降为 1 | 外生验证里去掉它也更好 |
| `Y` 昨板溢价 | 参与打分 | **不进判定** | 冰点场景下 47% 缺失，且缺失样本有系统性偏差 |

### 6.3 阈值敏感性（用外生标准看，不用循环标签）

| 规则 | 判启动 n | 3 日最大涨停 | 次日涨停均 | lift |
|---|---:|---:|---:|---:|
| 全体基线 | 18,842 | 3.07 | 1.79 | 1.00× |
| 只要必要条件 | 3,392 | 4.35 | 2.84 | 1.42× |
| 元宝 T=8 | 2,627 | 4.51 | 2.98 | 1.47× |
| 元宝 T=9 | 1,708 | 4.57 | 3.18 | 1.49× |
| **`N≥3` 且 `up_r≥0.7` 且 `R≥2%`** | **1,166** | **5.12** | **3.57** | **1.67×** |
| ⛔ `N≥2` 且 `up_r≥0.7` 且 `V≥30%` | 283 | 2.91 | 1.80 | **0.95×** |
| ⛔ **现行 B1** | 1,145 | **2.98** | 2.01 | **0.97×** |

**这张表是本轮最有价值的一页**：

- **现行 B1 规则对外生指标 lift 0.97× —— 等于没有预测力**（甚至比全体基线还差）；
- 一个只用 3 个条件的朴素规则（`N≥3` 且 `up_r≥0.7` 且 `R≥2%`）做到 **1.67×**；
- 而元宝那条 `V≥30%` 一旦加入，lift 立刻掉到 **0.95×** ⇒ 再次印证 `V` 是污染项。

---

## 7 必须诚实说的一条：「启动」目前不是买入信号

我做了「判启动之后真的更强吗」的外生检验（**用元宝规则，因为它不依赖现行标签**）：

| 分组 | n | 3 日涨幅均值 | 涨幅中位 | **超额（减大盘）** | 3 日最大涨停 | 次日涨停均 |
|---|---:|---:|---:|---:|---:|---:|
| 全体样本 | 18,842 | −0.05% | +0.09% | **+0.07%** | 3.07 | 1.79 |
| 现行判定 = 启动 | 575 | −0.24% | −0.16% | **+0.27%** | 2.74 | 1.80 |
| 现行判定 = 冰点 | 17,660 | −0.03% | +0.12% | +0.06% | 3.05 | 1.76 |
| 元宝 T=8 = 启动 | 2,627 | −0.24% | −0.19% | **+0.16%** | **4.51** | **2.98** |
| 元宝 T=8 = 非启动 | 16,215 | −0.02% | +0.14% | +0.05% | 2.84 | 1.60 |

**三条结论**：

1. **价格上**：启动组超额 **+0.27%**（现行）/ **+0.16%**（元宝），对照组 **+0.06%**。
   ⇒ 有**微弱正超额**，但**量级小于噪声**（整个样本期的超额均值只有 +0.07%，说明这是个几乎没有趋势的区间）。
2. **情绪上**：启动组的**绝对**涨停数明显更高（4.51 vs 2.84），但**增量是负的**（`dN3` = −1.00 ~ −2.18，基线 +1.33）⇒ **启动日之后情绪会回落**（均值回归）。
3. **绝对涨幅是负的**（−0.24%），比冰点组（−0.03%）还差 ⇒ **「启动日追进」在这段样本里是亏的**。

### ⚠️ 但这一段有硬约束，不能过度解读

- 样本只有 **72 个交易日**（2026-06-09 ~ 09-17），且**区间整体偏弱**（全体 3 日涨幅均值 **−0.05%**）；
- 在弱势市况中，「启动」这类强势信号本来就容易失败 —— **样本期不足以证否，也不足以证实**；
- 「启动」的**正确定位**很可能不是买入信号，而是**关注度提醒**：它告诉你「这个板块的资金开始动了」，但**从资金动到价格动，中间还隔着发酵与高潮两级**，而这两级的判据我们还没做。

⇒ **建议：先把 6 棵子树全部做完，再用同一套外生标准统一验收，最后才谈它的交易含义。**

---

## 8 待你拍板

| # | 事项 | 选项 | 我的建议 |
|---|---|---|---|
| ① | **`N`（涨停家数）下限** | 2 / 3 | **2**（对应「从 0 到 1」的语义；N≥3 更强但样本少一半） |
| ② | **`N` 上限**（超过多少就不算启动） | 不限 / ≤4 / ≤5 | **≤ 4**。理由：`N≥5` 已经是梯队成形，语义上是发酵；且元宝的 `H≥2` 错误同源 |
| ③ | **`up_r` 是否升为硬门槛** | 是 / 否 | **是**（10,098 条零通过，比 `R` 更强的门槛） |
| ④ | **`V`（成交额）是否进判定** | 进 / 不进 | **不进**。对标签 lift 5.20× 但对外生 0.90× ⇒ 会污染整个体系 |
| ⑤ | **`D`（跌停家数）是否进判定** | 进 / 不进 | **不进**。且它需要新增字段才能上生产（成本 vs 收益不划算） |
| ⑥ | **`H=1` 是否加分** | 加分 / 不加 | **加**（H=1 比 H≥2 更像启动） |
| ⑦ | **「冰点深度」是否进判据** | 进 / 不进 | **不进，但需要你回答一个语义问题**：`n0run` 越长（沉睡越久），后续扩散越弱 —— 这与「从 0 到 1 最强」的直觉冲突。**你要的「启动」，是「从 0 到 1」，还是「中断后的重燃」？** 后者才是数据上更强的那个。 |
| ⑧ | **判定式形态** | 硬门槛+打分 / 纯硬门槛 | **纯硬门槛更可能**。因为实测显示打分几乎不增加判别力（T=4→8 精确率只 +2 个点），而硬门槛可解释、可复现、无权重争议 |
| ⑨ | **是否先扩样本再定稿** | 现在定 / 扩到 1 年以上 | **建议扩样本**。72 天不足以外推；扩样只需重建历史（口径已定，零代码风险） |

---

## 9 下一步：剩下 5 棵子树

按你的「一个一个过」的要求，后面的顺序我建议：

| 顺序 | 子树 | 为什么这个顺序 |
|---|---|---|
| 2 | `T_启动`（启动 → ？） | 启动是一日态，出路必须定义，否则「启动→发酵」的链条断在这里 |
| 3 | `T_发酵`（发酵 → ？） | 承接启动，是主升段 |
| 4 | `T_分歧` 与 `T_高潮` | 这两个是「高位态」，判据互相纠缠（撕裂 vs 见顶），必须一起过 |
| 5 | `T_退潮`（退潮 → ？） | 退潮是一日态，且「退潮→启动」这条边你已经裁定为合法 |

**每棵子树我都按同样的流程交付**：
1. 先明确「进入条件」（上一态过来的样本）
2. 再用本轮这套外生标准反搜指标与阈值
3. 标注哪些指标是「标签污染」（共线）、哪些有真实预测力
4. 给出判定式 + 阈值敏感性 + 待拍板项

---

## 附录 A 本轮全部实测数字（可复跑）

**样本**：334 板块 × 72 交易日（2026-06-09 ~ 09-17）；「昨日 = 冰点」18,846 条 → 相邻 18,842 条
**落点**：冰点 17,660（93.73%）/ 发酵 607（3.22%）/ 启动 575（3.05%）
**严格冰点（昨日涨停 = 0）**：8,774 条（46.6%）

**关键指标分布（启动组 vs 冰点组，P25/P50/P75）**：

| 指标 | 启动组（n=575） | 冰点组（n=17,660） |
|---|---|---|
| `N` 涨停家数 | 2 / 3 / 6 | 0 / 1 / 2 |
| `R` 板块涨幅% | 1.60 / 2.40 / 3.52 | −1.35 / −0.11 / 1.03 |
| `D` 跌停家数 | 0 / 0 / 0 | 0 / 0 / 1 |
| `B` 炸板率% | 0 / 0 / 23.08 | 0 / 66.67 / 100 |
| `H` 最高连板 | 1 / 1 / 2 | 0 / 1 / 1 |
| `V` 成交额增幅% | −4.80 / 11.67 / 30.48 | −17.61 / −5.90 / 6.56 |
| `up_r` 上涨占比 | 0.71 / 0.81 / 0.90 | 0.22 / 0.43 / 0.69 |
| `Y` 昨板溢价% | 1.41 / 4.67 / 7.69 | −2.44 / 0.65 / 3.79 |
| `Y` 可用样本数 | **303 / 575（52.7%）** | 9,322 / 17,660（52.8%） |

**必要条件逐条过滤**：

| 步骤 | 剩余 n | 其中启动 | 启动率 | 召回 |
|---|---:|---:|---:|---:|
| `N≥2` | 6,155 | 575 | 9.342% | 100.0% |
| `+R≥0.5%` | 3,583 | 569 | 15.881% | 99.0% |
| `+H≥1` | 3,392 | 556 | 16.392% | 96.7% |
| `+D≤1` | **3,392** | **556** | **16.392%** | **96.7%** ← 与上一步完全相同（D 冗余） |

**冻结候选规则的外生表现**：

| 规则 | n | 3 日最大涨停 | 次日涨停均 | lift |
|---|---:|---:|---:|---:|
| 基线 | 18,842 | 3.07 | 1.79 | 1.00× |
| `N≥5` 且 `up_r≥0.4` | 1,420 | 7.70 | 5.44 | **2.51×** |
| `N≥3` 且 `up_r≥0.7` 且 `R≥2%` | 1,166 | 5.12 | 3.57 | 1.67× |
| 元宝 T=8 | 2,627 | 4.51 | 2.98 | 1.47× |
| 元宝 T=9 | 1,708 | 4.57 | 3.18 | 1.49× |
| `N≥2` 且 `up_r≥0.7` 且 `B≤25%` | 1,730 | 4.10 | 2.79 | 1.33× |
| `N≥2` 且 `up_r≥0.7` 且 `V≥30%` | 283 | 2.91 | 1.80 | **0.95×** ⛔ |
| 现行 B1 | 1,145 | 2.98 | 2.01 | **0.97×** ⛔ |

**现行 B1 在本样本上的表现**（含循环性说明）：
- 命中 1,145 条，其中次日 = 启动 575，精确率 50.22%
- ⚠️ **B1 自己就是产出 A0「启动」标签的规则**，所以精确率天然偏高（100% 召回是必然）
- 被 B1 拦下的 881 条里：**570 条是被 10 日窗 `active_recent` 拦的**（`lbc_max≤2` 与 `zt≥2` 都满足）

## 附录 B 取证脚本

`backend/_doc6_ice2start.py`（只读，`venv/Scripts/python.exe _doc6_ice2start.py`，约 12 秒）

输出 15 节：
| 节 | 内容 |
|---|---|
| 1 | 样本概览 + 落点分布 |
| 2 | 启动组 vs 冰点组 9 项指标 P25/P50/P75 |
| 3 | 每项指标分档的启动率与 lift |
| 4 | 元宝规则照搬 + 必要条件逐条边际过滤 + 阈值扫描 |
| 5 | 外生检验（含超额、中位数） |
| 6 | 交叉表（A0 落点 × 元宝判定） |
| 7 | 外生标准反搜（10 个候选规则） |
| 8 | 逐项打分删除法 |
| 9 | 现行 B1 对照 |
| 10 | 必要条件最小化（含蕴含关系检验） |
| 11 | 网格搜索（N×U×R，以 lift 为目标） |
| 12 | 单指标分档 × 外生（绝对量版） |
| 13 | ⚠️ 增量版修正（排除自相关） |
| 14 | 各指标分档 × 增量 |
| 15 | 严格冰点版（最干净）+ 冰点深度 |

**⚠️ 重要提醒**：脚本里用到的「未来 3 日」数据**只用于标定阈值，不进入判定**。
生产判定式里不得出现任何未来信息。

---

*本报告只做逻辑梳理，未改动 `backend/app/services/daily_market.py` 一行、未写入数据库、未变动版本号。*
