# AIReviewSystem V1.009.9 发布说明

**发布日期**：2026-09-17
**版本类型**：数据层改造 + 判据修正
**上一版本**：V1.009.7
**说明**：V1.009.9 = V1.009.7 全部内容 + **V1.009.8（数据层：中军 / 一字板 / 全链路复权口径审计）** + **V1.009.9（判据层：分歧的「扩散未走弱」护栏）**

> ⚠️ **V1.009.8 未单独发布，已并入本版** —— 两条改动同源（都指向板块情绪周期的判据精度），一起发能避免用户装两次、重建两次。

> ⚠️ **本版必须重做「历史数据重建」**：个股 K 线由 7 元组扩到 **11 元组**（新增前复权开/低、不复权开/低），旧库里的 7 元组会被补 `None` —— **不报错**，但「一字板」在历史日会**恒为 0**（静默降级）。情绪周期阶段也是重建时算好写进快照的。升级后请执行一次「历史数据重建」（70 个交易日，约 2 分钟）。

> ℹ️ **本版分发时已替你迁移好部署库**：5 张纯缓存表整体替换为已验证的新口径数据，17 张用户数据表行数逐张比对，**全部未变动**（`trades` 23 / `screenshots` 92 / `trade_screenshots` 87 / `users` 2 …）。

---

## 一、这一版在解决两件事

| # | 问题 | 触发 | 结论 |
|---|---|---|---|
| 1 | 「涨停/连板/炸板」这些情绪指标够不够？ | 用户提出：**「一字板你判断不了吗？」「中军涨才能说明除了游资外大资金也进场了」** | 补齐两个数据层指标 + 逐字段审计复权口径 |
| 2 | PCB 2026-09-14 到底该算「发酵」还是「分歧」？ | 用户裁定：**「我觉得 PCB 9/14 应该算发酵吧」**，并要求排查「发酵/分歧会不会因为中军的加入而误判」 | 中军不是元凶；真正的毛病是**分歧判据只有一条腿** |

---

## 二、V1.009.8 · 新增「中军」—— 大资金有没有进场

### 定义与口径

**中军 = 板块内「流通市值」前 3 名成分股的当日平均涨幅。**

三条口径都是刻意的，不是随手写的：

| 选择 | 做法 | 为什么 |
|---|---|---|
| 用**流通市值**（东财 `f21`） | 不用总市值 `f20` | 总市值含大量不流通的限售股，**真金白银能推动的盘子看流通** |
| 名单**静态取定、不逐日重排** | 按流通市值排一次，之后固定 | 若每天重排，「今天大涨所以进了中军榜」＝**用未来信息** |
| **不排除涨停股** | 涨停的中军照算 | 涨停的中军**恰恰是最强信号**，排掉它等于把最强的证据丢掉 |

### 标定结果与直觉相反

| 阶段 | 中军涨幅中位 |
|---|---|
| **启动** | **+2.770** ← 最强 |
| 发酵 | +2.05 |
| 高潮 | +1.253 |
| 分歧 | +1.16 |
| 潜伏（无周期） | +0.52 |
| 退潮 | −2.03 |

**中军在「启动」最强，「高潮」反而只有 +1.25** —— 与扩散度、板块涨幅**同向**（同 V1.009.7 那两个反直觉发现：扩散度在高潮低于发酵、高潮的板块平均涨幅低于发酵）。

### 由此推出三条口径

| 阶段 | 中军怎么用 | 理由 |
|---|---|---|
| **高潮** | **硬条件**：`中军 > 0` | 用户裁定。高潮要的是「游资 + 大资金一起进场」，中军为负说明大资金没参与。改后**高潮组 100% 中军为正** |
| 发酵 / 启动 | **不参与** | 中军在这两态本来最强 —— 拿高潮的特征去卡启动，**语义搞反了** |
| 退潮 | **加分腿（或）**：`中军 ≤ 0` 且 `板块涨幅 < 0` 且 `扩散度 < 50%` | 潜伏组约 40% 中军也为负，单用会把「无周期」误标成退潮，故加护栏 |

### 为什么退潮写「或」不写「且」

退潮是**判负**，`中军 ≤ 0` 是**负向证据**。写成硬条件（且）要多凑一条腿才成立 → **退潮更难触发**，方向反了。写成加分项（或）它才有独立触发权。

> **通用规律：负向证据写「或」，正向证据写「且」。**
> 实测佐证：加了这个加分腿后，退潮组里只有 **5.3%** 中军为正，干净。

---

## 三、V1.009.8 · 新增「一字板」+ 全链路复权口径审计

### 一字板

**判据**：当日 `开 = 收 = 低 = 高`，且封在**当日涨停价**上，四价全用**不复权**，容差 **0.005（5 厘）**。

> ⚠️ 容差不能放到 0.011 —— 那会把**T 字板**（开=收=高≠低）误判成一字板。5 厘只吸收浮点误差。

**取舍：一字板只作展示 + 高潮佐证，不进判定。**（用户裁定）
理由：一字板是「**买不到**」的强势。直接进判据会把「一致性」（大家都不卖）和「可交易性」（你根本买不进）混为一谈。

实测 `ow_n` 分布（840 条主线板块行）：`0→611`、`1→186`、`2→31`、`3→9`、`4/5/6→各1`，**`ow_n ≥ 1` 占 27.3%**。

### 复权口径审计（本轮最关键的一项）

逐字段核对后确定**口径分工**——这一条错了，除权日会产生系统性偏差：

| 用途 | 口径 | 原因 |
|---|---|---|
| 涨跌幅、中军涨幅 | **前复权** | 跨除权日**连续可比**（分红季约 66% 个股在窗口内有除权） |
| 涨停 / 跌停 / 连板 / 炸板 / **一字板四价** | **不复权** | 判定必须用**当日真实成交价**，否则除权日会凭空多出/少掉涨停 |

**验证**：源码级逐字段核对 **15/15 PASS**；结果级抽 24 只个股 × **2856 个交易日**（含 **85 个除权日**）→ **一致率 99.58%、生产口径漏判 0 例**。

### K 线结构升级为 11 元组

用户明确「原始 K 线数据肯定是需要的，open 和 low 都很有意义」——一字板与炸板都离不开 open/low。字段**一律追加在尾部**（向后兼容）：

```
[0]日期 [1]前复权收 [2]量 [3]额 [4]前复权高
[5]不复权收 [6]不复权高
[7]前复权开 [8]前复权低 [9]不复权开 [10]不复权低
```

---

## 四、V1.009.8 · 修掉的缺陷

| 缺陷 | 症状 | 修法 |
|---|---|---|
| **断板数漏计首板断板** | 「昨日**首板**、今日没再涨停」的股票没被算成断板 → 断板数系统性偏少 | 断板口径改为对 **`prev_zt`（昨日所有涨停股，含首板）** 判定，不再只看连板 |
| 接力溢价 / 晋级率未落库 | 前端只能显示 `-` | 落库 `prem`（接力溢价）/ `promo`（晋级率） |

---

## 五、V1.009.9 · 分歧的「扩散未走弱」护栏

### 触发与第一步排查

用户裁定 PCB 2026-09-14 应为**发酵**。当天 PCB 的形态是：

| 指标 | 9/14 | 9/15 |
|---|---|---|
| 最高板 | **2 → 3（创了新高）** | 3 |
| 涨停家数 | **8** | 6 |
| 扩散度（上涨占比） | **76.1%** | **52.7%** |
| 中军涨幅 | −2.776 | +0.608 |
| 旧判据结果 | ❌ 分歧 | 分歧 |

**先排除中军**：9/14 中军为负 → 被「高潮硬条件」挡下 → 顺位掉到下一个分支。
**所以落点错在「分歧」的判据，不在「中军」。中军只影响「能否进高潮」，不影响「是否掉进分歧」。**

### A3 分歧的判据原本只有一条腿

```
旧: is_split = (炸板率异常) 或 (断板 ≥ 2)
```

标定发现 `断板 ≥ 2` 在活跃态内：**发酵组命中 19% / 分歧组命中 97%**。

> 这里我最初怀疑「阈值太松」，**被数据推翻了** —— 97% vs 19% 的分离度很强，它**不是太松，而是核心判据**。真正的毛病是它是**单腿**：活跃态里「有断板」太常见（连板梯队本来就在换手），单靠它必然把大量发酵误判成分歧。

### 一个被实测否决的方案（本轮最重要的认知修正）

我原本提议加「**龙头仍在创新高**」护栏（`hi_rising`），依据是统计「龙头创新高：高潮 28% vs 转弱 8%（3.5×）」。

**这个统计是错的 —— 它没隔离活跃态，混进了大量潜伏 / 冰点样本。**

只取活跃态后，方向**完全相反**：

| | 分歧组 | 发酵组 | 比值 |
|---|---|---|---|
| 「龙头创新高」占比 | **52%** | **40%** | **0.77×（方向反）** |

加它会让分歧**更难**触发，与目标完全相反 → **放弃**。

> **两条方法论教训（已写进代码注释）**：
> ① **分组统计前必须先把自己限定在正确的子总体里** —— 一个 3.5× 的「强信号」可以是子总体混淆造出来的假象。
> ② **覆盖率 97% ≠ 有判别力** —— 高覆盖率配上低覆盖的对照组才是好判据。

### 真正的第二腿是「扩散度」

| | 发酵组 | 分歧组 |
|---|---|---|
| `up_r` 中位 | **0.806** | **0.587** |

阈值扫过 **0.70 / 0.75 / 0.80**（下表只记了有留档的两档）：

| 阈值 | 误判率（本应判发酵却被判成分歧） | 9/14 是否判发酵 |
|---|---|---|
| **0.70** | **0.09** ← 最优 | ✅ |
| 0.80 | 0.12 | ❌（0.761 仍被判分歧） |

**0.70 的最优性体现在两个方向同时成立**：它比 0.80 的误判率更低（判别更准），又是唯一能让 9/14（`up_r=0.761`）落回发酵的档位。取 0.75 则两边都不占优，故不用。

**新判据**：

```
A3 分歧 = 有撕裂（炸板率异常 或 断板 ≥ 2）  且  扩散度已走弱（up_r < 0.70）
```

误判率从单腿时的 **0.20 → 0.09**。分离得也干净：保留为分歧的样本 `up_r` 中位 0.587，改判发酵的样本 0.806。

### 语义

| 形态 | 含义 | 判为 |
|---|---|---|
| 撕裂 **且** 扩散已走弱 | 养家的「**龙头首阴**」 | **分歧** |
| 撕裂 **但** 扩散仍强 | **内部换手**（涨停家数不少、上涨占比仍高、龙头还在创新高） | **发酵** |

**PCB 就是这个对照**：

```
09-14  断板 5 只，但扩散度 76.1%、涨停 8 家、最高板 2→3 板创新高  →  发酵（内部换手）
09-15  扩散度掉到 52.7%，涨停缩到 6 家                            →  分歧
```

两天构成一个完整的「**换手 → 走弱**」序列。旧口径把 9/14 也判成分歧，等于把换手错当成了崩塌。

---

## 六、实测结果

### 阶段分布变化（70 个交易日 × 主线板块 = 852 行，真重建）

| 阶段 | 改前 | 改后 | 变化 |
|---|---|---|---|
| **分歧** | 137 | **74** | **−63** |
| **发酵** | 278 | **341** | **+63** |
| 潜伏 / 冰点 / 启动 / 高潮 / 退潮 | — | — | **零变化** |

**只有分歧与发酵对调** —— 说明护栏精确命中了 A3 → A4 那一条边界，没有溢出到别的分支。

### 改后各阶段形态（构造效度）

| 阶段 | n | `up_r` 中位 | 断板中位 | 涨停中位 |
|---|---|---|---|---|
| 发酵 | 341 | **0.806** | 1 | 5 |
| 高潮 | 130 | 0.746 | 2 | 5 |
| 启动 | 88 | 0.812 | 0 | 5 |
| **分歧** | **74** | **0.587** | **4** | **4** |
| 退潮 | 36 | **0.161** | 3 | 2 |

分歧组是唯一「断板高 + 扩散低」的组合；退潮组扩散度只有 0.161，分离干净。

### 一致性断言

- 活跃态内「分歧 **且** `up_r ≥ 0.70`」**漏网 0 条** ✅
- 被改判的 63 条样本 **100% 满足发酵条件**，无一掉进兜底分支 ✅

### 全市场重建

5915 只个股（5903 只有 K 线）、4394 只热度榜、**70/70 天落库**；840 条板块行含 `jun_pct`（中位 **1.509**，`≤0` 占 **27.9%**）、`ow_n ≥ 1` 占 **27.3%**。

### 涨停池召回率（数据层回归）

跌停 **100%** / 炸板 **100%** / 涨停 **98.9%**。剩 2 只（`002846` / `300656`）属同花顺前复权回溯污染，已确认与判定逻辑无关，不再追。

---

## 七、界面上的变化

| 位置 | 变化 |
|---|---|
| **周期阶段 · 悬停明细** | 新增 `中军涨幅`（高潮时标注「硬条件：大资金已进场」）与 `一字板 n 家` |
| **分歧 TIP** | 重写：「分歧 =（炸板率异常 或 断板 ≥2）**且扩散度已走弱（<70%）**」，附 9/14 vs 9/15 实例 |
| **反直觉说明（新增一条）** | 「区分分歧用的是**扩散度**，不是『龙头是否创新高』」，并附 52% vs 40% 的否决证据 |
| **主线题材表** | 阶段列随之更新（PCB 9/14 由「分歧」变「发酵」） |

---

## 八、升级后必须做的操作

1. **执行一次「历史数据重建」**（70 个交易日，约 2 分钟）。
   不重建的后果是**静默的**：一字板在历史日恒为 0、阶段仍是旧口径，**接口和页面都不报错**，只是数据是旧的。
2. 若你上一版的部署库里留过 `kind=live` 的快照（最后浏览过的实盘日），重建会**主动覆盖**它们（V1.009.9 起只对「今天」保留 live 保护）。若发现某个日期字段仍缺，先删该日快照再重建一次。

---

## 九、这一版是怎么验证的

| 项 | 方式 | 结果 |
|---|---|---|
| 复权口径 | 源码级逐字段核对 + 24 只 × 2856 交易日结果级比对（含 85 除权日） | 15/15 PASS；一致率 99.58%，漏判 0 例 |
| 数据层 | 单元测试 + 与东方财富逐只比对 + 全池召回 | 14/14 PASS；9/16 PCB 涨停逐只一致；涨停召回 98.9% |
| 判据层 | 真重建 70 天 + 7 项断言 | **7/7 PASS** |
| 生产口径回归 | `verify_production_phase.py`：504 板块 × 72 交易日 = 23670 条，走的是**后端真正上线的那份实现**（`rebuild_board_daily` + `attach_board_phases`） | 分布 / 构造效度 / 炸板率单调链 **达标**；④跨级转移、⑦次日走向**未达标但均被本版改善**（详见 §十 第 6~8 条） |
| 判据改动的净效应 | 同源 A/B：运行期覆盖 `_EMO_SPLIT_UPR`（9.0＝关闭 / 0.70＝开启），**不改生产代码**，同一份数据跑两遍 | 分歧 1231→828、发酵 1217→1615，其余阶段几乎不动；④ 54.6%→45.5%；炸板率 1.92×→**2.01×** |
| 界面 | 无头浏览器实渲染，读取真实页面上的阶段标签与浮层原文 | PCB 9/14 显示「发酵」，理由文案完整 |
| 打包 | 双 zip 逐项校验（22 项） | 全部通过 |
| 部署 | 迁移后逐表比对 + 启动后 `/api/health` 与页面 | 17 张用户表行数**全部未变动**；版本号 1.0.9.9 |

---

## 十、已知局限（诚实说明，不掩饰）

1. **`up_r < 0.70` 与三档误判率同源于同一份 70 天样本，存在过拟合风险，样本外未验证。** 建议按季度滚动复核这个阈值。
2. **「内部换手」是语义解读，不是独立验证过的结论。** 数据支持的只有「扩散度能把两组分开」这一件事；把它解释成「内部换手」是我基于养家理论的读法。
3. **护栏把分歧样本砍掉了 46%**（137 → 74）。此前文档里分歧组的各项均值（§5.2 那批）**需要按新样本重算才能引用**，旧数字已失效。
4. **中军名单是"静态取定"的**，若成分股流通市值排序在窗口内发生重大变化（如大额解禁），名单会滞后。
5. **「一字板」不进判定**——它只能告诉你「有没有情绪」，区分不了「高潮 vs 分歧」（高潮 38.8% vs 分歧 35.0%，太接近）。
6. **两项构造验收指标仍未达标，但它们是预存问题，不是本版引入的**（本轮专门做了同源 A/B 才查清）：

   | 指标 | 护栏关闭（＝上一版口径） | **护栏开启（本版）** | 目标 |
   |---|---|---|---|
   | 活跃态内跨 ≥2 级阶段转移 | 54.6% ❌ | **45.5% ❌** | ≤30% |
   | 炸板率 分歧 ÷ 高潮 | 1.92× ❌ | **2.01× ✅** | ≥2× |
   | 高潮次日转弱 vs 发酵 | 73.1% vs 81.0% ❌ | **62.4% vs 65.7% ❌** | 高潮 > 发酵 |

   即：**本版把这三项全部朝正确方向推动了**（跨级转移改善 9.1pp、炸板率首次达标、⑦ 的缺口从 −7.9pp 收窄到 −3.3pp），
   但没有一项是「达标」。
7. **跨级转移率偏高的最可能来源是上一版的「中军 > 0 高潮硬条件」**（该条件是你裁定的）——
   它把大量「达峰值但中军为负」的板块从「高潮」挤到了「分歧」，两边都变碎。
   → 若要继续压低这个指标，方向应是**重新审视中军硬条件的收紧幅度**，而不是再加护栏。
8. **⑦ 的样本很薄**：本版 70 天里「高潮」只有 198 条（占 0.84%），用它算「次日走向」噪声很大，
   引用这个数字时要连着 n 一起看。

---

## 十一、兼容性

- **数据库**：无新增表；K 线字段追加在尾部，旧数据靠补 `None` 兼容（不报错，但功能降级，见开头升级须知）。
- **API**：板块行新增 `jun_pct` / `ow_n` / `prem` / `promo` 字段；`stage` 取值集合不变（仍为 6+1），只是分布变化。
- **前端**：需配套更新（本版已含）。
- **配置 / 数据目录**：无变化。
