# 汽车整车「今日启动」与「无周期」并存 —— 根因排查

> 日期：2026-09-18 ｜ 对象：BK1029 汽车整车 ｜ 结论：**两条独立通路的必然冲突，不是数据错**

---

## 0. 一句话结论

「今日启动」和「情绪周期阶段」是**两套互不相干的判据**，用同一个快照里两个不同字段渲染：

| 界面元素 | 数据字段 | 后端算法 | 看历史吗 |
|---|---|---|---|
| 卡片上方「**今日启动**」标签区 | `concept.emerging` | V1.009.6 独立信号（源码 1230 / 2021 行） | **不看** |
| 卡片下方「概念涨幅榜」的「**阶段**」列 | `concept.top_up[].stage` | V1.009.7 六阶段状态机 `_board_phase` | **看**（昨日状态 + 20 日炸板基准 + 10 日在榜历史） |

汽车整车 9/17 同时命中两者：**emerging 成立** → 进「今日启动」；**`_board_phase` 判「潜伏」** → 前端 `stageLabel` 把潜伏渲染成「无周期」。

源码注释自己写明了这件事：
- 后端 2172 行：`# 今日启动信号（V1.009.6）：与主线表互相独立，不做排序过滤`
- 前端 1584 行：`// 涨幅高不等于在周期里 —— 很多是「无周期」中的一日脉冲。`

---

## 1. 两条通路的判据（实测常量）

### 1.1「今日启动」`emerging`（V1.009.6）

```python
_EMERGING_TOP   = 20     # 板块涨幅进全市场概念前 N 名
_EMERGING_RATIO = 3.0    # 且 涨停占板块比 >= 3%
```

```python
_top_by_pct = sorted([b for b in ranked if not _is_noise_board(b.get("name"))],
                     key=lambda x: -x["pct"])[:_EMERGING_TOP]
for b in _top_by_pct:
    _eratio = (_ezt / _esz * 100) if _esz else 0.0
    if _eratio < _EMERGING_RATIO:
        continue
    emerging.append({...})
```

**只有 2 个条件，且全部是「当日横截面」** —— 无历史、无状态机、无强度分位、无中军。
设计意图（源码 2021 行注释）：补上「启动首日 / 重启首日」在四维打分里的结构性盲区（历史组占 60%，新题材结构上拿不到高分）。

### 1.2「情绪周期阶段」`_board_phase`（V1.009.7/V1.009.8）

输入 8 项，**其中一半依赖历史**：

| 输入 | 含义 | 是否依赖历史 |
|---|---|---|
| `row.pct / zt / up` | 当日涨幅 / 涨停 / 上涨家数 | 当日 |
| `qz` | 涨停强度 z 在**当日在册板块**中的分位 | 当日横截面 |
| `zb_base` | 该板块**近 20 日**炸板率均值 | ✅ 历史 |
| `active_recent` | 此前 **10 日**内是否出现过活跃态 | ✅ 历史 |
| `prev_stage` | **上一交易日**阶段 | ✅ 历史 |
| `prev_lbc` | 上一交易日最高连板 | ✅ 历史 |
| `jun_pct` | 中军（流通市值 top3）平均涨幅 | 当日 |
| `sh_pct` | 上证指数涨跌幅（相对强度基准） | 当日 |

---

## 2. 汽车整车 9/17 逐步复算

### 2.1「今日启动」为什么命中

| 条件 | 阈值 | 汽车整车 | 结论 |
|---|---|---|---|
| 涨幅排名（全市场概念，剔噪声板块） | ≤ 20 | **第 7 名**（pct=+1.88%） | ✅ |
| 涨停占比 | ≥ 3.0% | **7.7%**（2 家 / 26 只成分） | ✅ |

⇒ **`emerging` 命中**，进入「今日启动」标签区（`in_main=True`，快照原文）。

### 2.2「情绪周期」为什么是潜伏（→ 无周期）

关键：**9/16 = 冰点**（−1.58%、涨停 0、上涨 13.04%）→ 9/17 走的是 `_board_phase` 的**分支 B（昨日非活跃态）**。

逐条比：

| 分支 | 判据 | 汽车整车实际 | 结果 |
|---|---|---|---|
| **B1 启动** | `not active_recent` **且** `qz≥0.85` 且 `up_r≥0.55` 且 `lbc_max≤2` 且 `zt≥2` | 其余 3 条**全满足**，但 **`active_recent=True`**（9/14 刚判过启动） | ❌ 被锁 |
| **B2 直判发酵** | `qz≥0.85` 且 `up_r≥0.55` 且（`lbn≥1` **或** `zt≥3`） | zt=2、lbn=0 → 两条**都不满足** | ❌ |
| **B3 冰点** | `zt==0` 且 `rs<−1.0` 且 `up_r<0.35` | zt=**2** ≠ 0 | ❌ |
| **兜底** | — | — | ✅ → **潜伏** |

输出原文（快照）：

```
name=汽车整车  stage=潜伏  pct=1.88
reason=未进入情绪周期（无涨停联动，或强度/扩散度不足）；中军 +0.01%
```

⇒ 前端 `stageLabel = (s) => (s === '潜伏' ? '无周期' : s)`（DailyReview.jsx:1121）→ 显示「**无周期**」。

### 2.3 当日同卡片的完整画面

「今日启动」6 条（`emerging`）：

| 板块 | 涨幅 | 涨停 | 占比 | in_main | 阶段 |
|---|---|---|---|---|---|
| 转基因 | +3.68% | 2 | 10.0% | ✅ | 发酵 |
| 粮食概念 | +3.26% | 3 | 8.1% | ✅ | 发酵 |
| CRO | +2.56% | 2 | 4.1% | ❌ | **潜伏** |
| **汽车整车** | **+1.88%** | **2** | **7.7%** | ✅ | **潜伏** ← 你看到的 |
| 调味品概念 | +1.38% | 2 | 6.1% | ❌ | — |
| 供销社概念 | +1.23% | 1 | 4.8% | ❌ | — |

⇒ 6 条里 **2 条（33%）** 阶段是「潜伏」＝ 界面显示「无周期」。**这是普遍现象，不是汽车整车特有。**

---

## 3. 全期规模：这不是孤例

遍历 72 个交易日、963 条 `emerging` 记录：

| 命中「今日启动」时的阶段 | 条数 | 占比 |
|---|---|---|
| **潜伏（→ 前端「无周期」）** | **277** | **28.76%** |
| 发酵 | 272 | 28.25% |
| 启动 | 185 | 19.21% |
| 高潮 | 48 | 4.98% |
| 分歧 | 37 | 3.84% |
| （不在主线表 / 涨幅榜 TOP10，取不到） | 144 | 14.95% |

- **「今日启动 + 无周期」= 277 / 963 = 28.76%** —— 每 3.5 条就有 1 条矛盾
- 「今日启动 + 阶段∈{启动,发酵}」（真正的启动窗口）= 457 / 963 = 47.46%

⇒ 你的感受是对的：**这个组合在界面上每天都会出现**，只是恰好今天你盯了汽车整车。

---

## 4. 你的口径「发酵需要启动前置」—— 现状偏差

### 4.1 全期「发酵」入边统计（重跑生产状态机）

有效行 23668，其中「发酵」1606 行。**前一日状态分布：**

| 前一日 | 条数 | 占比 | 是否合规 | 来源分支 |
|---|---|---|---|---|
| 潜伏 | **514** | **32.00%** | ❌ | **B2 直判** |
| 发酵 | 455 | 28.33% | ✅ 延续 | A4/A5 |
| 分歧 | **268** | **16.69%** | ❌ | A4/A5 |
| 启动 | 151 | 9.40% | ✅ | A4/A5 |
| 冰点 | **93** | **5.79%** | ❌ | **B2 直判** |
| 退潮 | **92** | **5.73%** | ❌ | **B2 直判** |
| 高潮 | **33** | **2.05%** | ❌（你明确禁止） | A4/A5 |

**合规（启动→发酵 / 发酵→发酵）= 606 (37.73%)**
**违规 = 1000 (62.27%)**

### 4.2 根因：`B2 直判发酵` 这条规则

```python
# ---- 分支 B：昨日非活跃态（启动 / 发酵 / 冰点 / 潜伏）----
if (not active_recent and qz >= _EMO_START_QZ and upr >= _EMO_START_UPR
        and lbmax <= 2 and zt >= 2):
    return "启动", ...
# ↓↓↓ 这一条让 潜伏/冰点/退潮 直接跳「发酵」，完全绕过「启动」
if (qz is not None and qz >= _EMO_START_QZ and upr >= _EMO_START_UPR
        and (lbn >= 1 or zt >= 3)):
    return "发酵", ...
```

它在**昨日非活跃态**的分支里，却输出「发酵」—— 这正是「发酵无启动前置」的源头，贡献 **699 天（514+93+92 = 43.5%）**。

另外两条：
- **分歧→发酵 268 天** —— 若「延续分歧」被正式采纳（你 09-18 的第 2 点裁定），这 268 天应改判为「分歧（延续）」
- **高潮→发酵 33 天** —— 你已明确「高潮后只可能是分歧或退潮」

### 4.3 若严格执行「发酵需启动前置」，影响面

| 改动 | 涉及天数 | 后续待定 |
|---|---|---|
| 删/改 B2 直判 | 699 | 改判为「启动」还是「潜伏」？ |
| 分歧→发酵 | 268 | 采纳「延续分歧」→ 判分歧 |
| 高潮→发酵 | 33 | 判分歧或退潮 |

粗算「发酵」将从 1615 → 约 615 行（−62%），「启动」大幅增加。
**这是本轮以来最大的一次口径改动，牵动全盘阶段分布，需要你先定改判方向再动手。**

---

## 5. 附带发现：兜底文案与事实不符（真 bug）

汽车整车 reason 写的是：

> 「未进入情绪周期（**无涨停联动**，或强度/扩散度不足）」

但它当天**有 2 家涨停**（安凯客车、众泰汽车，均首板）。真实原因是：

1. 昨日非活跃，且近 10 日内有活跃 → **B1 被 `active_recent` 锁死**
2. 涨停 2 家 < 3 家 → B2 不够

⇒ 与上一轮发现的 A3「龙头首阴（最高板 1 → 0）」属于**同一类问题**：兜底/模板文案在边界情况下给出与事实相反的依据，会误导判断。零风险可修。

---

## 6. 结论与待定

**问 2（为什么矛盾）：已完全查清。** 两条独立通路 + 阈值口径不同，`emerging` 不看历史、`stage` 看历史，汽车整车恰好卡在中间：
- 当日强度够（涨幅第 7、涨停占比 7.7%）→ 进「今日启动」
- 但从状态机看它昨日是冰点、10 日内又活跃过、涨停不足 3 家 → 判不出「启动/发酵」，落「潜伏」→ 显示「无周期」

**问 1（发酵需启动前置）：现状 62.27% 违规，根因是 B2 直判。** 需要你定三件事：
1. B2 的 699 天改判成「**启动**」还是「**潜伏**」？
2. 「分歧→发酵」268 天是否随「延续分歧」一起改判？
3. 「高潮→发酵」33 天改判「分歧」还是「退潮」？

---

## 附：取证脚本

| 脚本 | 作用 |
|---|---|
| `backend/_car_0916p.py` | 9/17 两条通路字段对照（emerging / main_lines / top_up） |
| `backend/_fade_entry.py` | 全期「发酵」入边统计 + emerging×stage 交叉 |
| `backend/_emerging_stage.py` | 全期 emerging 命中 × 阶段分布（72 日 963 条） |

均只读，未改任何生产代码、未动版本号。
