# AIReviewSystem V1.009.10 发布说明

**发布日期**：2026-09-17
**版本类型**：前端缺陷修复（体验修复，无数据层改动）
**上一版本**：V1.009.9
**说明**：V1.009.10 = V1.009.9 全部内容 + **「AI 每日复盘」草稿提示误报修复**

> ℹ️ **本版不需要重建历史数据、不需要迁移数据库** —— 改动只在前端（3 个文件），后端与数据表结构、历史快照、缓存口径**全部未动**。升级只需要替换程序文件。

> ℹ️ **本版分发时已替你保留原库**：部署库 `app.db` 未做任何改写（本次不涉及），并额外留了一份时间戳备份 `app.db.bak_v1.009.10_*`。

---

## 一、这一版在解决什么

| # | 问题 | 触发 | 结论 |
|---|---|---|---|
| 1 | 「AI 每日复盘」里手写的内容**保存成功后，仍然每次都弹提示问草稿是否要保存** | 用户反馈：「我写了内容保存后依然每次都会弹出来提示我草稿是否需要保存，这个影响使用体验」 | 不是单点 bug，是 `utils/draft.js` 里**两个独立缺陷叠加**（依赖引用 + 缺「已持久化」概念），两个都修掉 |

复现时的视觉指纹很明确：复盘卡片上 **「已保存」与「有草稿」两个 Tag 同时挂着** —— 一边说已经存了，一边说还有草稿没存。

---

## 二、根因（两个缺陷，各自都能独立造成这个现象）

### 缺陷 ①：防抖 effect 的依赖用了「对象引用」

调用点的写法是：

```jsx
const draft = useDraft(draftKey, { formData: manual ? { manual } : null })
```

`{ manual }` 是**对象字面量**，每次 render 都会新建一个对象 —— 而 effect 的依赖数组 `[key, formData]` 用的是**引用比较**，所以**每次 render 都被判定为「依赖变了」**：

```
render → 依赖"变了" → 重排 400ms 定时器
       → 定时器到点：saveDraft(...) + setDraft(新对象)
       → setState 触发 render → 回到第一步
```

于是形成一个 **400ms 一轮的循环**：保存成功后刚把草稿 `clear()` 掉，下一轮定时器又把它**自动写回来**。表现就是「明明保存了，还是提示有草稿」。

**修法**：依赖改用**序列化后的字符串**（`JSON.stringify(formData)`）—— 内容不变时字符串就是同一个值，引用自然不变，effect 不会再空转；同时补齐定时器清理。

### 缺陷 ②：从数据库读回来的内容被当成了「新草稿」

打开某个交易日时，接口会把数据库里的 `manual_content` 回填进表单 → `formData` 发生变化 → 自动保存逻辑把它当成「用户在输入」→ **存了一份与数据库里一模一样的内容作为草稿**。

于是你切走再切回该日期，`checkDraft` 读到这份草稿，就弹「检测到未保存的草稿」—— 而它其实**一个字都没改过**。

**修法**：引入「**基线**」概念 —— 记录该草稿 key 当前「**已持久化**内容的指纹」。收紧后的语义只有一句：

> **草稿存在 ⇔ 当前内容 ≠ 已持久化内容**

内容与已落库内容一致时，**不产生草稿**；`checkDraft` 读到一份与基线相同的草稿，视为「多余草稿」直接清掉。

---

## 三、修法：把「登记基线」和「清草稿」拆成两件事

新增两个语义分明的方法，替代原来含糊的一个动作：

| 方法 | 何时调用 | 行为 |
|---|---|---|
| `markClean(values[, key])` | **从库/接口载入**一份「已持久化」的内容 | 只登记基线，**绝不删** localStorage 里的草稿 |
| `markSaved(values[, key])` | **保存成功** | 登记基线 **＋** 清掉草稿（内容已在库里，草稿再无价值） |
| `clear([key])` | 用户主动「放弃草稿」 | 只清草稿，**不动基线** |

> ⚠️ **为什么必须拆开**（我第一版写错过，自己审计出来的）：
> 第一版只写了 `markClean`，并且顺手把草稿一起删了。但 `markClean` 会在「从库载入内容」时被调用 ——
> 而那一刻**表单里的内容随时可能覆盖掉用户尚未确认的编辑**。顺手删草稿会造成
> 「切走再切回，弹窗还没点，草稿已经被清掉」的**丢数据**问题。
> **「登记基线」与「清草稿」是两件事，不能合成一个动作。**

同步改造了两个调用点：

- `DailyReview.jsx`（AI 每日复盘的「我的复盘」编辑区）：`loadByDate` 回填后 `markClean`；保存成功由原来的 `clear()` 改为 `markSaved`
- `PeriodSummary.jsx`（阶段复盘「写总结」弹窗）：`openEdit` 预填后 `markClean`；保存成功 `markSaved`；「放弃草稿」回到库里的值后 `markClean`

改动量：`utils/draft.js` +104/−21、`DailyReview.jsx` +15/−3、`PeriodSummary.jsx` +21/−6（共 3 文件 **+140/−30**）。

---

## 四、验证（双向自证，不只是「不弹了」）

用 Chrome 无头 + CDP **真实渲染**跑完整交互（不是只看接口返回），并对**正反两个方向**都做了断言：

| 场景 | 断言 | 结果 |
|---|---|---|
| S1 打开一个**数据库里已有内容**的日期 | 载入后无草稿、无「有草稿」Tag、显示「已保存」 | ✅ |
| S2 输入内容 → 点「保存复盘」 | 草稿被清空、无「有草稿」Tag、仍显示「已保存」 | ✅ |
| S3 切到别的日期再切回来 | **不再弹出**「检测到未保存的草稿」确认框 | ✅ **你报的问题消失** |
| T2 反向：输入内容后**不保存**就切走再切回 | 草稿**仍在**，并**正常弹出**「检测到未保存的草稿」，可点「恢复草稿」找回 | ✅ **草稿功能没有被削弱** |

截图留档（工作区根目录）：`草稿提示_修复前_自证.png` / `草稿提示_修复后_自证.png` / `草稿提示_反向回归_未保存草稿仍保留.png`。

> ⚠️ 顺带记录一个**测试手法**上的坑（已写进我的截图技能，避免下次再花时间）：
> 用合成事件改受控输入框的值（`dispatchEvent(new Event('input'))` / 原生 setter / `execCommand('insertText')`）
> **三种写法都会让 `el.value` 变掉、`execCommand` 还返回 `true`，但 React 收不到 `onChange`** ——
> 页面不重渲染，受控值也不会被回滚，**看起来"输入成功"**，其实组件状态、派生的 localStorage、网络请求全都没动。
> 我据此误判了 5 轮，最后才定位到是**输入手法**而不是业务逻辑。正解是走浏览器级真实输入
> （真实点击聚焦 → Ctrl+A → CDP `Input.insertText`）。
> 注意别把这个结论扩大化：**`el.click()` 点按钮是有效的**，失效只发生在 input/textarea 的**值注入**上。

---

## 五、界面变化

无新增界面元素 —— 本次是**让已有的两个 Tag 不再自相矛盾**：

- 保存成功后：只显示「已保存」，不再同时挂「有草稿」
- 真正有未保存内容时：正常显示「有草稿」+ 切日期弹恢复确认（**这个行为必须保留**，否则就变成丢数据了）
- 顺手修掉一个静默问题：**打开一个已有内容的日期，不会再无端生成一份与库里相同的草稿**

---

## 六、升级后需要做什么

**什么都不用做。**

- 不需要「历史数据重建」
- 不需要迁移数据库（本版未改任何表结构、缓存结构、判据口径）
- 直接把新版的 `AIReviewSystem.exe` 与 `_internal` 覆盖过去即可；`app.db` 与 `uploads/` 保持不变

> 🔄 **回滚**：部署目录里保留了 `AIReviewSystem.exe.bak_v1.009.9` 与 `_internal.bak_v1.009.9`，需要退回上一版时改回原名即可（库不需要动，两个版本共用同一个库结构）。

---

## 七、兼容性

| 项 | 说明 |
|---|---|
| 数据库 | **完全兼容**，本版不改 schema、不写历史数据 |
| 历史快照 / 缓存 | **完全兼容**，本版不改判据、不重算任何指标 |
| 前端缓存（localStorage） | 兼容。旧版遗留的草稿仍可被读取；若某份旧草稿的内容与库里完全相同，会被判为「多余草稿」自动清掉（这正是期望行为） |
| 与 V1.009.9 的关系 | V1.009.10 = V1.009.9 + 本修复。只装过更早版本的用户，装本版即包含 V1.009.8 / V1.009.9 的全部内容（那两版的**升级后必须重建历史**要求仍然适用，见 V1.009.9 发布说明） |
