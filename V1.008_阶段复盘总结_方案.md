# V1.008 功能方案：阶段复盘手写总结（我的阶段总结）【确认版】

> 提出时间：2026-09-02 ｜ 目标版本：V1.008 ｜ 状态：**4 点决策已确认，进入实施**
> 核心诉求：① 在软件内手写/编辑阶段复盘总结并保存在本地数据库；② AI 阶段复盘分析时自动读取这些总结作为上下文，实现"阶段性复盘的连续性改进"。

---

## 0. 已确认的 4 点决策（用户拍板）

| # | 决策点 | 确认结果 |
|---|--------|----------|
| 1 | 时间段选择方式 | **周复盘 / 月复盘 两种模式**：周复盘选某一个周（跨度周一 ~ 周日）；月复盘选某一个月（跨度 1 号 ~ 本月最后一日）。不再用任意起止日期 |
| 2 | 绑定维度 | **只选品种，不考虑币种**。绑定键 = (时间段, 品种类型)，品种类型取值：全部/通用、A股、商品期货、数字货币 |
| 3 | 文件导入 | **做**，支持 **.txt / .md / .docx（Word）** 三种格式。docx 用 Python 标准库 zipfile+xml 解析段落文本，**不引入 python-docx/lxml**（避免打包体积膨胀 ~20M） |
| 4 | AI 结果保存 | **做**。AI 阶段分析结果 JSON 自动保存到对应 phase_reviews 记录的 `ai_result` 字段，支持跨期 AI 结论对比 |

---

## 1. 需求理解

| 用户诉求 | 拆解 |
|----------|------|
| "导入或者打开一个页面，自己手写阶段复盘总结" | 阶段复盘页内嵌"我的阶段总结"区块，周/月粒度选择 + 品种维度，支持 txt/md/Word 导入 |
| "保存在本地数据库" | 新增 phase_reviews 表，随 app.db 备份走，多用户数据隔离 |
| "AI 阶段复盘分析的时候也可以调用阅读" | phase_summary 构造 prompt 时注入：**本期总结 + 历史最近 3 期总结** |
| "方便分析阶段性复盘的连续性改进" | AI 对比"上一期总结中提出的问题/改进计划"与"本期数据与本期总结"，输出改进落实情况追踪（continuity） |
| "AI 结果顺带保存" | AI 分析结果 JSON 落库，历史 AI 结论可跨期查看 |

一句话方案：**阶段复盘页新增"📝 我的阶段总结"区块（周/月粒度 + 品种维度，支持 txt/md/docx 导入）；AI 阶段分析时自动携带本期 + 历史最近 3 期手写总结，输出"连续性改进追踪"，并把 AI 结果顺带保存回对应总结记录。**

---

## 2. 总体架构

```
┌─────────────────────────── 前端 PeriodicAnalysis.jsx ───────────────────────────┐
│  筛选器(时间/业务种类/币种) → 统计卡/图表（现有，不动）                            │
│  ┌────────────────────────────────────────────────────────────┐                  │
│  │ 📝 我的阶段总结（新）                                        │                  │
│  │  · 模式切换：周复盘 / 月复盘（Radio）                        │                  │
│  │  · 周选择（DatePicker picker="week"，归一周一~周日）           │                  │
│  │    或 月选择（DatePicker picker="month"，归一月初~月末）      │                  │
│  │  · 品种选择（全部/A股/商品期货/数字货币）                     │                  │
│  │  · 当前(周/月×品种)总结：显示/新建/编辑/删除（弹窗多行文本）    │                  │
│  │  · 编辑弹窗支持【导入文件】：.txt/.md/.docx                    │                  │
│  │  · 历史总结列表：时间倒序，可展开查看（含 AI 结果摘要）         │                  │
│  └────────────────────────────────────────────────────────────┘                  │
│  [AI 阶段分析]（现有按钮）→ 弹窗新增：                                   │
│  · "已参考 N 期手写总结"标签                                            │
│  · "改进落实情况追踪"区块（continuity，状态着色）                    │
└──────────────────────────────────────────────────────────────┬───────────────┘
                                                               │ REST
┌─────────────────────────────── 后端 ──────────────────────────▼───────────────┐
│  routers/phase_reviews.py（新）  ──  CRUD + 历史查询 + 文件解析                │
│    · GET  /api/phase-reviews?period_type=&start=&end=&instrument_type=        │
│    · GET  /api/phase-reviews/history?end=&instrument_type=&limit=3            │
│    · POST /api/phase-reviews（新建/覆盖）                                      │
│    · PUT  /api/phase-reviews/{id}                                             │
│    · DELETE /api/phase-reviews/{id}                                           │
│    · POST /api/phase-reviews/parse-file（txt/md/docx → 纯文本，docx 零依赖）    │
│  services/analysis.py phase_summary（改）                                      │
│    · 注入本期 + 历史最近 3 期手写总结（每期截断 300 字控 token）                │
│    · PHASE_OUTPUT_FORMAT 增加 continuity 字段                                  │
│    · 成功后把 AI 结果 JSON 保存到对应 phase_reviews 的 ai_result（决策4）        │
│  models.py PhaseReview（新表 phase_reviews） → create_all 自动建表              │
└──────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. 数据模型（新表 phase_reviews）

```python
class PhaseReview(Base):
    """阶段复盘手写总结（V1.008）"""
    __tablename__ = "phase_reviews"

    id: Mapped[int] = mapped_column(Integer, primary_key=True, autoincrement=True)
    user_id: Mapped[int | None] = mapped_column(Integer, default=None, nullable=True)
    period_type: Mapped[str] = mapped_column(String(10), default="week")  # week / month / custom(AI占位)
    start_date: Mapped[date] = mapped_column(Date, nullable=False)   # 阶段开始日（周=周一；月=1号）
    end_date: Mapped[date] = mapped_column(Date, nullable=False)     # 阶段结束日（周=周日；月=月末）
    instrument_type: Mapped[str] = mapped_column(String(20), default="")  # ''=全部/通用；A股/商品期货/数字货币（绑定维度，决策2）
    title: Mapped[str] = mapped_column(String(100), default="")      # 标题（可选，如"第8周复盘"）
    content: Mapped[str] = mapped_column(Text, default="")           # 手写总结正文（''=仅AI结果占位）
    ai_result: Mapped[str] = mapped_column(Text, default="")         # 最近一次 AI 阶段分析结果 JSON（决策4）
    created_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now)
    updated_at: Mapped[datetime] = mapped_column(DateTime, default=datetime.now, onupdate=datetime.now)
```

**唯一约束**：`(user_id, period_type, start_date, end_date, instrument_type)` —— 同一个"周/月 × 品种"只有一条总结，重复提交走编辑覆盖（upsert 语义）。

**关键设计**：
- 周/月归一化在**前端**完成（dayjs 计算周一~周日 / 1号~月末），后端只存归一后的起止日期，纯数据层不感知周月换算
- 时间范围 **包含结束日**（end_date 存"周日"或"月末最后一天"，不 +1 天），匹配统一用 `start_date <= X AND end_date >= X`
- AI 结果占位记录：AI 分析时若没有匹配的手写总结记录，自动创建一条 `content=''` 的占位记录存 AI 结果（period_type 随前端传入，默认 custom）

---

## 4. 后端 API（新路由 routers/phase_reviews.py）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/phase-reviews?period_type=&start=&end=&instrument_type=` | 查某（周/月×品种）组合的总结；`start/end` 缺省时返回该用户全部总结（时间倒序） |
| GET | `/api/phase-reviews/history?end=&instrument_type=&limit=3` | **AI 连续性用**：end 之前的历史总结（时间倒序，默认 3） |
| POST | `/api/phase-reviews` | 新建；同键已存在则**覆盖更新**（upsert，返回 200+记录） |
| PUT | `/api/phase-reviews/{id}` | 编辑（title/content） |
| DELETE | `/api/phase-reviews/{id}` | 删除（校验归属，非本人 403） |
| POST | `/api/phase-reviews/parse-file` | 上传文件解析纯文本：.txt/.md 直读；.docx 用 zipfile+xml 提取 `<w:p>` 段落 |

- 鉴权：全部挂现有 `get_current_user`；写操作校验 `review.user_id == 当前用户`
- 入参校验：`start <= end`、content 上限 50000 字
- **docx 解析（零依赖）**：docx 本质是 zip，`word/document.xml` 中 `<w:p>` 为段落、`<w:t>` 为文本；用 `zipfile + xml.etree.ElementTree` 按命名空间提取，段落间换行拼接。忽略表格/图片/样式，仅取正文文字

---

## 5. 前端交互（PeriodicAnalysis.jsx 新增区块）

**位置**：筛选区 Card 与统计卡之间插入"📝 我的阶段总结"Card（span=24）。

**控件布局**（一行）：
```
[周复盘 | 月复盘]（Radio.Group）
[DatePicker picker="week" | picker="month"]（随模式切换，允许清除）
[品种 Select：全部/通用、A股、商品期货、数字货币]
[写总结/编辑] [导入文件] [删除]      —— 状态区：最后编辑时间
```

**状态流转**：
1. 周/月模式 + 日期 + 品种 任一变化 → `GET /api/phase-reviews?period_type=&start=&end=&instrument_type=` 加载该组合总结
2. 无总结 → 空态"本阶段还没有手写总结" + 【写总结】按钮
3. 有总结 → 显示标题 + 内容（最多 8 行截断，展开全文）+ 最后编辑时间 + 【编辑】【删除】（Popconfirm 确认）
4. 【写总结/编辑】→ 弹窗：标题（可选）+ 多行文本框（autoSize {minRows:8,maxRows:22}）+ 底部【导入文件】按钮（隐藏 file input，accept=".txt,.md,.docx"）+ 保存/取消
   - **导入流程**：选文件 → .txt/.md 前端 `FileReader` 直读文本；.docx 前端读为 ArrayBuffer 上传 `/api/phase-reviews/parse-file` 解析 → 返回纯文本 → **追加填入文本框**（不覆盖，可编辑后再保存）
5. 【历史总结】→ 区块底部折叠列表：该用户全部总结（时间倒序，每条显示 周期标签+标题+摘要+AI结果徽标），点击展开只读全文
6. **AI 分析联动**：
   - 后端自动注入匹配的总结（时间相交 + 品种匹配，本期 + 历史最近 3 期）
   - 弹窗 AI 结果区新增：标签行"📋 参考了你的 N 期手写总结" + "🔄 改进落实情况追踪"区块（continuity：item / status 着色：已改进绿、部分改进黄、未改进红、无法判断灰 / evidence）

**约定**：前端不缓存总结，控件变化重新拉取；周模式 DatePicker 用 `value` 显示周内任意一天，保存时归一：`start = value.subtract((value.day() || 7) - 1, 'day')`，`end = start.add(6, 'day')`；月模式：`start = value.startOf('month')`，`end = value.endOf('month')`。

---

## 6. AI 集成：连续性改进分析 + 结果保存

### 6.1 匹配规则（_load_manual_reviews）

```
本期 = start_date <= end AND end_date >= start（时间相交）的记录，按 end_date 倒序
       （若顶部筛选了具体品种，只匹配该品种；筛选"全部品种"则不过滤品种）
历史 = end_date < start 的记录，按 end_date 倒序取 3 条
注入总数上限 5 条，每期 content 截断 300 字
```

### 6.2 prompt 注入（_build_phase_messages 新增段）

```
=== 用户手写阶段总结（时间顺序，最后一条为本期；AI 应尊重并引用用户自己的视角）===
【08-03 ~ 08-09 · 周复盘 · 全部】(第8周)
  本期总结：<content 截断300字>
【08-10 ~ 08-16 · 周复盘 · 全部】 ← 本期
  本期总结：<content 截断300字>

要求：
1. 如果存在【上一期及更早】的总结，逐条对比其中提出的"问题/改进计划"与本期数据、本期总结，
   判断是否落实，输出到 continuity 字段。
2. 手写总结与统计/交易明细冲突时，以统计为准，并在 summary 中说明差异。
```

### 6.3 输出格式新增字段（PHASE_OUTPUT_FORMAT 追加 continuity）

```json
{
  "...原字段不变...",
  "continuity": [
    {"item": "上期提出的改进点/问题", "status": "已改进|部分改进|未改进|无法判断", "evidence": "依据"}
  ]
}
```

- 无历史总结时 `continuity: []`，前端不渲染该区块；解析沿用 `_extract_json` + 新增 `_continuity_list(v)` 规范化

### 6.4 AI 结果保存（决策4，phase_summary 收尾）

```
phase_summary 成功后：
1. 查找"本期"匹配记录（时间相交；优先 instrument_type 匹配；存在多条取 end_date 最近）
2. 命中 → 更新该记录 ai_result = 完整结果 JSON
3. 未命中 → 创建占位记录（content=''，period_type=路由传入或'custom'，title='AI阶段分析'，start/end=筛选范围）
4. 路由返回结果里附带 saved_to_review_id（前端可提示"已保存至该阶段总结"）
```

---

## 7. 数据迁移与兼容

- **建表**：models.py 新增 PhaseReview → ensure_schema 的 create_all 自动创建（本项目惯例，无需 ALTER）
- **清洁版**：build_zips.py 遍历清空全部表，phase_reviews 自动纳入（无需改脚本）
- **备份**：总结存 app.db，随现有备份/升级流程（复制 app.db 即迁移完成）
- **兼容**：旧数据无该表，启动自动建表；旧 AI 分析调用（不带 include_manual）行为不变

---

## 8. 测试计划

新建 `test_phase_review_api.py`（沿用现有测试基建，连 8011）：
1. CRUD 全链路：新建/查询/编辑/删除
2. 同键覆盖：同 (user,period_type,start,end,instrument_type) 二次 POST → 更新而非重复
3. 权限隔离：用户 B 访问用户 A 的总结 → 404/403
4. 参数校验：start>end、空 content → 400
5. AI 注入：mock `chat()` 断言 prompt 包含"用户手写阶段总结"与总结内容；无总结时不注入
6. continuity 解析：mock 返回含 continuity 的 JSON → 规范化正确；status 非法值回退"无法判断"
7. docx 解析：构造最小 docx（zipfile 手写 document.xml）→ parse-file 返回正确文本
8. 回归：test_period_metrics_api 等现有套件全过

---

## 9. 发布计划（沿用 V1.007.2 沉淀的 10 步流水线）

1. 后端：models.py + routers/phase_reviews.py + analysis.py 改动 + main.py 注册
2. 前端：PeriodicAnalysis.jsx 新增区块 + api/index.js 新增接口
3. 版本号 7 处同步 → V1.008（main.py ×2 / launcher.py / MainLayout.jsx / build_zips.py ×3 / README.md / 需求日志第 50 条）
4. 前端 build + 重启 8011 开发服务 → 测试套件全绿
5. PyInstaller 打包 → exe 冒烟（断言 V1.008 + phase-reviews 接口 200）
6. 部署目录同步（备份 .bak_v10072 → 换 exe/_internal → 验证数据保留）
7. build_zips.py 生成双 zip（**已修复 .bak_* 排除，体积维持 83M/91M 量级**）
8. git commit + tag V1.008 + push（直连 `-c http.proxy=`）
9. 更新 MEMORY.md 流水线记忆 + 需求日志
10. GitHub Release 由用户网页操作（只传清洁版）
