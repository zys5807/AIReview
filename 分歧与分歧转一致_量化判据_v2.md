# 分歧与分歧转一致 · 量化判据 v2

> **v2 相对 v1 的变化**：v1 是按你口述 + 我的实证自拟的；v2 接入你提供的腾讯文档
> （智能文档 `DSmRWUUp4clRSeWdm`，AI 生成、引用 14 篇资料），逐条做**可计算化 + 数据可得性核查 + PCB 全链路回算**。
> v1 的判据凡与文档冲突的，以文档 + 实测为准。
>
> **本文档所有数字均为实测**，来源：东财官方三池接口（`getTopicZTPool` / `getTopicZBPool` / `getTopicDTool`）、
> 本地 `backend/app.db`（70 天快照）、`stock_kline_cache`、通达信不复权日线。
>
> ⚠️ **一条更正**：v1 及前轮对话中我说过「超声电子 9/10、9/11、9/14 连续三个一字板」——**这是错的**。
> 根因是我误读了缓存 bar 的字段布局（详见 §1.3）。真实数据是：三个**收盘涨停的换手板**，不是一字板。

---

## 1. 数据可得性核查（先看这个 —— 它决定哪些判据能落地）

### 1.1 完全可用（覆盖率 100%）

数据源：`daily_market_cache.snapshot_json → limit_up.stocks[]`，70 天共 **5,414 条**涨停记录。

| 字段 | 覆盖 | 可支撑的判据 |
|---|---|---|
| `lbc` 连板数 | **5414 / 5414 = 100%** | **梯队完整度**（首板/2板/3板分布）、最高板、断板面 |
| `amount` 个股成交额 | 100% | **缩量**（环比）、资金容量 |
| `ltsz` 流通市值 | 100% | **市值分层**（中军 / 后排） |
| `hs` 换手率 | 100% | 换手板 vs 缩量板 |
| `zbc` 炸板次数 | 100% | 板的质量（烂板识别） |
| `is_st` | 100% | ST 剔除口径 |
| `daily_market_cache → limit_down.stocks[]` | 有 | **跌停与核按钮** |
| `board_hist.pct` | 70 天 | **板块涨幅排名**（启动阈值） |

> **这条推翻了上一轮的一个判断**：我曾说"历史缓存的池子字段全是空"。错。
> `fbt/lbt/industry/stat` 才是空的（0.6%），而 `lbc/amount/ltsz/hs/zbc` 是 **100% 完整**。
> 梯队完整度这个最关键的新判据，**历史 70 天全部可回测**。

### 1.2 只在"当天"可用 —— 历史不可用

| 字段 | 覆盖 | 说明 |
|---|---|---|
| `fbt` 首次封板时间 | **32 / 5414 = 0.6%** | 只有 2026-09-15 这一天的 32 条有值 |

`fbt` 是**判定「一字板」最干净的字段**：
- `09:25:00` = 竞价一字（如 9/15 的 `002912 中新赛克 lbc=4`）
- `09:30:00 ~ 09:30:30` = 秒板
- 下午 = 尾盘板 / 烂板

**但它只有当天有。** 所以"一字板"判据在历史回测上**不可用**。

### 1.3 ⚠️ 一个必须澄清的工程陷阱：缓存里没有 open / low

`stock_kline_cache.bars_json` 的 7 元组布局是（技能里写过，我上一轮读错了）：

```
[0] 日期  [1] 前复权收  [2] 量  [3] 额  [4] 前复权高  [5] 不复权收  [6] 不复权高
```

**没有开盘价，没有最低价。** 因此：

| 想做的判定 | 能否用缓存 | 替代方案 |
|---|---|---|
| 「涨停」 | ✅ 用 `b[1]` 环比 | — |
| 「收盘即最高（封板至收盘）」 | ✅ `b[4] == b[1]` | — |
| **「一字板」（开=高=低=收）** | ❌ **无法判定** | 见下 |
| 「缩量」 | ✅ `b[3]` 成交额环比 | — |

**"一字板"的两条出路**：
- **A（轻）**：历史回测降级为「涨停 **且** 收盘=最高 **且** 换手率低于自身近 20 日均值」——识别的是"缩量封板"，不是严格一字
- **B（重）**：新抓一张东财不复权日线表（`fqt=0`，含 OHLC），历史 70 天补抓
- **C（实时）**：当天用 `fbt` 精确判（数据已经有）

**我的建议**：**A + C**。理由：B 要补抓 ~5000 只 × 70 天，成本高；而文档自己说
「有时候也不一定是严格的一字，而且有时可能高潮出现在盘中」——**你要的本来就不是严格一字，而是"买不到"这个状态**。A 能表达这个状态。

### 1.4 已算出但**没落库** —— 白算的

| 指标 | 状态 | 位置 |
|---|---|---|
| `prem` 接力溢价 | rebuild 里已算（`daily_market.py:2895-2900`） | **未写入 `board_hist`** |
| `promo` 晋级率 | rebuild 里已算（`2893`） | **未写入 `board_hist`** |
| `brk_n` 断板数 | rebuild 里已算（`2891`） | **未写入 `board_hist`** |
| `lbc_n` 连板家数 | rebuild 里已算 | 落库了（= `lbc2`） |

`board_hist` 当前只落 12 个字段：
`date / name / pct / zt / up / size / ratio / lbc_max / lbc2 / amt / stage / zb_r`

**文档五项判定表里有 3 项吃的是没落库的字段**（断板面、接力溢价、首板数）→ 必须扩字段。

---

## 2. 文档判据的可计算化

### 2.1 高潮：文档说"方向对但不充分，要补四个条件"

| # | 文档原文条件 | 可计算化 | 数据 |
|---|---|---|---|
| 1 | **梯队完整**：不只有龙头一字，下面要有 2 板、3 板、首板梯队 | `lbc2 ≥ 2`（连板梯队 ≥2 只）**且** 首板数 ≥ 3 | ✅ `lbc` |
| 2 | **炸板率低位但非极度压缩**：清一色一字反而是"一致到极致"，次日风险最大 | `zb_r ≤ 0.20` **且** `zb_r > 0`（不是 0） | ✅ `zb_r` |
| 3 | **昨日涨停溢价高、红盘率高** | `prem ≥ 0` **且** `红盘率 ≥ 60%` | 🔧 需落库 |
| 4 | **补涨扩散而非孤龙**：低位首板批量上、中军放量 | 后排（市值后 50%）上涨占比 ≥ 70% **或** 中军（市值前 10）中位涨幅 ≥ 1% | ✅ `ltsz` + 日线 |

文档给的**实操定性建议**（这条我直接采纳，写成状态机的出口）：

| 文档情形 | 建议定性 | 我的状态机对应 |
|---|---|---|
| 龙头首次缩量封板 + 涨停环比明显增加 + 梯队完整 | **发酵尾段 / 进入高潮** | 发酵 → 高潮 |
| 连续 2 天以上全板块一字、补涨满天飞、低位杂毛都板 | **高潮极致/毕业照**（只卖不追） | 高潮 → 高潮（带 `peak_exhausted` 标记）|
| 龙头封板但跟风炸板多、首板少、中位断层 | **不判高潮，判"假加速/潜在分歧"** | 发酵 → 分歧 |

### 2.2 分歧：文档分四种情况（这是 v2 最重要的增量）

| 情形 | 文档特征 | 可计算化 | 状态机出口 |
|---|---|---|---|
| **(1) 强分歧/高低切** | 老龙温和断板（放量换手、不跌停）＋新龙同属性补涨＋板块低位首板仍有＋跌停少＋溢价非负 | `老龙次日涨跌 > -5%` 且 `新龙∈本板块` 且 `首板数 ≥ 3` 且 `板块跌停 ≤ 1` 且 `prem ≥ 0` | **分歧**（周期未死）|
| **(2) 分歧偏退潮** | 老龙爆量断板（高开低走/天地/跌停）＋新龙独木（板块仅它 1~2 只）＋中位批量掉队＋溢价转负 | `老龙次日涨跌 ≤ -5%` 且 `涨停数 ≤ 2` 且 `prem < 0` | **分歧**（带 `decay_watch` 标记）|
| **(3) 旧退潮·新试错** | 老龙断板＋新龙属**另一概念**＋老板块中军阴线 | `新龙 ∉ 本板块` 且 `中军中位 < 0` | **退潮** + 全市场侧记"新方向试错" |
| **(4) 分歧转一致** | 老龙断板但新龙一字、老龙**次日反包**、新龙带跟风继续晋级 | 见 §2.4 | **分歧 → 发酵/高潮**，**不重新计冰点** |

> 文档明确说 (4) 要「阶段仍按原发酵/高潮延续处理，**不重新计冰点**」——
> 这正是我上一轮报的缺陷 ②「退潮→启动不可达」的姊妹问题。

### 2.3 五项判定表（文档给"老龙断板日"用的，可直接当判据）

| # | 文档判据 | 量化 |
|---|---|---|
| 1 | 老龙收盘：反包/红盘/平盘＝**承接好**；-5% 以下或跌停＝**坏** | 昨日 `lbc_max` 那只票今日涨跌幅 |
| 2 | 新龙属性：同老板块＝补涨/高低切；不同＝新题材 | 今日最高板票是否 ∈ 本板块成分 |
| 3 | 板块首板/2板数：环比不崩＝有活口；仅新龙 1 只＝孤立 | 今日 `lbc==1` 计数、`lbc2` 计数 |
| 4 | 跌停与核按钮：板块高位跌停 ≥3 或全市场跌停扩散＝退潮；个位数且多在后排＝分歧 | 板块内跌停数 + 全市场跌停数 |
| 5 | 昨日涨停溢价：正＝可修复；负且红盘率 <30%＝退潮 | `prem` + `红盘率` |

**文档给的结论套用**（我原样采纳为路由规则）：

```
1好 + 2同  + 3不崩 + 4轻 + 5正   → 强分歧/高低切
1坏 + 2同  + 3仅新龙 + 4中 + 5转负 → 分歧转退潮观察
1坏 + 2新题材 + 3新首板群 + 4全市场不崩 → 旧板块退潮、新板块启动试错
1好 + 2同  + 3增强 + 5正次日      → 分歧转一致
```

### 2.4 「分歧转一致」的判据（文档第 (4) 种 + 实测补强）

文档只给了定性描述，我补成可证伪的：

```
分歧转一致 = 以下三条同时成立
  ① 新龙确立：今日最高板 > 昨日最高板（新高度）               [实测 9/16: 3→4 ✅]
  ② 承接修复：炸板率下降 ≥ 50%  OR  接力溢价环比回正           [实测 9/16: 0.375→0 ✅]
  ③ 老龙未深杀：老龙今日涨跌 > -5%（反包/红盘）                [实测 9/16: +5.10% ✅]
```

---

## 3. PCB 全链路回算（BK0877，202 只成分股）

这是**用你给的案例做端到端验证**。先用我的模型复现，再看文档判据对不对。

### 3.1 逐日实录（东财官方接口，非估计）

| 日期 | 涨停 | 炸板 | 最高板 | 梯队结构 | 板块pct | 涨幅排名 | 我的判定 | 文档判据 |
|---|---|---|---|---|---|---|---|---|
| 09-04 | 0 | — | — | — | -2.73% | 429/441 | 冰点 | 冰点 |
| **09-07** | **19** | — | — | — | **+4.45%** | **4/441** | 发酵 ❌ | **应为启动** |
| 09-08 | 2 | — | — | — | -0.03% | 338/441 | 分歧 | — |
| 09-09 | 2 | — | 1 | — | +1.10% | 21/441 | 分歧 | — |
| 09-10 | 2 | — | 1 | — | -0.15% | 26/441 | 发酵 | — |
| 09-11 | 7 | — | 2 | — | -0.33% | 12/441 | 发酵 | 发酵 |
| **09-14** | **8** | 4 | **3** | 3板×1 + 2板×1 + 首板×6 | **+2.19%** | 14/441 | 高潮 ✅ | **高潮**（梯队完整✅）|
| **09-15** | **6** | **5** | 3 | 3板×1 + 首板×5（**无 2 板**）| +1.01% | **7/421** | 发酵 ❌ | **强分歧/高低切** |
| **09-16** | **8** | **0** | **4** | 4板×1 + 2板×1 + 首板×6 | — | — | — | **分歧转一致** |

> **9/15 与 9/16 的涨停家数经东财官方池二次核对**：9/15 = **6 家**、9/16 = **8 家**（你核的没错）。
> 9/16 全市场涨停 **89 家**（9/15 是 32 家）——市场情绪整体回暖。

### 3.2 9/15 五项判定表逐项打分（文档判据 → 实测值）

| # | 文档判据 | 9/15 实测 | 判定 |
|---|---|---|---|
| 1 | 老龙收盘 | **超声电子 +5.61%**（高开 23.00、最高 24.89 触板、收 23.90）| **承接好** ✅ |
| 2 | 新龙属性 | **澳弘电子 2板→3板**，属本板块 | **同板块** ✅ |
| 3 | 首板/2板数 | 涨停 6 家中 **首板 5 只**、3板 1 只 | **有活口，不崩** ✅ |
| 4 | 跌停与核按钮 | 板块跌停 **0**、全市场跌停 **0** | **轻** ✅ |
| 5 | 昨日涨停溢价 | 9/14 涨停 8 只在 9/15 的涨幅：均值 **+2.31%**、中位 +2.77%、正溢价 5/8 | **正** ✅ |

**→ 文档结论：`1好 + 2同 + 3不崩 + 4轻 + 5正 = 强分歧/高低切`** —— 五项全中。

而同期硬指标也全部指向分歧（不是高潮）：
- 断板面 **7 / 8 = 87.5%**（9/14 涨停 8 只，9/15 只剩 1 只续板）
- 炸板数 **4 → 5**
- 涨停数 **环比 −25%**（8 → 6）
- 梯队**断层**：9/14 有 2 板，9/15 一只都没有

### 3.3 9/16 的验证 —— 文档第 (4) 种「分歧转一致」

| 判据 | 9/15 | 9/16 | 是否成立 |
|---|---|---|---|
| ① 新龙确立（最高板创新高） | 3 | **4** | ✅ |
| ② 承接修复（炸板率降 ≥50% 或溢价回正） | 炸板 5 家 | **炸板 0 家** | ✅ |
| ③ 老龙未深杀 | +5.61% | **+5.10%** | ✅ |
| ④（加分）接力溢价 | +2.31% | **+3.46%** | ✅ |

**9/16 八家涨停明细**：澳弘电子 4 板 + 西陇科学 2 板 + 6 只首板
（中岩大地 / 威尔高 / 东材科技 / 奥士康 / 威派格 / 依顿电子）

→ **`分歧转一致` 成立**，且**次日走势证实了这个判定**。文档第 (4) 种在真实数据上跑通了。

### 3.4 ⚠️ 一条与文档直觉相反、但要如实报告的实测

文档说「补涨扩散而非孤龙：**中军放量趋势**，才算板块级高潮」。我按板块市值前 10 只算"中军"：

| 日期 | 中军（市值前10）中位涨幅 | 上涨只数 | 后排（51+）中位 | 后排上涨占比 |
|---|---|---|---|---|
| 09-11 | +0.30% | 5/8 | -1.48% | — |
| **09-14** | **-1.82%** | **0/8** | **+2.12%** | **83%** |
| **09-15** | +0.66% | 7/7 | +0.02% | 50% |

**9/14 是"小票狂欢、大票被抽血"；9/15 才是中军转强。** 和"高潮=中军大涨"的直觉**错开一天**。

如果按文档把"中军大涨"设成高潮的**硬条件**，9/14 反而判不出高潮。
→ **需要你定**（见 §7）。

---

## 4. 与当前 v1.0.9.7 实现的差异（含两个已定位的根因缺陷）

### 4.1 判定结果差异

| 日期 | v1.0.9.7 | 应有 | 差因 |
|---|---|---|---|
| 09-07 | 发酵 | **启动** | 缺陷 A：`active_recent` 拦截 |
| 09-14 | 高潮 | 高潮 ✅ | — |
| 09-15 | 发酵 | **强分歧** | 缺陷 B：`brk_n` 漏首板断板 |

### 4.2 缺陷 A：`active_recent` 拦掉 602 个真启动信号

启动判据里有前置条件「近 10 日内不能出现过活跃态」（`_EMO_NEW_LOOKBACK=10`）。

**PCB 9/07 就是被它拦掉的**：9/04 冰点（涨停 0）→ 9/07 涨停 19 家、上涨 88%、排名 4/441。
但 8/31 那天是"发酵"，落在 10 日窗口内 → 不判启动 → 落到"发酵"。

全样本量化：满足启动形态的共 **1,820** 次，其中 **602 次被这个条件拦截**，
且 **100% 是"昨日冰点/潜伏/退潮"** 的情况。

**这同时解释了上一轮报的「退潮 → 启动 = 0 次」** —— 同一个条件在卡：
刚退潮的板块必然"近期活跃过"，于是永远无法重新启动。

**修法**：前置条件从「近 10 日无活跃」改为「**昨日为非活跃态（冰点/潜伏/退潮）**」。

### 4.3 缺陷 B：`brk_n` 只数连板断板，漏掉首板断板

`daily_market.py:2891`：

```python
brk_n = len(prev_lb - set(zt_codes)) if prev_lb else 0
```

而 `prev_lb` 只在 `_lb >= 2` 时才追加（`2877`）——**昨日首板、今天断板的一个都不算**。

**9/15 PCB 是这个 bug 的活标本**：9/14 涨停 8 只 → 9/15 只剩 1 只续板，**真断板 7 只，代码只数到 1 只**
（昨日连板只有超声电子 + 澳弘电子 2 只，其中 1 只续板）。

于是 `is_split = (炸板率异常升 or 断板 ≥ 2)` 两个腿都不成立：
- 断板数 1 < 2 ❌
- 炸板率 0.375 < 自身基期 + 0.25 ❌

→ 落到"发酵"。**龙头炸板这个最明确的分歧信号，被整个漏掉了。**

**修法**：`prev_lb` 改用**昨日全部涨停股集合**；并新增显式判据
「**昨日最高板那只票今日未涨停**」（= 文档说的"龙头首阴/断板"）。

### 4.4 缺陷 C（上一轮已定位，仍未修）：最新交易日 bar 字段错位

`limit_state()` 判涨停读 `b[5]`（标称"不复权收"）。实测**历史 bar 正常，只有最后一根错位** ——
`b[5]≈最低价`、`b[6]≈开盘价`。

| 全市场 09-15 | 值 |
|---|---|
| 东财官方涨停池 | 32 家 |
| `limit_state` 判定 | **28 家（漏 4 家）** |
| 板块传导 | **79 / 504 个板块 `zt` 被低估，合计少算 95 家次** |
| 09-14（历史日）对照 | 判 57 vs 东财 55 —— **漏 0 家** |

→ **只影响最新交易日**，但**实时路径与重建同源**，所以当天盘后看到的板块涨停家数同样偏少。
**PCB 9/15 落库 `zt=5`，东财 = 6，差的就是西陇科学（002584）。**

---

## 5. 改动清单

### 5.1 后端字段（`board_hist` 扩 12 → 19 字段）

新增需落库：`prem`（接力溢价）、`promo`（晋级率）、`brk_n`（断板数）、
`lead_chg`（老龙今日涨跌幅）、`new_lead`（今日最高板票代码）、
`lead_in_board`（新龙是否属本板块）、`zt_first`（首板数）

### 5.2 状态机（`_board_phase()`）

| 改动 | 内容 |
|---|---|
| 拆 | 启动前置条件 `active_recent` → 改为「昨日 ∈ {冰点, 潜伏, 退潮}」|
| 修 | `brk_n` 口径改为「昨日全部涨停股 − 今日涨停股」|
| 加 | 龙头断板显式判据：昨日 `lbc_max` 票今日未涨停 |
| 加 | 分歧三出口路由（五个判据打分，见 §2.3）|
| 加 | 分歧转一致判据（三条，见 §2.4）|
| 加 | 高潮四条件（梯队/炸板率/溢价/扩散）|

### 5.3 前端

- 分歧阶段悬停展示 **五项判定表**（老龙/新龙/活口/跌停/溢价）
- 新增「分歧转一致」标记（现 6 阶段命名不变，作为 `stage` 的子标记 `sub_stage`）

---

## 6. 验收线（可证伪）

| # | 验收项 | 阈值 | 当前 |
|---|---|---|---|
| 1 | PCB 9/07 | 判 **启动** | 发酵 ❌ |
| 2 | PCB 9/14 | 判 **高潮** | 高潮 ✅ |
| 3 | PCB 9/15 | 判 **分歧** | 发酵 ❌ |
| 4 | PCB 9/16 | 判 **分歧转一致** | 未入库 |
| 5 | `退潮 → 启动` 转移数 | **> 0** | 0 ❌ |
| 6 | `冰点 → 发酵` 直接跳转 | **= 0**（必须先经启动）| 484 ❌ |
| 7 | 首日判"发酵"占比 | **< 30%** | 53.1% ❌ |
| 8 | 全市场 09-15 涨停数 | **= 32**（东财口径）| 28 ❌ |
| 9 | 构造效度不许退化 | 最高板 冰点 0 < 发酵 1.68 < 高潮 4.36；炸板率 分歧 ≥ 高潮 2 倍 | 通过 |

---

## 7. 需要你定的四件事

1. **「一字板」判据的取舍**：接受 **A（缩量封板近似）+ C（实时日 fbt）**，
   还是要上 **B（补抓 70 天不复权日线）**？
2. **「中军大涨」是硬条件还是加分项**？§3.4 实测显示 9/14 中军 **-1.82%、0/8 上涨**。
   设成硬条件 → 9/14 判不出高潮。
3. **9/14 与 9/15 谁是高**？你之前说"15日整体开始出现高潮迹象"，
   但数据（涨停 8→6、炸板 4→5、梯队断层、断板 7/8）指向 9/15 是**分歧**。
   是你看的是**板块整体/其他软件口径**，还是我漏了什么？
4. **缺陷 C 先修还是先写判据**？我建议**先修**——否则分歧判据吃的是错的输入。

---

## 附：本文档用到的复验命令

```bash
# 1) 东财官方三池（可指定历史日）
curl -s "https://push2ex.eastmoney.com/getTopicZTPool?ut=7eea3edcaed734bea9cbfc24409ed989&dpt=wz.ztzt&Pageindex=0&pagesize=600&sort=fbt%3Aasc&date=20260915" -H "Referer: https://quote.eastmoney.com/"
# getTopicZBPool = 炸板池；getTopicDTPool = 跌停池

# 2) 本地字段覆盖核查
backend/venv/Scripts/python.exe _d_cov.py

# 3) PCB 全链路（涨停池 + 接力溢价）
backend/venv/Scripts/python.exe _d_three.py
backend/venv/Scripts/python.exe _d_prem.py

# 4) 腾讯文档正文（智能文档，需连接器授权）
cd ~/.workbuddy/plugins/cache/*/tencent-docs-plugin/*/skills/tencent-docs
python tencentdocs.py tdoc_call tencent-docs smartcanvas.read '{"file_id":"JdVQJxrTRygf","size":20}'
#   file_id 来源：manage.query_file_info 用分享 ID DSmRWUUp4clRSeWdm 解析
#   next_token 分页，共 3 页 5,644 字符
```
