# 情绪周期判定逻辑全解 与「发酵入边」修正取证

> 2026-09-17 · 生产口径 = V1.009.10（`_EMO_JUN_ON=0.0` / `_EMO_SPLIT_UPR=0.70`）
> 数据：504 个板块 × 72 个交易日（2026-06-09 ~ 09-17），23668 条参与判定的板块日
> 取证脚本：`backend/_trans_matrix.py`（只读，运行期包 hook，未改生产码）
> 起因：升升提问「各状态的判断逻辑是什么 / 什么叫退潮·中军腿 / 发酵不该从高潮转来」

---

## 一、前置：板块当日是否参与判定

`_emo_eligible(row)` —— **不满足就不判周期，直接显示「潜伏 + 样本过小」**

| 条件 | 阈值 | 常量 |
|---|---|---|
| 有效成分股数 | ≥ 20 只 | `_EMO_MIN_CNT` |
| 非伪板块 | 宽基 / 资金属性 / 交易标签 / 地域统计一律排除 | 复用 `_is_noise_board` |

> 20 只里 2 家涨停 = 10%，置信区间横跨 3%~32%，贝叶斯收缩也救不回语义 —— 宁可说「样本不足」，不伪造一个阶段。

## 二、强度分位 `qz` 怎么来的

三步，缺一不可：

1. **板块涨停强度（收缩后）**
   `_emo_z(row) = (zt + 20 × 0.016328) / (count + 20)`
   K=20 是等效样本量，p₀=1.6328% 是全样本基准涨停占比。
   作用：把「20 只里 2 家涨停 = 10%」收敛到 5.8%，而 985 只的板块几乎不动（收缩量仅 20/1005）。

2. **当日横截面分位**
   在**当日在册的全部合格板块**里排名，`qz = bisect_right(zs, z) / n`。
   必须跨板块排名，否则「20 只 vs 985 只」的规模歧视会从这条缝里钻回来。

3. **沿日期推进状态机**（逐板块，状态跨日传递）

> ⚠️ **`_emo_z` 对超大板块系统性偏低**：772 只里 20 家涨停 z≈0.0398，80 只里 19 家 z≈0.1933（**差 4.9 倍**）。机器人概念（772 只）连续 27 天 qz 卡在 0.69，永远够不到启动门槛 0.85。**这是独立于中军的第二个缺陷，尚未修**。

---

## 三、六个状态的判定逻辑（逐条）

状态机分**两大分支**，由 `prev_stage in _EMO_ACTIVE` 决定走哪条：
`_EMO_ACTIVE = ("启动", "发酵", "高潮", "分歧")`

### 分支 A —— 昨日在活跃态内（按 A1 → A5 **短路**判断，命中即返回）

| 顺序 | 名称 | 触发条件 | 输出 | 依据文案 |
|---|---|---|---|---|
| **A1** | 明显转弱 | `rs < -0.8` **且** `upr < 0.40` **且** `zt ≤ 1` | 退潮 | 「明显转弱：板块跑输大盘、上涨仅 x%、涨停 x 家」 |
| **A1b** | **退潮·中军腿** | `jun ≤ 0` **且** `rs < 0` **且** `upr < 0.50` | 退潮 | 「中军 −x%转负——游资不接、大资金亦撤」 |
| **A2** | 高潮 | `peak_ok` **且**（`hi_rising` **或** 非撕裂） | 高潮 | 「出现 N 板高标；中军 +x%（硬条件：大资金已进场）」 |
| **A3** | 分歧 | `is_split` **且** `upr < 0.70` | 分歧 | 「龙头首阴（最高板 X → Y）＋炸板/断板；扩散度仅 x%」 |
| **A4** | 发酵 | （`lbn ≥ 1` **或** `qz ≥ 0.80`）**且** `upr ≥ 0.50` | 发酵 | 「连板 N 家、涨停 N 家、上涨 x%，梯队仍在扩散」 |
| **A5** | 兜底 | `rs < 0` → 退潮；否则 → 发酵 | 退潮/发酵 | 「跑输大盘 x 个点，且梯队未接上」 |

**A2 的三个子条件拆开看：**

```
peak_ok  = is_peak 且 jun_pos
is_peak  = peak_h 或 peak_w
           ├ peak_h = lbmax ≥ 4                                  （高度腿）
           └ peak_w = qz ≥ 0.97 且 upr ≥ 0.70 且 zt ≥ 5 且 lbn ≥ 1 （广度腿）
jun_pos  = 中军数据缺失（放行）或 jun > 0                       （用户硬条件）
hi_rising = lbmax ≥ prev_lbc                                    （龙头仍在创新高）
is_split  = zb_up 或 brkn ≥ 2
            └ zb_up = zb ≥ zb_base + 0.25                       （炸板率比自身近 20 日均值高 0.25）
```

**语义说明（A2 的 `hi_rising or not is_split`）**：
活跃态内用「龙头是否仍在创新高」划分高潮/分歧，是 V1.009.7 的核心修正 —— 旧版把 72 天「同时满足高潮条件却被判成分歧」的高潮延续日误判（涨停 8.71 家，比高潮组还多）。养家对「分歧」的定义是**龙头首阴**；龙头还在加速时的炸板/断板只是**内部换手**。

### 分支 B —— 昨日非活跃态（按 启动 → 发酵 → 冰点 → 潜伏 判断）

| 顺序 | 名称 | 触发条件 | 依据 |
|---|---|---|---|
| **B1** | 启动 | `not active_recent`（近 10 日无活跃）**且** `qz ≥ 0.85` **且** `upr ≥ 0.55` **且** `lbmax ≤ 2` **且** `zt ≥ 2` | 「近 10 日无活跃，今日涨停强度进前 15%、高度仅 N 板，梯队尚未成形」 |
| **B2** | 发酵 | `qz ≥ 0.85` **且** `upr ≥ 0.55` **且**（`lbn ≥ 1` **或** `zt ≥ 3`） | 「涨停强度进前 15%…」 |
| **B3** | 冰点 | `zt == 0` **且** `rs < -1.0` **且** `upr < 0.35` | 「无涨停、板块跑输大盘、上涨仅 x%」 |
| **B4** | 潜伏 | 以上都不满足 | 「未进入情绪周期（无涨停联动，或强度/扩散度不足）」 |

> **发酵 / 启动 不加中军硬条件**（用户 2026-09-17 裁定）。
> 实测依据：中位 `jun_pct` 按阶段 → 启动 **+2.770** > 发酵 +2.045 > 高潮 +1.253 > 分歧 +1.164 > 潜伏 +0.519 > 退潮 −2.030。
> 中军在「启动」最强、在「高潮」反而更低 —— 把中军设成发酵/启动的准入，等于用高潮的特征去卡启动，会把最强的启动信号整体拒掉，**语义搞反**。

**变量速查：**

| 变量 | 含义 |
|---|---|
| `rs` | 板块涨跌幅 − 上证指数涨跌幅（相对强度） |
| `upr` | 上涨家数 ÷ 有效成分股数（扩散度） |
| `zt` | 当日涨停家数（**剔 ST**，不复权口径） |
| `lbn` | 当日连板家数（`lbc2`） |
| `lbmax` | 当日板块内最高连板（`lbc_max`） |
| `brkn` | 断板家数 = 昨日涨停股今日未再涨停（含首板） |
| `zb_base` | 该板块近 20 日炸板率均值（样本 < 10 时禁用该判据） |
| `jun` | 中军涨幅 = 板块内**流通市值 top3**（东财 f21）平均涨幅 |
| `ow_n` | 一字板家数（**仅展示 + 高潮佐证，不改判定**） |

---

## 四、什么叫「退潮·中军腿」

**一句话**：退潮有**两条独立触发路径**（两条「腿」），A1 是「看板块整体转弱」，A1b 是「看大资金撤退」。

**为什么叫「腿」**：判定是**短路**结构 —— A1 未命中才轮到 A1b。两条腿任一条成立都判退潮。这个命名是为了在讨论「某天为什么判退潮」时能分开归因。

**A1b 的三个条件缺一不可：**

| 条件 | 阈值 | 常量 | 为什么需要它 |
|---|---|---|---|
| 中军转负 | `jun ≤ 0` | `_EMO_JUN_ON` | 这是**负向证据**本身 |
| 板块跑输大盘 | `rs < 0` | `_EMO_FADE_JUN_RS` | 护栏①：潜伏组约 **40%** 中军也为负 → 中军为负单独不足以判退潮 |
| 扩散已走弱 | `upr < 0.50` | `_EMO_FADE_JUN_UPR` | 护栏②：同上，必须叠加「板块已在走弱」才与「明显转弱」同义 |

**为什么是「加分项（或）」而不是「硬条件（且）」—— 这条是纯逻辑推导：**

> 退潮是**判负**，`jun ≤ 0` 是**负向证据**。
> - 写成硬条件（**且**）→ 要多一腿才成立 → 退潮**更难触发** → **方向搞反**
> - 写成加分项（**或**）→ 有独立触发权 → 退潮**更易触发** → 方向正确

**实测支撑**：退潮组仅 **7.1%** 中军为正（`jun ≤ 0` 命中 92.9%），其余组约 27%。
本腿对 ④ 指标本身的贡献 = **+2.3pp**（由 A2 臂隔离），对高潮段长度**无影响**。

**已知副作用**（本轮取证发现）：这条腿会把板块**踢出活跃态**，而分支 B **不认高度** —— 09-03 在线教育中军只跌 **0.47%** 就触发本腿，此后 09-04（中军 +0.91%、5 板）、09-07（中军 +1.02%、6 板）**中军已转正、高度还在推，依然回不来**，真实退潮是 09-09 —— **提前 4 个交易日宣布行情结束**。

---

## 五、你的口径修正：取证结果

### 5.1 现状转移矩阵（504 板块 × 72 日）

| 昨日 \ 今日 | 启动 | 发酵 | 高潮 | 分歧 | 退潮 | 冰点 | 潜伏 |
|---|---|---|---|---|---|---|---|
| **启动** | 0 | 151 | 12 | **201** ✗ | 229 | 0 | 0 |
| **发酵** | 0 | 455 | 92 | 398 | 648 | 0 | 0 |
| **高潮** | 0 | **33** ✗ | 41 | 54 | 69 | 0 | 0 |
| **分歧** | 0 | **268** ✗ | 53 | 175 | 332 | 0 | 0 |
| **退潮** | 0 | 92 | 0 | 0 | 0 | 208 | 977 |
| **冰点** | 107 | 93 | 0 | 0 | 0 | 468 | 2147 |
| **潜伏** | 468 | 514 | 0 | 0 | 0 | 2128 | 12921 |

**按你的语义判非法（✗）的三条入边，合计 502 天，占活跃态内转移 1933 的 26.0%：**

| 入边 | 天数 | 你的表态 | 语义问题 |
|---|---|---|---|
| 高潮 → 发酵 | **33** | ✅ 明确否掉 | 发酵是启动之后的扩散期，高潮后只应分歧/退潮 |
| 启动 → 分歧 | 201 | 未表态 | 分歧是**高位**概念，启动失败应走退潮 |
| 分歧 → 发酵 | 268 | 未表态 | 分歧后应「转一致→高潮」或退潮，不应退回扩散期 |

> **另注**：高潮 → 启动 = **0 天**（分支 A 根本不产「启动」），所以你说「不能从高潮后转入发酵」在代码里**只对应一条真实路径**，就是那 33 天。

### 5.2 五条臂的 A/B（每次完整重跑 `rebuild_board_daily + attach_board_phases`）

| 臂 | ④ 跨≥2级 | ⑦ 高潮转弱 | 炸板率比 | ≥3 天高潮段 | 高潮段均长 | 高潮天数Δ | 发酵天数Δ |
|---|---|---|---|---|---|---|---|
| **A1 现状** | **45.5%** | 62.4% | 2.01× | **2** | 1.26 天 | — | — |
| **L4 只封 高潮→发酵** | **45.0%** | 62.3% | 1.94× | **17** | **1.60 天** | **+48** | **−48** |
| L5 只封 启动→分歧 | 30.8% | 62.4% | 2.09× | 2 | 1.26 天 | 0 | 0 |
| L6 只封 分歧→发酵 | 27.6% | 62.4% | 1.80× | 2 | 1.26 天 | 0 | −397 |
| L1 三条全封（维持前态） | 18.4% | 62.3% | 1.79× | 17 | 1.60 天 | +48 | −322 |
| L2 全封·高潮后改判分歧 | 18.4% | **79.2%** ✗ | 1.82× | 2 | 1.26 天 | 0 | −322 |
| L3 全封·高潮后改判退潮 | 19.2% | **80.0%** ✗ | 1.85× | 2 | 1.25 天 | −7 | −313 |

### 5.3 三个关键发现

**① 你提的那一条，正好精准命中「高潮段被切碎」这个 bug，且几乎零副作用。**

- ④ **45.5% → 45.0%（−0.5pp）** —— 几乎不动
- ⑦ 高潮转弱 62.4% → 62.3% —— 不动
- 炸板率比 2.01× → 1.94× —— 仍达标
- **≥3 天高潮段 2 → 17 个（8.5 倍）**，均长 1.26 → 1.60 天

**② ④ 的恶化主要来自另外两条**（你不曾提及的外推项）：只封「启动→分歧」④ −14.6pp，只封「分歧→发酵」④ −17.9pp，而两者对高潮段长度**毫无影响**。这解释了 ④ 与「模型可用性」为何长期对不上。

**③ 「把高潮后的日子改名成分歧/退潮」救不了段长度。**
L2 / L3 的 ④ 也降到 18.4% / 19.2%，但 **高潮段分布与现状一字不差**（还是 `{1:118, 2:37, 3:2}`），且 ⑦ 恶化到 79~80%（分歧/退潮组被污染）。
→ **真正的损失是「段被打断」，不是「名字起错」。** 这直接排除了「换标签」类的作弊式修法。

### 5.4 「高潮 → 发酵」这 33 天长什么样（与同源转移对照）

| 转移 | n | 扩散度中位 | 涨停中位 | 最高板中位 | 断板中位 | 中军中位 |
|---|---|---|---|---|---|---|
| **高潮 → 发酵** | 33 | **0.732** | 3.0 | 3.0 | 3.0 | −0.45% |
| 高潮 → 高潮 | 41 | 0.607 | 4.0 | 5.0 | 3.0 | +1.17% |
| 高潮 → 分歧 | 54 | 0.503 | 3.0 | 2.0 | 5.5 | +0.25% |
| 高潮 → 退潮 | 69 | 0.253 | 1.0 | 1.0 | 4.0 | −1.93% |

**「高潮→发酵」的扩散度比真正的高潮延续还高（0.732 vs 0.607），断板数完全一样（3.0）。**
它们的唯一「罪状」是：最高板从 ≥4 掉到 3（高度腿失效）+ 次日中军未转正 → `is_peak=False` → 掉出 A2 → 落到 A4。

**这不可能是发酵。** 举例（原判「发酵」）：

| 板块 | 日期 | 涨幅 | 涨停 | 最高板 | 连板 | 断板 | 扩散 | 中军 |
|---|---|---|---|---|---|---|---|---|
| 小金属概念 | 06-15 | +3.75% | **12** | 3 | 4 | 8 | **86%** | +3.03% |
| CPO概念 | 08-07 | +3.81% | 6 | 4 | 3 | 3 | **87%** | −1.34% |
| AIGC概念 | 09-01 | +1.96% | 6 | 3 | **5** | 6 | 76% | −1.46% |
| 东数西算 | 08-05 | +2.12% | 1 | 2 | 1 | **10** | 71% | +0.30% |

### 5.5 具体案例：只封这一条之后

**鸿蒙概念（BK0953）· 2026-07-30 ~ 08-05 —— 最高板 4→5→6→7→8 连推五级**

| 日期 | 涨幅 | 涨停 | 最高板 | 断板 | 扩散 | A1 现状 | L4 |
|---|---|---|---|---|---|---|---|
| 07-30 | −1.17% | 3 | 4 | 4 | 28% | 高潮 | 高潮 |
| 07-31 | +4.55% | 5 | 5 | 1 | **95%** | 高潮 | 高潮 |
| **08-03** | **+1.21%** | **4** | **6** | 4 | **71%** | **发酵** ✗ | **高潮** |
| 08-04 | +2.58% | 5 | 7 | 2 | 87% | 高潮 | 高潮 |
| 08-05 | +1.97% | 5 | 8 | 3 | 81% | 高潮 | 高潮 |

**职业教育（BK1022）完全同步**：07-30 ~ 08-05 最高板 4→5→6→7→8，08-03（1 家涨停、最高板 6、扩散 76%）被切成发酵。

**在线教育（BK0662）**：07-30 ~ 08-06 同样是 4→8 板连推 6 级，08-03 被切；另有 09-01 一处（启动→高潮→**发酵** ⇒ 启动→高潮→**高潮**→分歧→退潮，补成一个完整周期）。

**CPO概念（BK1128）**：08-05 发酵 → 08-06 高潮 → **08-07 高潮**（原为发酵）→ 08-10 退潮。

> **三个板块在 2026-07-31 ~ 08-05 被同一条规则同步切碎** —— 这正是那波 AI/科技主线的整体主升段。对聚焦 AI/科技成长赛道的使用场景，这一条的杀伤是系统性的。

---

## 六、建议

### 建议 A（**推荐**）：只封「高潮 → 发酵」这一条

**修法**（最小改动，约 6 行）：在 `_board_phase` 分支 A 的 A4 之前加一道校验 ——

```python
# A4 发酵
if prev_stage != "高潮":
    if (lbn >= 1 or (qz is not None and qz >= _EMO_FERM_QZ)) and upr >= _EMO_FERM_UPR:
        return "发酵", _why
# 高潮后不落发酵（语义：发酵只能由启动来）→ 维持高潮
if prev_stage == "高潮":
    return "高潮", _why + "；未现转弱亦无撕裂，判为高潮延续"
```

**收益**：≥3 天高潮段 **2 → 17**、均长 1.26 → **1.60 天**；④ 仅 **−0.5pp**；⑦ / 炸板率 / 其余阶段分布全不动。
**风险**：无新增参数、无新数据依赖，只影响 `prev_stage == "高潮"` 的一个分支。

> **更稳的工程形态**：把约束抽成常量表 `_EMO_ILLEGAL = {("高潮","发酵")}` + 在函数末尾统一校验，以后加约束只改常量、不动分支。这样「哪条转移不合法」变成**可读、可配置、可测**的一等公民，而不是散落在 if 里的隐含假设。

### 建议 B（**待你拍板**）：另两条是否也封

| 项 | 封的代价 | 封的收益 | 需要你先确认的语义 |
|---|---|---|---|
| 启动 → 分歧（201 天） | ④ −14.6pp | ④ 更接近「真跨级」定义 | 启动试错失败，算**退潮**还是**分歧**？ |
| 分歧 → 发酵（268 天） | ④ −17.9pp | 同上 | 分歧之后能否退回扩散期？还是只能「转一致→高潮」或退潮？ |

三条全封 → ④ **18.4%**、≥3 天段 **17**、炸板率 1.79× 仍达标；代价是「高潮后不撕裂不转弱的日子」统一判为**维持高潮**（+48 天）。

### 建议 C（独立于以上）：分支 B 的「高度腿」缺失

现状：**分支 A 认「最高板 ≥ 4」，分支 B 完全不认高度**，只剩强度分位一条腿。
后果：板块一旦被降级出活跃态（**最容易由 A1b 退潮·中军腿触发**），**最高板推到 8 板也回不来**。
在线教育 09-03 中军仅 −0.47% → 跌出活跃态 → 09-04（+0.91%、5 板）、09-07（+1.02%、6 板）**永久停在「潜伏」**，真实退潮 09-09，**提前 4 个交易日**。
**这条要单独跑标定**（修改核心状态机分支，影响面大于建议 A）。

---

## 七、局限（不做外推声明）

1. **样本窗口 72 天**（2026-06-09 ~ 09-17）—— 只覆盖一段行情结构，跨周期泛化性未验证。
2. **④ 指标本身已被证明「极易被做漂亮」**：加「分歧滞后」探针后 ④ = 1.6%，代价是分歧从 828 条掉到 74 条（**指标达标、模型报废**）。故本报告不以「④ 降到多少」作为唯一验收标准，**并列给出高潮段长度**作为反向护栏。
3. **A0（V1.009.7 口径）的 813 天/236 天等数字**来自另一套实现（无中军字段），**不能当作生产基线做因果推断** —— 本轮所有对比都在同一套缓存与同一版实现内完成。
4. **`_emo_z` 的规模歧视（机器人概念那类）本轮未处理**，它是独立缺陷，修它需重跑标定。
5. 取证脚本 `_trans_matrix.py` **未入库**（被 `.gitignore` 的 `_*.py` 覆盖）—— 与 `_calib_04.py`、`_case_hi_split.py` 同属一次性诊断脚本，报告内的数字已足够复现结论。

---

## 八、一句话总结

**你的这条口径修正，是三个缺陷里性价比最高的一个** —— 它精准命中「高潮段被切碎」这个真实 bug（≥3 天段 2 → 17），代价只有 ④ 指标 0.5pp，且不需要新数据、不引入新参数。而 ④ 指标的长期恶化，主要责任在另外两条你没提的入边上 —— **那两条要不要动，取决于你对「启动失败」和「分歧之后」的语义定义，这个我不替你定。**
