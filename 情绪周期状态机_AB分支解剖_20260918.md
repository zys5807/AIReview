# 情绪周期状态机：为什么有 A/B 两套判据 + 是否该推倒重来

> 日期：2026-09-18
> 触发：升升 09-18 晚三条裁定 + 质疑「我不懂为什么还有 A/B 两套方案。不得已的情况下，需要推倒重来」
> 状态：**只读诊断，未改任何生产代码、未动版本号、未写数据库**
> 脚本：`backend/_doc3_final.py`、`backend/_doc3_ab.py`

---

## 0. 先确认你的三条裁定（我的理解）

| # | 你的裁定 | 我的落实 |
|---|---|---|
| ① | **高潮、分歧、发酵、冰点都是可以连续和延续的** | 四态**允许自环** |
| ② | **只有启动和退潮是一日状态** | 两态**禁止自环**（结构性） |
| ③ | **先验概率是拍脑袋决定的，不能直接用于实际生产** | 文档的先验矩阵**只作对照基准，不进生产** |

✅ **三条我全部接受。** 特别第 ③ 条 —— 我在上一份报告里把它列为「⭐⭐⭐ 建议做」，**现在撤回这个建议**。我犯的错是：把「元宝给的数字」当成了「有依据的数字」，但那些 0.7/0.4/0.5 确实只是他凭经验拍的。**你是对的。**

**按你的新裁定重算的违规率**：

| 边表 | 违规 | 违规率 |
|---|---:|---:|
| 旧版（09-18 白天定稿） | 502 / 4,179 | 12.01% |
| **新版（你 09-18 晚三条裁定）** | **126 / 4,179** | **3.02%** |

**违规只剩两条**：
- `冰点 → 发酵` **93 条**
- `高潮 → 发酵` **33 条**（你早已明确否掉的那条）

**你的三条裁定直接消掉了 376 条「违规」**（分歧→发酵 268 + 启动→分歧 201 现在都合法了）。这印证了一件事：**之前的 16.4% 违规率里，大部分是「边表比你的实际口径更严」造成的假阳性，不是判据真的错了。**

---

## 1. 核心问题：为什么会有 A/B 两套方案？

### 1.1 设计意图（源码自证）

`daily_market.py:1249-1251` 的注释写得很清楚：

> ③ 六阶段是「环」不是「轴」：启动与退潮强度都低但方向相反、分歧强度不低但含义是「高位撕裂」
> → **状态机**（活跃态只能在活跃态内演化），否则会出现「退潮→高潮」这种**无中间过程的跳跃**。

`daily_market.py:1253` 是这个设计的落点：

```python
_EMO_ACTIVE = ("启动", "发酵", "高潮", "分歧")     # ← 冰点/退潮/潜伏 不在里面
```

`daily_market.py:3404 / 3467`：

```python
if prev_stage in _EMO_ACTIVE:
    # 分支 A：A1 退潮优先 → A1b 退潮·中军腿 → A2 高潮 → A3 分歧 → A4 发酵 → A5 兜底
else:
    # 分支 B：B1 启动 → B2 直判发酵 → B3 冰点 → B4 兜底潜伏
```

### 1.2 但实测暴露了一个更本质的事实 —— 分支 A 其实不看 `prev_stage` 是谁

**探针 1（把昨日态强行设为各值，跑分支 A 判据）**：

| 昨日 = | 输出分布 |
|---|---|
| 冰点 | 冰点 11.5%（**其余全空**） |
| 启动 | 发酵 42.0% / 退潮 47.8% / 分歧 8.8% / 高潮 1.4% |
| 发酵 | 发酵 42.0% / 退潮 47.8% / 分歧 8.8% / 高潮 1.4% |
| 高潮 | 发酵 42.0% / 退潮 47.8% / 分歧 8.8% / 高潮 1.4% |
| 分歧 | 发酵 42.0% / 退潮 47.8% / 分歧 8.8% / 高潮 1.4% |
| 退潮 | 冰点 11.5%（**其余全空**） |

**启动/发酵/高潮/分歧 四行的输出分布逐位完全相同。**

⇒ **分支 A 根本不在乎你昨天是启动、发酵、高潮还是分歧 —— 它只需要知道「你在周期内」。**

**所以 A/B 的真实分工不是「两套判据」，而是：**

| 分支 | 真实语义 | 回答的问题 |
|---|---|---|
| **A** | 「**已经在周期内**，今天怎么演化」 | 是延续？走弱？还是到顶？ |
| **B** | 「**还在周期外**，今天要不要进来」 | 够不够格进（启动）？还是继续待在外面（潜伏/冰点）？ |

**这个设计是有道理的**，因为这两种情形的**可选项根本不同**：
- 已经在周期内 → 只能沿环演进（不能凭空退回「潜伏」，那会丢失历史）
- 还在周期外 → 只有「进」或「不进」两个选择

**但实现上出了两个问题**（下面第 2、3 节详述）。

---

## 2. ⛔⛔ 分支 B 的结构性缺陷 —— 这是「推倒重来」的真正理由

### 2.1 决定性证据

**探针 2 实测**：全期「昨日非活跃」且「当日最高板 ≥ 4」的样本 = **502 条**

| 它们的实际输出 | 条数 |
|---|---:|
| **潜伏** | **439（87.5%）** |
| 发酵 | 63（12.5%） |
| **高潮** | **0** |
| 分歧 | 0 |

**⇒ 分支 B 里压根没有「高潮」和「分歧」这两条出路。最高板推到 4 板、5 板、甚至 8 板，只要昨天不在活跃态里，今天**永远不可能**判成高潮。**

你之前已经发现过这个问题的另一个侧面：

> ⚠️ **A/B 准入不对称：B 分支完全不认高度** —— 被踢出活跃态后（最易被**退潮·中军腿**踢），最高板推到 8 板也回不来（131 天里 **129 天** `jun ≤ 0`）

**这次是它的完整版量化**：502 条高标被拦，其中 **439 条（87.5%）落到「潜伏」** —— 也就是前端显示成「无周期」的那个态。

### 2.2 后果链

```
某板块因一天走弱被判「退潮」（退潮是一日态）
  ↓ 次日 prev_stage = 退潮，不在 _EMO_ACTIVE 里
  ↓ 落到分支 B
  ↓ 分支 B 只有 B1 启动（要求 active_recent=False，即近 10 日无活跃）/ B2 发酵 / B3 冰点 / B4 潜伏
  ↓ 而 active_recent 因「刚活跃过」= True → B1 锁死
  ↓ 只剩 B2 / B3 / B4
  ↓ 最高板 8 板也只能判「发酵」或「潜伏」
  ↓ 前端 stageLabel 把「潜伏」渲染成「无周期」
  ↓ 你看到「今日启动 + 无周期」（汽车整车 9/17 就是这个）
```

**这条链就是「汽车整车 9/17 今日启动 + 无周期」的根源**，也是「发酵需启动前置 62.27% 违规」的根源（B2 直判发酵绕过启动，699 天）。

### 2.3 ⭐ 根本矛盾：`active_recent` 与「退潮一日态」的设计冲突

```python
active_recent = 近 _EMO_NEW_LOOKBACK(10) 日内是否出现过活跃态
```

B1 启动要求 `not active_recent`，即「近 10 日无活跃」。

**但如果一个板块是「高潮 → 分歧 → 退潮 → 重新启动」这样一个完整的周期循环，`active_recent` 必然是 True** —— 因为 10 天窗口内一定抓到过活跃态。

⇒ **`active_recent` 的 10 天窗口，直接把「同一个周期的二轮启动」全部锁死了。**

而你的口径（6-24 原文）明确说：「**退潮后可以接启动，可以接冰点**」。

**这是设计与口径的直接冲突，不是阈值调参能解决的。**

---

## 3. ⛔ 分支 A 的问题：退潮也被塞进了「活跃态内」

### 3.1 一个自我矛盾的实现

`_EMO_ACTIVE = (启动, 发酵, 高潮, 分歧)` —— **退潮不在里面**。

但 A1 判据却是：

```python
if prev_stage in _EMO_ACTIVE:      # 进分支 A
    if rs < _EMO_FADE_RS and upr < _EMO_FADE_UPR and zt <= _EMO_FADE_ZT:
        return "退潮", ...          # ← 在分支 A 里输出「退潮」
```

**退潮是在分支 A 里被产生的**（实测：退潮 1,278 段，全部来自分支 A），但它**自己不能被写进 `_EMO_ACTIVE`**，否则第二天就会走分支 A 而破坏「一日态」。

⇒ **`_EMO_ACTIVE` 这个名字是误导性的**。它实际的含义是：**「用过的、需要沿环演化的态」**，而不是「活跃的态」。退潮明明在语义上也是活跃态的一部分。

**Fork 3 实测佐证**：一旦把 `_EMO_ACTIVE` 扩到全部 6 态（让退潮也走分支 A）：

| 方案 | 违规率 | 退潮自环 | 退潮段均长 |
|---|---:|---:|---:|
| **A1 现状（A/B 两分支）** | **3.02%** | **0** | **1.00 天** |
| D1 全走分支 A（6 态都当活跃） | **21.20%** | **4,255** | **1.81 天（最长 10 天）** |

**⇒ D1 直接把「退潮是一日状态」这条铁律砸烂了**（4,255 个自环、均长 1.81 天）。

**这解释了为什么退潮必须被排除在 `_EMO_ACTIVE` 之外** —— 不是为了「防退潮→高潮跳跃」（注释里写的理由），而是因为**分支 A 内部没有「自环禁止」机制**，一旦放进去，退潮就会自己延续。

### 3.2 那注释里写的「防退潮→高潮跳跃」防住了吗？

**没有。**

D1 实测：退潮走分支 A 时，它的输出是 `{发酵 3074, 高潮 74, 分歧 546, 退潮 4255}` ——
**退潮→高潮 74 条**，确实出现了「退潮 → 高潮」的跳跃，而且**同时**产生了 4,255 个自环。

⇒ **注释里的理由只对了一半**：把退潮排除确实**间接**避免了「退潮→高潮」，但代价是引入了 A/B 两套判据。**真正该做的是「在分支 A 内禁止退潮自环」，而不是「把退潮赶出分支 A」。**

---

## 4. 该不该推倒重来？—— 我的实测判断

### 4.1 三方案对照（含取消 A/B 的两种改法）

| 方案 | 活跃态转移 | 违规率 | 启动自环 | 退潮自环 | 高潮条数 | 分歧条数 |
|---|---:|---:|---:|---:|---:|---:|
| **A1 现状（A/B 两分支）** | 4,179 | **3.02%** | **0** ✅ | **0** ✅ | 198 | 828 |
| D1 全走分支 A | 21,416 | **21.20%** | 0 ✅ | **4,255** ❌ | 433 | 2,835 |
| D3 全走分支 B | 1,339 | 12.99% | **174** ❌ | 0 ✅ | **0** | **0** |

**⇒ 取消 A/B 的两条路都是死路**：
- **D1**：退潮一日态彻底失效（4,255 自环，均长 1.81 天、最长 10 天）
- **D3**：高潮和分歧**完全消失**（0 条），启动一日态失效

**所以「A/B 两分支」这个结构本身是必要的，但它的实现方式有问题。**

### 4.2 ⭐ 我已经把改法的对照臂跑出来了（只读，未改生产）

核心洞察：**A/B 的问题全部来自「用 `prev_stage in _EMO_ACTIVE` 这一个大条件」来切换，而这个条件是「状态归属」，不是「判定需求」。**

**真正的判定需求是这样的**：

| 情形 | 需要什么判据 |
|---|---|
| 板块**在周期内** | 沿环演化：退潮？高标？撕裂？延续？ |
| 板块**在周期外** | 要不要进来：够格启动吗？不够就潜伏/冰点 |

**关键：`prev_stage = 退潮` 的板块，语义上「仍在周期内」（它刚走完一轮），应该走分支 A 的判据，但必须在分支 A 内部禁止退潮自环。**

#### 四臂实测结果

| 臂 | 改法 | 活跃态转移 | 违规率 | 启动自环 | 退潮自环 | 一日态 |
|---|---|---:|---:|---:|---:|---|
| **A1** | 现状 | 4,179 | 3.02% | 0 ✅ | 0 ✅ | 成立 |
| **E1** | 退潮纳入 A，**不加护栏** | 18,221 | **19.69%** | 0 ✅ | **3,514** ❌ | **破了** |
| **E2** | 退潮纳入 A **+ 一日态护栏** | 8,008 | **2.58%** | 0 ✅ | 0 ✅ | **成立** |
| **E3** | E2 **+ B2 直判发酵 → 启动** | 8,008 | **0.67%** | 0 ✅ | 0 ✅ | **成立** |

**违规明细**：

| 臂 | 违规内容 |
|---|---|
| A1 | `冰点→发酵` 93、`高潮→发酵` 33 |
| E1 | `退潮→退潮` **3,514**、`高潮→发酵` 74 |
| E2 | `冰点→发酵` 153、`高潮→发酵` 54 |
| **E3** | **只剩 `高潮→发酵` 54** |

#### ⭐ E3 的成绩

| 指标 | A1 现状 | E3 |
|---|---:|---:|
| 违规率 | 3.02% | **0.67%** |
| 违规条数 | 126 | **54** |
| 启动自环 | 0 ✅ | 0 ✅ |
| 退潮自环 | 0 ✅ | 0 ✅ |
| 高潮段均长 | 1.26 天 | 1.22 天 |
| 高潮 ≥3 天段 | **2** | **46** ⭐ |
| 分歧自环 | 175 | 290 |
| 发酵段均长 | 1.39 天 | 1.57 天 |

**⭐ 高潮 ≥3 天段从 2 个 → 46 个** —— 你想「**高潮可以连续**」，A1 现状只做出 2 个 ≥3 天的段（`jun>0` 硬条件切碎），**E3 做出 46 个**。这条直接对上你的裁定 ①。

#### ⚠️ 但代价很大：**15.2% 的天数判定会变**

A1 → E2 逐板块逐日差异：**3,597 / 23,668 = 15.2%**

| 变化组合 | 天数 |
|---|---:|
| 潜伏 → 发酵 | **1,352** |
| 潜伏 → 冰点 | **655** |
| 潜伏 → 退潮 | **585** |
| 潜伏 → 分歧 | **576** |
| 冰点 → 退潮 | 222 |
| 启动 → 发酵 | 57 |
| 潜伏 → 高潮 | 49 |
| 发酵 → 高潮 | 36 |

**⇒ 改动的主效果是「把原本判成潜伏（前端显示『无周期』）的板块拉进周期」**，合计 3,000+ 天。

**这既是收益也是风险**：
- **收益**：你说的「无周期」自相矛盾问题（今日启动 + 无周期，28.76%）会大幅缓解
- **风险**：有些板块可能**不该进周期却被拉进来了**（尤其「潜伏→退潮」585 天、「潜伏→冰点」655 天 —— 这两个方向是**变差**，把一个无周期的板块直接说成退潮/冰点）

#### 汽车整车这条案例：**E2/E3 没有改善**

| 日期 | A1现状 | E1 | E2 | E3 |
|---|---|---|---|---|
| 2026-09-14 | 启动 | 发酵 | 启动 | 启动 |
| 2026-09-15 | 退潮 | 退潮 | 退潮 | 退潮 |
| 2026-09-16 | 冰点 | 退潮 | **冰点** | **冰点** |

⇒ 汽车整车 9/16 在 E2/E3 下**仍判冰点**（9/17 已不在有效成分股范围内）。

**说明 E2/E3 治的是「退潮之后的板块能否重新走高」，治不了「一个板块如何从冰点走到发酵」。** 后者是冰点→发酵 93/153 条违规那个问题，属于另一条线。

### 4.3 关于「推倒重来」

**我的判断：不必推倒，但必须动结构。而且现在不动才是更危险的。**

理由：
1. **A/B 的「分工」是对的**（D1/D3 实测证明取消会坏）
2. **但切换条件是错的**（用「状态归属」代替了「判定需求」）
3. **E2/E3 已经给出了一条可行的最小改动路径**，且**两条铁律（启动/退潮一日态）都保住了**
4. **不改的代价**：502 条高标被拦（439 条显示成「无周期」）、发酵需启动前置 62.27% 违规、`jun>0` 切碎高潮（≥3 天段仅 2 个）

**但 15.2% 的判定变化量太大，不能我一个人拍。** 需要你先看方向对不对，再决定要不要走这条路。

### 4.4 如果决定改，我建议的顺序

```
① 先出「逐日逐板块差异清单」（3,597 天的前后对照，可导出 CSV）→ 你人工抽查 20~30 天
② 抽查通过 → 改 E2（退潮纳入 A + 一日态护栏），重建历史，UI 自证截图
③ E2 验收通过 → 再定 B2 直判发酵（E3）是否跟进
④ 全程不动版本号，直到你确认
```


---

## 5. 一句话回答你的三个问题

**① 为什么有 A/B 两套方案？**
因为六状态是「环」不是「轴」，必须区分「**在周期内演化**」和「**在周期外决定要不要进**」。这个**分工是对的**（实测取消它会坏掉：退潮自环 4,255 或高潮分歧全消失）。

**② 但它为什么让你觉得麻烦？**
因为切换条件用错了。`prev_stage in _EMO_ACTIVE` 是**状态归属**，不是**判定需求**。结果：
- 退潮明明在周期内，却被赶去分支 B（注释里的理由是「防跳跃」，但那其实是**另一个问题**）
- 分支 B 里没有高潮/分歧的路 → **502 条高标被拦、439 条落到「潜伏」**
- `active_recent` 的 10 天窗口把「退潮→启动」的二轮启动全部锁死，**与你的口径直接冲突**

**③ 该不该推倒重来？**
**不必推倒，但要动结构。** 最小改法是「把退潮纳入分支 A + 在分支 A 内加一日态护栏」，这既保留 A/B 的合理分工，又拆掉了三个已知缺陷的根。

**我已经把对照臂跑出来了**：
- **E2**（退潮纳入 A + 一日态护栏）：违规 3.02% → **2.58%**，两条铁律保住
- **E3**（E2 + B2 直判发酵改判启动）：违规 3.02% → **0.67%**，**高潮 ≥3 天段 2 → 46 个**（对上你「高潮可以连续」）

**⚠️ 但代价是 15.2% 的天数判定会变**，主要是「潜伏 → 发酵/冰点/退潮/分歧」3,000+ 天。**这个量级需要你先看方向，不能我一个人拍。**

---

## 6. 需要你定

| # | 事项 | 我的建议 |
|---|---|---|
| **A** | **是否走 E2/E3 这条路？** | 我先出「逐日逐板块差异清单（3,597 天）」供你抽查 20~30 天，你确认方向再改生产 |
| **B** | 15.2% 的判定变化量能否接受？ | 其中「潜伏→退潮」585 天、「潜伏→冰点」655 天是**变差方向**（把无周期板块直接说成退潮/冰点），需要你判 |
| **C** | B2「直判发酵」是否改判「启动」（E3）？ | **建议等 E2 验收后再说** |
| **D** | 冰点→发酵 93 条 | 与 C 同根（都是 B 分支直判）。⚠️ **E2/E3 没能消掉这条**（E2 反而 153 条），需单独治 |
| **E** | 文档先验矩阵用途 | 按你裁定：**只作对照基准，不进生产**。已撤回相关建议 |
| **F** | 「今日启动」与阶段是否打通（28.76%） | 待定；E2/E3 会间接缓解但治不了根 |
| **G** | ④ 指标降级 / 文案 bug | 前者建议先做，后者零风险 |

---

## 附：本次用到的脚本（均只读）

| 脚本 | 作用 |
|---|---|
| `backend/_doc3_final.py` | 新边表违规率重算 + A/B 分支交叉表 + 取消 A/B 的三种改法 |
| `backend/_doc3_ab.py` | 探针 1（分支 A 是否看 prev）/ 探针 2（502 条高标被拦）/ 探针 3（D1/D3 对照） |
| `backend/_doc3_arm.py` | **四臂对照**（A1 现状 / E1 无护栏 / E2 候选 / E3 更激进）+ 3,597 天差异清单 + 汽车整车案例 |

**生产代码未改一行，数据库未写一个字节，版本号未动。**
